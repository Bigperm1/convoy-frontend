<!-- ═════════ RULE #1 — READ THIS BEFORE ANYTHING ELSE ═════════ -->

## OTA-AP — 2026-09-11 22:2x PDT · group `a4b9d748-4189-47e8-829c-1611e81fa2b1` · commit `9aa55b1` · runtime 1.28.0
`KEY_PRESENT=1` on both platforms (`openweathermap=2  neg_control=0`). Build 78 / channel `mapbox-migration`, parity verified on both platforms before publish.

1. **The follow camera's TILT is now one fixed angle (`src/chasePitch.ts`, `CHASE_PITCH_FIXED = 48`).** It used to lerp 48→60 between 45 and 95 km/h. Measured on Jeff's drive home (`ogb3m4-967731`, 88 cam-probe rows, 31.4 min): **168° of tilt travel with 20 direction reversals**, the horizon rising and falling with every gap in traffic. The slew limiter and the low-pass were already there and did not help — they smooth the path while the *target* churns. Prior art (fetched + citation-checked): Mapbox `defaultPitch = 45.0`, Google 45, MapLibre iOS 45; none drive pitch from speed, and the two that vary it LOWER it near a maneuver. Gate `tools/sim-qc/chase_pitch_test.mts` + trap rule 31, both negative-control verified.
2. **The imminent turn callout outranks the 1.5 s rate gate** (`38c73a1` + call-site trap rule, `bb2196a`). Jeff's 09-11 drive dropped "Turn right." 0.77 s behind the reroute's prepare cue.
3. **A CarPlay warm-root failure now falls back to the cold root** (`b3a8bfc`, `1edd71f`) and logs `carplay-root` / `carplay-root-failover`. Previously the phone map screen claimed the root, the cold bootstrap stood down, and a failure left the head unit with no template and zero telemetry.

**NOT in this OTA and NOT approved:** the zoom ceiling near 16 and the speed-debounce on the zoom input. **Not touched:** `FOLLOW_LOWER_PAD_FRAC` / `CAR_LOWER_PAD_FRAC` — Jeff: "do nto touch the car position".

**Read after the next drive:** `cam-probe surf=car` — `p=` and `pt=` must be 48 on every row regardless of `spd=`. Then `carplay-root` on the next CarPlay connect, and `tts-skip why=rate` should no longer carry a bare-verb `len=`.

# 🛑 NO GUESSING. NO THEORIZING. NO HALLUCINATING.

**Every claim is VERIFIED (ran the query · read the file · measured it · asked Jeff — with the receipt) or the word
HYPOTHESIS is said out loud.** Reading code is NOT verification. Check the instrumentation that ALREADY EXISTS before
inventing an explanation. "I don't know — here is the ONE check that settles it" is a GOOD answer. Full text: `CLAUDE.md`.
<!-- ═════════ END RULE #1 ═════════ -->

# HANDOFF — 2026-09-07 (morning) — CURRENT STATE

**This file is CURRENT STATE.** It supersedes `HANDOFF-2026-09-06.md` (now the 09-05 → 09-06 log: keep it for the
root-cause write-ups and the receipts of each fix) and `HANDOFF-2026-09-02.md` (the 09-02 → 09-05 log). Every number
below was read live on 2026-09-07 between 07:50 and 08:10 PDT — git, EAS, the Render health endpoint, Supabase, Mongo,
`crash_reports` — and every gate was run at the current HEAD. Line numbers drift: **locate by the quoted text.**

**Read order for a new session:** `RULES.md` → this file → `ROADMAP.md` (ranked issues, launch table) → `CLAUDE.md`
(conventions) → the dated logs only when you need the history of a specific fix.

---

## 0 · The one-screen summary

| what | state (VERIFIED 2026-09-07 08:00 PDT) | receipt |
|---|---|---|
| Native builds | **BUILD 78 on BOTH, runtime 1.28.0**, channel **`mapbox-migration`**, cut 2026-09-11 00:16 PDT from `58b7142` — iOS `a6b63840` → TestFlight (Apple accepted the upload 00:3x; processing → "3.10.0 (78)"), Android `bdc6100e` → Play internal COMPLETED (Android 77 `5df23053` sits underneath it). iOS 77 `aa691ab3` was REFUSED by App Store Connect (no watch-app icon, ITMS-90713/90391) — never install it. Next cut = **79 on BOTH** (paid, Jeff's go). ⚠ The first OTA after this reaches 1.28.0 ONLY — every 75/76 install is orphaned until it takes 78 | `app.json`; `eas build:list` (read 09-11 00:35); §0b |
| Latest OTA | **OTA-AO** 2026-09-11, group `048b95b2-5ca3-4d3a-bfb1-d205b51ed62f`, code `0d7c6c5`, runtime **1.28.0** (the FIRST OTA at 1.28.0 — it reaches build 78 ONLY; every 75/76 install is orphaned until it takes 78), KEY_PRESENT=1 both. Carries: the off-route HEADING fast path with its lot-storm guards (join + rolling growth + clear-on-trip), the nose judged against the full-rate course (the on-line sharpness half was reviewed out), the greeting no longer sending the saved-place label as the weather city (kills "It's cloudy in Work" against the UNCHANGED live backend) + a generation token so a superseded prepare cannot speak the old destination, and three receipts (`cam-fetch`, `bc-events`, `pin-imgs`) to name the author of the 25 s exit-ramp frame gap. ⚠ The greeting's TEMPERATURE, "when you arrive" and varied wording need the five backend commits Jeff has not pushed. **Rows to read after the next drive:** `off-route tripped … why=heading` (did the fast path fire, and did the drive end with exactly one reroute?), `cam-fetch`/`bc-events`/`pin-imgs` immediately before any `main-gap`, `pose-fix … estHdg` vs `course` through corners, `greet-prep text=` | `eas update:list --json`; `tools/ota/verify-bundle-key.py` |
| Previous OTA | **OTA-AN** 2026-09-10 21:39 PDT, group `74f5918d-0755-4ce6-85c1-1cfcda0f71fc`, code `626e37b`, both platforms, KEY_PRESENT=1 both (control AM `dced6a0d` = 1 both) — (1) the Clearbrook overpass triple ding: direction-aware posted-limit snap (`src/speedLimitSnap.ts`), the double chime chained after the first finishes, the route greeting's text finally logged (`greet-prep`); (2) the NEON RING pins (cameras, DriveBC events, crew reports, place pins; sim-verified on a live DriveBC event); (3) THE SELF CAR IS LIFTED ONLY OFF THE ROAD — 0 on a road, 10 m (arrow 16) in a lot / driveway / a footprint's roof + 2, from the app's own streets-v8 source with Mapbox's idle event as the proof (7 commits `814aba2`→`f3afa18`, Codex ×9, sim receipts on Marshall Rd / a driveway / the Sevenoaks lot / the Superstore footprint / a real route on the iOS 27 sim); (4) `ios-stack` receipts for Rodrigo's dead-buttons report. §1b has every row. **FIELD-UNVERIFIED — the rows to read after Jeff's next drive:** `self-lift to= why=` only `road|speed|nav` on roads (never `offroad` at a light), `why=offroad lot=1` in his driveway, `self-lift q … ms=` (a phone's query cost), `ribbon-trim lift=0` on the route, no `main-gap` at nav start; `speed-limit … x=50@…` rejected under the Clearbrook overpass with no `speed-alert`; `greet-prep text=` at route start; the neon camera pins on a routed drive; CarPlay rows `surf=car`. Before it: OTA-AM `dced6a0d` (the big-3 location architecture), AL `21226ff7`, AK `98340bcb` | `eas update:list --json`; `tools/ota/verify-bundle-key.py` |
| Backend | `~/convoy-backend` master **`629d3f5` LIVE** on Render (security release + event pushes + notification category + arrival quip) | `GET /api/health` → `{"ok":true,"weather_key":true,"supabase":true,"rev":"629d3f5"}`; `/api/health/supabase` → `{"enabled":true,"probe_status":200,"rows":1,"ok":true}` |
| Supabase | migration `20260906000100_account_scan_recovery` applied; `scan_jobs_for_handle(text)` EXECUTE = `postgres, service_role` only | `information_schema.routine_privileges`, re-read 08:00 |
| Git | frontend `mapbox-migration` = origin = **`0d7c6c5`** (= the PUBLISHED OTA-AO code); was `58b7142` (build 78 bump; `5a02e68` watch icon; `b4b6208` build 77 bump; `626e37b` = the PUBLISHED OTA-AN code); backend master = origin = `629d3f5`. Always-dirty, never-committed: `.claude/skills/ship-ota/SKILL.md`, `.mcp.json` | `git status -sb` both repos |
| Gates | **ALL GREEN at `fa59a2d` / `629d3f5`**: typecheck · trap-check **20 rules** 0 hits · doc-check · **14 Node gates** PASS · nav-permissions 6/6 · backend **55 passed** | §7, run 08:0x |
| Telemetry volume | rows per tester-hour: 09-05 **390** · 09-06 **172** · 09-07 so far **46** | §3.8 query |
| Jeff's phone | TestFlight 75, last seen on OTA-AD (`01a079e0`) 09-06 20:45; the two simulators report `embedded` with `channel ""` | `crash_reports` |

**Who is on what (newest bundle per handle, rows since 2026-09-06 20:00 PDT):**

| handle (person) | platform | newest bundle | last seen | note |
|---|---|---|---|---|
| `Enablewhore` (Olaf) | iOS | **AF** `01a07a7a` | 09-07 00:17 | CarPlay driver; two more `js-error … updateCameraStop` rows on AD at 00:11 (§4 A1) |
| `Ni GR` | iOS | **AF** | 09-06 23:39 | **no longer stranded** (was NULL/unknown through 09-06 13:45); arrived at a saved Home on AF: `arrive-speak … ending=1 poi=0` |
| `SPL_GRC` (Say Phin, he/him) | Android | **AF** | 09-06 23:18 | on AE+AF since 23:17; **the `spotDrop=` verdict for his stale pin is not readable yet** — his AF launch logged only `cam-mode` rows (no `draw-cmp`), so whether the SunTea pin was refused is unknown until his next map session |
| `SMSGRC` | Android | **AF** | 09-07 07:58 | driving right now (nav=1, 112 km/h at 07:58); logged `dest-place none ms=177` at 07:50 = a destination with no business within 40 m |
| `GRSIENNA` (Alfred) | iOS | AD `01a079e0` | 09-06 22:12 | not on AF yet |
| `Rodrigo` | iOS | AD `01a079e0` | 09-06 22:33 | not on AF yet; **no stranding rows since 20:00** |
| `Victor 3d Dude` | Android | AD | 09-06 21:21 | first rows in days (17); the AA cold-connect item (§4 B6) now has a device to read |
| `Jeff` | iOS | AD (phone) | 09-06 20:45 | — |

Nobody has `launch_kind=unknown` / `update_id NULL` rows since 09-06 20:00 (CarPlay-first stranding detector: clean overnight).

> Update-id → OTA map (VERIFIED with `eas update:view <group> --json`, which prints per-platform ids; `update:list` does not): W `01a07201` · X `01a072fa` · Y `01a073fd` · Z `01a07580` · AA `01a0776c` · AB `01a0778e` · AC `01a079d7` · AD `01a079e0` · AE `01a07a5f` · AF `01a07a7a`. `crash_reports.update_id` is TEXT; `runtime_version` pins the binary (1.26.0 = 74, 1.27.0 = 75/76).

---

## 0b · BUILD 77 → 78 — CUT 2026-09-10/11 (the native change = the Apple Watch companion; receipts in memory `build-77-cut`)
- **Scope (what is native in 78 over 75):** `targets/watch` (HairpinWatch, `com.sw0rdfisch.convoy.watchkitapp`), `targets/watch-widget`
  (complication, `…watchkitapp.widget`), `modules/hairpin-watch` — 12 commits of 09-10. NOT in it: the widget family / iOS 27
  `systemExtraLargePortrait` widget, standalone CarPlay, the AA Kotlin guards, RevenueCat, the expo-location patch → build 79.
- **Numbers:** v3.10.0 · runtime 1.27.0 → **1.28.0** · iOS `buildNumber` 77 → **78** · Android `versionCode` 77 → **78** (same number on
  both, Jeff: "both"). Gates before the cut: typecheck clean, trap-check 27/0, doc-check 0, all 23 sim-qc gates rc=0, lockfile clean.
- **What it took (each one VERIFIED, each one free until the builds):**
  1. EAS CLI 24.1.2 registered the new watch-widget bundle id but Apple's API rejected its capability PATCH (`…bundleIdCapabilities.data.[0].attributes`)
     — latest CLI + latest apple-utils, no fix upstream. Claude did the portal work in Jeff's Chrome on his "do it": App Group
     `group.com.sw0rdfisch.convoy.watch`, App Groups on `…watchkitapp.widget` (7B6WA52ZSK) and on the new App ID `…watchkitapp` (C3DJVZHR8T).
     ⚠ Apple's App Group form pre-prefixes `group.` (caught `group.group.…` before Register).
  2. Non-interactive `eas build` still refused: the two new targets had no distribution certificate linked on EAS; only interactive
     mode links one. The scripted-PTY attempt was blocked by the classifier → **Jeff ran** `npx eas-cli build --profile mapbox-ios
     --platform ios --no-wait` (keychain password, 2FA via Settings → Get Verification Code, y to "Generate a new Apple Provisioning
     Profile?" ×2). One-time: 78 queued fully non-interactively.
  3. Both 77s green first try (iOS 8 min, Android 14 min). Android 77 → Play internal COMPLETED. **iOS 77 refused by App Store Connect:**
     ITMS-90713 no `CFBundleIconName` in the watch app + ITMS-90391 no icons in `Hairpin.app/Watch/HairpinWatch.app` — the watch target <!-- doc-check:ignore: Apple bundle path inside the IPA, not a repo path -->
     had no `icon`. Fix `5a02e68`: `icon: '../../assets/HAIRPIN.png'` in `targets/watch/expo-target.config.js` (+ the generated
     `targets/watch/Assets.xcassets/AppIcon.appiconset/`); proven on a local watchOS-simulator build (`CFBundleIconName = AppIcon`,
     Assets.car carries the 1024 icon). Re-cut **78 on both** per the same-number rule; both green; both submitted; Apple's upload
     processing (the step that refused 77) completed clean.
- **⚠ OPEN — rotate the App Store Connect API key `P9NUHM3CL7` ("[Expo] EAS Submit"):** an `EXPO_DEBUG=1 eas build` printed it into the
  Claude transcript on 09-10 (scratch log deleted at once). Revoke in App Store Connect → Users and Access → Integrations, create a
  new one, upload via `eas credentials` (interactive, Jeff). Until then the old key still works for submits. Never run `EXPO_DEBUG=1 eas …` again.
- **Field-unverified until installs:** the watch companion on Jeff's wrist (WCSession activation, the turn card + haptics,
  the complication), CarPlay-first launch on a runtime-bumped build (no `launch_kind=unknown` at 1.28.0), Say Phin's AA on 78.
  The crew note goes out on Jeff's word only ("Hey guys, it's Claude"): TestFlight 3.10.0 (78) / Play internal 3.10.0 (78),
  install before the next drive, old builds stop receiving updates.

## 0d · FIELD 2026-09-11 PM — the first drive on OTA-AO (receipts: memory `field-2026-09-11-pm-drive-on-ota-ao`)
- ✅ **The heading fast path FIRED:** `off-route tripped d=32m why=heading step=4` (21:15:36 UTC), reroute applied 270 ms
  later, two seconds after the release (`snap-mode gate=hdg hd=91`). Four reroutes over two sessions, no storm. Jeff:
  *"off route was quick to snap to the correct route."* ✅ **Cornering good** (his words) — the reverted-to course judge holds.
- ✅ **The greeting fix is LIVE against the unchanged backend:** `greet-prep … "Good afternoon. Heading home. Roads are
  clear. It's cloudy."` — no place name. Temperature / "when you arrive" / rotation still need Jeff's backend push.
- ⚠ **JITTER root-caused — two map instances interleave.** `heat-probe` with phone map + CarPlay: `dtP50=0.0 dtP95=51–77`;
  with the car surface alone: `dtP50=8–9 dtP95=24–26`. Frames arrive in PAIRS then a long gap (each surface ~16 fps).
  NOT FIXED — candidates: do not render the phone map while CarPlay drives, or pace both surfaces off one clock.
- ⚠ **CarPlay End + Search: the taps NEVER REACHED JS.** Zero `ios-stack` rows all drive (the receipt shipped in OTA-AN
  covers end/search/whereto/dismiss/selected/recover). The CarPlay session is connect → chrome → live-paint → viewport →
  silence. **This excludes the whole JS layer; the buttons are dead at the template/native level.** No JS fix applies.
- ⚠ **Scout ate the turn callout** — `tts-skip why=rate len=11` one second after a reroute queued a 43-char instruction;
  `nav.ts:1999` drops any non-priority line inside 1.5 s, and the imminent-turn callout (`nav.ts:1277`) is not priority.
  Candidate: make the turn-now line priority, or reset the gate on a route swap. NOT built — needs a replay + Codex.

## 0c · FIELD 2026-09-11 — build 78's first drive (receipts in memory `field-2026-09-11-exit-stall-and-corners`)
- ✅ **Road-only lift VERIFIED:** `ribbon-trim lift=0` the whole route; `self-lift to=10 why=offroad lot=1` in a lot, `to=20.6
  why=bld:19` under the house at 16:08:25 → `why=road → 0` on the move; no `offroad` at any light; phone query `ms=32–155`.
- ⚠ **"Stutter after the exit" = `main-gap dt=25098 surface=car app=a fixGap=49`** (16:30:13 → 16:30:40, 46 s after the
  reroute; the reroute swap itself cost 0.79 s). No author in the rows — the three post-route jobs had no receipts.
  **`9455694` adds `cam-fetch` / `bc-events` / `pin-imgs`**; read whichever lands just before the next `main-gap`.
- ⚠ **"Corners wide" = the nose ~30° behind the GPS course** at 37–50°/s corners. ~25 variants measured against the 60-seed
  X-suite (memory): every knob that halves it breaks a refuter-set bar (X1n 6 m-noise ≤ 50°, the 70°/s swing rule, the
  exit-leg 3° rule). **Shipped `da651a1`** (all bars green): the road judged against the full-rate course + the sharpness fade
  gated on distance — X1n 45.5→40.9, X4 43.3→40.6, S-curve 47→43; the sharp corner itself NOT fixed (Y3 median 45.6°).
  ⚠ Reviewed 09-11 evening by nine skeptics + Codex ×2: the on-line sharpness half was REVERTED (`a843845`) — it
  leaned the nose into a turn the driver was MISSING (+4.7°) and was worse on noisy fixes; the full-rate course
  judge is kept and improves the corner at every noise level. **Jeff's decision pending:** relax X1n 50→54 and
  ship the on-line AGREEMENT gate (halves the lag on clean fixes) — a different knob from the reverted one.
- ✅ **Exit reroute: 134 ms to reroute, 8 s to DECIDE** — **shipped `da651a1`: the heading fast path** (`HDG_FAST_*`, 3 known-heading
  ticks > 12 m off at ≥ 4 m/s with no maneuver within 100 m). Replay: Jeff's ramp 3 s sooner; legit corner / coarse line /
  divided highway / lot storm quiet. ⚠ Review found the fast path could re-create the 09-04 LOT STORM at road speed
  (6–11 reroutes in 4 min) and re-trip every 9 s at a steady offset — fixed `541fd16` (join + rolling growth +
  clear-on-trip, gate X). OTA on Jeff's go.
- ✅ **Greeting "It's cloudy in Work"** — the saved-place label was the weather city. App fix `9455694` (OTA-able, no city,
  "20 degrees"); backend template + prompt `cd4be64` in `~/convoy-backend` — **Jeff pushes**. Only ONE `greet-prep` row exists
  and it is `src=template`: whether Claude ever authors the greeting on Render is unknown (key / "degraded" log line).

## 1 · What shipped 2026-09-05 → 09-07 (all JS-only OTAs on runtime 1.27.0; KEY_PRESENT=1 both on every one)

| OTA | published (PDT) | group | code | what it changed | receipts it added |
|---|---|---|---|---|---|
| **W** | 09-05 07:38 | `cf954078` | `a008317` | ribbon lead pitch-compensated; off-route gate for Olaf's lot storm; nose clamp; AA follow re-assert | `ribbon-trim pitch= leadDp=`, `off-route held why=`, `corner-trace fix=`, `aa-appstate` |
| **X** | 09-05 12:11 | `c8f031d4` | `9b7a7c3` | the locked-phone CarPlay timer freeze: liveness heartbeat, fix-driven car backstop, ONE reroute in flight aged off GPS fixes (`src/rerouteSlot.ts`) | `timer-starve`, `route-fetch-abort-fired src=fix`, `off-route held why=inflight`, `reroute-slot-freed` |
| **Y** | 09-05 16:52 | `9f3dc52a` | `30c9afd` | aborted route responses dropped; 45 s plot timeout vs 15 s reroute; 5 mutation-tested trap rules (13 total) | `route-fetch-settled-late … dropped=`, `reroute=0|1` |
| **Z** | 09-05 23:56 | `71099527` | `bca1dd7` | ribbon cut ANCHOR (417 m off on Jeff's drive) + lead 60→40 dp; ONE ding per speeding episode; arrival line with weather + 20 closers; weather pill centred; per-path post-swap holds; head-unit car-size refresh | `ribbon-trim hint=`, `speed-alert episode=`, `arrive-speak wx= ending=`, `self-scale-refresh` |
| **AA** | 09-06 08:54 | `f920b579` | `95acc6f` | **fatal crash reports had never been sent since 07-23** — fixed; `js-error` rows; second abort guard; telemetry cadence cut; head-unit refresh vs last zoom; speed episode ticks every fix; **bearing on** | `js-error kind=`, `depart-rank constrained=`, `reroute-result bearing=` |
| **AB** | 09-06 09:29 | `b472fd1d` | `7c9a22e` | creep hold ≤160 m; windowed anchor; serialised crash queue; heat-probe pruning; 2 s throttles; 3-digit weather pill; nav start no longer prompts for notifications (Codex's edit) | — |
| **AC** | 09-06 19:5x | `7301112b` | `9dee6bf` | ⚠ **wrong, corrected by AE**: gated car-spot WRITES on speed (a spot stopped following the car) | `draw-cmp … spotDrop=` (kept) |
| **AD** | 09-06 20:1x | `ac3e3b43` | `563af5a` | Hub keeps an event that started TODAY listed until local midnight, greyed, "Started N h ago"; event pushes get **"I'm in" / "Can't make it"** buttons that call `/confirm` or `/decline` before opening the event | `event-push-action id= action= ok=` |
| **AE** | 09-06 22:4x | `6bffb233` | `e7ed982` | spots follow the car again; a persisted spot is refused at launch when its last write was moving (`mv` ≥ 1.5 m/s), when written with a head unit attached but never witnessed ending, or when the driving stamp is > 60 s newer than it (`drove-after`) — `src/carSpotTrust.ts` | `draw-cmp … spotDrop=written-moving|unwitnessed-attached|drove-after|stale|malformed` |
| **AF** | 09-06 23:06 | `78080ad5` | `9e1696b` | **Scout arrives by business name**: one Google nearby search per plotted (non-saved) destination; closer chosen by place type (47 rules, 101 lines), the closed line, or a backend-generated quip | `dest-place name= type= open= dist= quip= ms=`, `arrive-speak … poi=1 ending=100|101|102` |
| **AG** | 09-09 ~0x:xx | `cd011b4f` | `7d02637` | **arrival says ONE line and starts it 3 s early** (the final-leg "you will arrive at your destination" heads-up deleted; `arrive` joined SILENT_MANEUVERS so the second-to-last step stops announcing it; the composed line now speaks `min(60, spd*3)` m early, keyed on the destination); **CarPlay weather box** temperature offset in EMS not points; **route line** cut ahead of where the car is DRAWN (it is rendered 10 m in the air); **roundabout voice cue** restored; **roundabout marker drift** (nose clamp admits 2.2 m/s with 3 consistent fixes); **/tts bounded** 8 s / 15 s so one slow clip cannot hold the queue; **aa-stack** receipts | `arrive-speak … early=`, `arrive-speak-early lead= d= spd=`, `tts-fail why=timeout|error`, `ribbon-trim … lift=<pt>`, `aa-stack op=push|pop|reset` |
| **AH** | 09-09, ~20 min after AG | `8f1abda4` | `37eef8c` | two findings from the 31-agent pre-ship review (it returned ZERO ship blockers): a **painted arrow** is drawn at `SELF_ARROW_LIFT_M` again — its model id is `ARROW_MODEL_ID + "_" + paint` so the `===` test gave it the CAR's 10 m, which put it under the ribbon (old) and, once AG fed the lift into the trim, started the line far too far ahead for it (new, phone-only); and **`prefetchTts` now normalises through `toSpeech`** so the arrival line is actually served from cache at an address destination (`123 Main St` -> `Main Street` missed the key every time). Trap rules 17 -> 18. | `tts-fail why=timeout` should now be rare on arrival |
| **AI** | 09-09 ~19:0x | `866ab289` | `835ce63` | **the Android Auto receipt could not survive the moment it was written for.** Every `aa-stack` row used plain `logEvent`, which returns early and DROPS the row while the Supabase client is still being built — the normal state on an AA cold connect, i.e. exactly when `op=root` fires; the receipt written to answer Say Phin's black screen could have logged nothing a second time. All six sites now use `logEventReliable`. Codex's review then found the reliable path itself lossy and **both findings reproduced**: `deliverAndHarvest` awaited its INSERT then wiped the WHOLE queue key, destroying rows queued mid-flight (the 09-06 fix serialised the writers and left the deleter outside the chain) — delivery now claims a snapshot and drops only the ids it delivered; and the fallback is a bounded 25-row FIFO shared with crash reports, so `op=root` can still be evicted — **NOT** redesigned, instead the instrument is documented as read-PRESENCE-never-absence. New pure module `src/crashQueue.ts` + gate `crash_queue_test.mts` (20 assertions, incl. a control reproducing the old deleter). Trap rules 18 -> **19**. | `aa-stack op=root … n=N` arriving from a real AA connect; a session with none = **UNKNOWN**, never "JS never set the root" |
| **AJ** | 09-09 ~20:1x | `96bca8c8` | `fa59a2d` | **DRIVES, three measured defects.** (1) recordTrip credited the ROUTE'S PLANNED distance, never the distance driven — four rows in Jeff's own history banked 17.2 km ~139 s after the route was plotted, top speed 0, **445 km/h average, 68.9 km he never drove**, on the club board. New `src/tripOdometer.ts` (5 gates + `isPlausibleDrive` >200 km/h avg) and `src/driveOdometer.ts`, ONE counter fed by BOTH the fg watcher and the bg NAV_TASK so a locked-phone CarPlay drive is not under-counted. (2) recordTrip ran ONLY from `onArrive`, so End on phone, End on head unit and a never-firing arrival all saved NOTHING — **Ni GR (John Mungai) drove 22:29→23:46 UTC, 77 min, 859-point route, zero rows**; arrival and End now share one `recordDriveNow`, banked once. (3) no ACTIVE club ⇒ `community_id` NULL ⇒ credited to nobody (170.6 km of Olaf's, all of SMSGRC's) — falls back to the driver's first club, read-only. Plus `driveStartedAtRef`, because a reroute resets `tripBaselineRef.startedAt` and shortened the recorded duration. Codex found **5 issues, all 5 reproduced and fixed** (measured ZERO fell back to the route — the original bug inside its own fix; `endNavFromCar` `useCallback([])` froze the recorder at a null route; `resetColdArrival` runs on every geometry swap and wiped the cold baseline; standalone `endCarNav` never recorded; locked-phone under-count). **Also: Android navigation templates no longer call `createTemplate` TWICE** (`defersNativeCreate`) — halves the passes through an unguarded native path. Trap rules 19 → **20**. **DB: the 4 phantom rows DELETED** (Jeff 1989.5 → 1920.6 km, 44 → 40 trips); restore SQL in the session scratchpad. | `trip-record km= src=odo\|route routeKm= dur= club=`, `trip-skip why=short\|implausible` |

### IN TREE, NOT SHIPPED — the CarPlay weather callout (2026-09-08/09)

Jeff, 09-07: *"I was also told and shown photos that the weather was fixed in the end destination."* It was not.
**The CarPlay temperature had NEVER been rendered before his photograph** — the evidence shown to him was
`tools/wx-pin/preview_align.py`, a PIL model of the PHONE's React Native `<Text>` ("PIL only, no app/JS involved",
its own docstring); the ROADMAP line "CarPlay's SymbolLayer measured +0.4 pt and was left alone" is that PIL number.

**ROOT CAUSE, MEASURED** (simulator, camera pinned from code at pitch 57 = the head unit's chase pitch, zoom 16.5,
12 callouts at 4 ground distances, 3 mechanisms side by side): Mapbox multiplies symbol `icon-size`/`text-size` by a
perspective ratio `0.5 + 0.5*(D/w)`, so a billboarded callout SHRINKS with distance; `text-translate` is a paint
property in constant screen POINTS and is not scaled by it, so the text climbed out of the shrinking box.
Text centre below the box top, box width 69.7 → 59.3 pt across +350 m: **translate 16.5 → 10.0**, **ems 16.5 → 13.7**.
Error is `25 × (1 − ratio)` pt: 1.8 at 300 m, 3.6 at 500 m, 6.2 at 1 km. **At pitch 0 all three are identical**, which
is why every flat check passed. Fix: `text-offset` in EMS, which rides `text-size`. `src/calloutTextOffset.ts`
(the measurement is in the file), gate `tools/sim-qc/callout_offset_test.mts` (19 assertions incl. 4 negative
controls), trap-check rule `callout-text-translate-in-points` (14 rules). Memory: `mapbox-perspective-ratio-vs-paint-translate`.
Codex proposed the ems fix and its adversarial review then killed the first version of the gate, which tested only the
helpers and would have passed a zeroed production offset — the gate now evaluates the SHIPPED expression.

**Open, same class, NOT measured (HYPOTHESIS):** `car-waypoint-badge` translates its disc a fixed `29.5 × uiScale`
while the numeral it backs uses ems, so disc and numeral may separate at distance (~4.5 pt at the ratio above);
the phone's `gl-places-bg`/`gl-places-num` pair is the same shape. **Also for Jeff:** the phone draws this callout as
a constant-size MarkerView while CarPlay's shrinks with distance — a parity difference, his call.

### IN TREE, NOT SHIPPED — the route line under the car at an exit ramp (2026-09-09)

Jeff, 09-07: *"i was told the route line was fixed but it wasnt, especially when taking the off ramp."* OTA-W
(pitch compensation) and OTA-Z (cut anchor) had each already "fixed" it. **Both were about the LEAD, and the lead
was never wrong** — his own receipts put the cut ~35 screen pt ahead at BOTH the highway frame and the ramp frame.

**ROOT CAUSE: the self marker is DRAWN 10 m in the air** (`modelTranslation: [0, 0, 10]`, 16 for the arrow — added
2026-08-19 so the 3D buildings' shared depth buffer could not eat it; the vendored SDK documents that field as
metres of altitude). On a pitched camera a raised object projects toward the horizon, i.e. FORWARD along the road,
and the lift is a fixed number of GROUND metres, so its screen cost **doubles with every zoom level**. The ribbon
is a LineLayer with no lift. **MEASURED, not derived:** two simulator frames at an identical pinned camera
(zoom 16.5, pitch 57, 402×874 pt), lift 10 m vs lift 0, ground-anchored control markers unchanged **to the pixel** —
the drawn car moved **17.2 pt** up-screen (model predicts 15.3). On the 470×265 head unit that is **3.4 pt on the
highway and 62.5 pt on the ramp**. Nose-to-line gap: highway **+19.9 pt**, ramp **−31.9 pt** (line under the car),
arrow skin on the ramp **−70.5 pt**. That is exactly the three photographs.

**FIX:** move the cut UP-SCREEN by exactly the lift's shift, then convert back to ground metres
(`leadShiftedByLift` in `src/routeTrim.ts`); both surfaces pass the lift + their map height, and the lift value has
ONE definition (`SELF_MODEL_LIFT_M` / `SELF_ARROW_LIFT_M` / `PEER_MODEL_LIFT_M`) that `modelTranslation` also reads.
Adding the lift's ground-equivalent instead is NOT enough and the gate rejected it — high on a pitched screen the
perspective is so compressed that the arrow skin kept only 3.7 pt. After: highway 19.9 → **23.0**, ramp −31.9 →
**23.6**, arrow −70.5 → **23.6** (car and arrow converge by construction). Receipts gain `lift=<screen pt>`.
Gate `tools/sim-qc/self_lift_lead_test.mts` (37 assertions, incl. negative controls that fail if the compensation is
removed or made additive); trap rule `self-model-lift-hardcoded` (15 rules) — it immediately caught a SECOND
hardcoded lift on the peer twins. Memory: `self-marker-lift-moves-it-up-the-road`.
**Codex adversarial review of the committed fix returned TWO high findings, both real, both fixed:**
1. **Android Auto under-compensated.** `routeTrimLeadM(..., lift, mapH) * mapScale` scaled the ALTITUDE correction
   as well as the design lead, while `modelTranslation` stays at a full 10/16 m — at mapScale 0.53 only ~68 % of the
   shift was cancelled (gate H2 measures 22.2 pt of 32.4) and the overlap would have survived on AA. Now the design
   lead is scaled first and the lift correction applied at full strength. CarPlay (mapScale 1) is unchanged.
2. **The correction could have ERASED the line.** The inverse projection runs to the horizon; at z19 / pitch 60 on a
   265 dp head unit with the arrow it wants **3481 m**, and `buildRibbonFeatures` drops every feature once
   `totalM - cut < 1` — the whole route line would vanish rather than merely overlap. Now capped
   (`LIFT_LEAD_MAX_M = 120`, measured against the envelope) **and** `clampCutToRoute` keeps a 40 m tail, floored at
   the car's own anchor so arrival still clears the line exactly as before.

**Known, bounded, asserted limitation:** the ARROW skin on the short Android Auto canvas above z18 needs 480 m,
more than the cap, so its compensation is PARTIAL there. At that camera the marker is drawn 124 pt up a 201 pt
layout — 71 % of the way to the horizon — so the LIFT is the broken thing, not the trim. A zoom-aware lift is the
real fix and cannot be done in a layer style without the per-tick main-thread write the 0x8BADF00D rule forbids.
Gate I7 pins it so it cannot silently change.

**Not field-verified:** the numbers above are the model + the measured A/B, not a drive. Jeff's next drive is the verdict.

**Backend (`~/convoy-backend` master, all LIVE at `629d3f5`):**
- `466f2a3` 11:41 — **Codex security release** (Claude reviewed the real diffs, ported the 47-test suite, released; Jeff pushed): verified-email-only social linking (409 on subject replacement); invite code only to admins, private rosters only to members, admin rights need membership; `floor_acquire` membership check + deleted users refused on the socket; Supabase→Mongo scan-slot sync before every credit decision (503 when unreadable); `/scan/mine` by account ownership, never the handle; `/health` returns `rev`. Supabase migration applied + ownership set from the authenticated `users.car_scan_id` pointer (Jeff `jeff-20260902-193844`, Olaf `enablewhore-20260901-210315`). Full record: `HANDOFF-2026-09-06.md` §1.
- `eea3db1` 19:45 — **no duplicate event pushes**: reminders already inside their window are marked sent at create AND when the start moves; the create-time invite stamps `announced_at_ms` so an Announce tap within 10 min is throttled. Rodrigo's "four pushes in 70 s" (invite, Announce 10 s later, "is tomorrow · still attending?", "in 2 hours") cannot recur. `tests/test_events_windows.py` (5).
- `bdfa558` 20:17 — invite + 24 h confirm pushes carry `categoryId: event-confirm` (the app registers the buttons).
- `629d3f5` 23:04 — `POST /scout/arrival-quip`: one Claude Haiku line for the business at the destination, cached per place in Mongo `arrival_quips`, 204 without `ANTHROPIC_API_KEY` or on any error; `tests/test_arrival_quip.py` (3). **No quip has been generated yet** (`arrival_quips` is empty at 08:00) — nobody has arrived at a resolved business since AF.

---

## 1b · OTA-AN — PUBLISHED 2026-09-10 21:39 PDT, group `74f5918d-0755-4ce6-85c1-1cfcda0f71fc`, code `626e37b`, KEY_PRESENT=1 both (Jeff: "ota-an and go")

| commit | what it changes | receipts it adds | verification |
|---|---|---|---|
| `172ee14` · `595fb64` · `daf72cc` · `8a8d989` | **The 09:01:55 triple ding.** `speed-alert tier=2 mode=ding over=45 limit=50` at 95 km/h on the TCH under the Clearbrook Rd overpass: the nearest OSM way by flat distance was the OVERPASS (secondary, 50). `src/speedLimitSnap.ts` (pure): the road you are on runs the way you are going — a way > 35° off the course is a crossing, not your road, while moving ≥ 3 m/s (one-way ways only match forward). The chime overlap (second chime started 190 ms into a 480 ms clip) → the double chains after `didJustFinish`. Codex findings fixed: only `coords.course` feeds the limit (never the sticky heading), null-limit transitions settle 2 s (`SPEED_LIMIT_SETTLE_MS`), the episode reducer's `armed` flag makes settling silent, a delayed fetch resolves at the position it was asked for. | `speed-limit lim=a>b near= cls= x=<crossing> crs= spd=` (≤ 80 / session) · `greet-prep src= len= text="…"` / `greet-prep-fail` / `greet-none` / `greet-play` / `greet-done` / `greet-cancel` | gates `speed_limit_snap` (16) + `speed_episode` §G; the overpass geometry replays (A1–A4); Codex ×2. Field receipt still owed: a drive under that overpass with no ding. |
| `66aaa36` | **Neon pins (Jeff picked "F · Neon rings" off the mockup):** `src/components/NeonPin.tsx` — the wordmark's ring-head teardrop, 26 pt, glass body + neon rim + glow + glyph, ONE component for cameras, DriveBC events, crew reports and place pins on all four surfaces; the phone snapshots it into Mapbox symbol images (font-readiness gate, remount snapshots, tier refresh), cameras/events cluster (28 pt to z13, tap = expansion zoom), the next one ahead ≤ 500 m stands up at 32 pt, minor DriveBC events smaller and dimmer; CarPlay/AA MarkerViews at 0.8×. The 44 pt PNG teardrops, the stock triangle and the glass camera square are gone. | none (visual) | **Sim-VERIFIED on the iPhone 16 Pro:** a live DriveBC construction event on the Abbotsford-Mission Hwy renders as the amber ring (scratchpad sim20/21). NOT individually shown: camera pins (need a route), clusters, place pins, the car surfaces — same snapshot path. Codex ×3 approve. Recipe: memory `sim-tap-free-verification-recipe`. |

| `814aba2` → `f3afa18` (7 commits) | **The self car is lifted ONLY off the road (Jeff: "take away the lift while driving; on a property the lift occurs … pronounced at a light and slow speeds, it should never get lifted; parking lots / driveways / parkades lifted").** Since 06-29 the car was drawn 10 m in the air everywhere (arrow 16) to beat the 3D buildings' shared depth test — 3 pt up the road on the highway frame, **56 pt on his exit ramp tonight** (`ribbon-trim lift=56`): the "lag" he saw on the ramp. In the chase view the sight line to the car runs over the road behind it, so the lift is only needed where buildings stand behind and around the car. `src/selfLiftRule.ts` (pure, 55-assertion gate): speed ≥ 5 m/s, the route within 25 m, or a drivable road's centreline within 30 m ground it at once; POSITIVE off-road evidence (a driveway / parking aisle under the car and closer than any road, with no road within a lane's 9 m; an EXTRUDED footprint → its roof + 2 m) must hold 2.5 s on its own clock, an ABSENCE alone (no drivable road within 30 m) 8 s, and BOTH count only from a query the map was IDLE for (Mapbox's onMapIdle = every tile loaded; onCameraChanged clears it; completeness travels with the cache entry) — a tile still loading looks like an absence, and a loaded driveway can precede the closer road of a tile still loading (`8d3f826` + `7791fdc` + `42064cc` + `babb1b7`, Codex passes 3–6: a query counts only if idle at both ends with no camera change between; `2925671` + `f3afa18`: something seen under the car by ANY query SUSTAINS a confirmed lift while creeping — for at most 45 s past the last idle-map proof — and never starts one); no evidence never lifts. Accepted consequence: a car CREEPING through a lot (camera moving, map never idle) is not lifted until it stops. Evidence = `querySourceFeatures` on the app's OWN invisible mapbox-streets-v8 source (the road-snap's `convoy-roads`, + a new invisible `building` FillLayer on both surfaces): **Standard hides its layers from every query — the rendered query returned only our POI labels and our own car, and would have lifted the car on every road** (20:12–20:20 receipts). Once a second while slow, 3 m / 20 s cache, generation guard, lift rides the source feature `trn`; the ribbon trim reads the DRAWN lift; peers: parked twins keep 10 m, live ones 0. | `self-lift to= why=speed\|nav\|road\|offroad\|bld:<h>\|unknown spd= nav= road= bld= lot= idle=` (≤ 40) · `self-lift q roads= drv= drvM= propM= bldN= ms= road= bld=` (≤ 24) · `self-lift q-fail` (≤ 3) | **Sim-VERIFIED** (iPhone 16 Pro, 20:38–20:41): Marshall Rd 21 m off the centreline → never lifted; a mapped driveway → 10 m; the Sevenoaks lot aisles → 10 m; queries 24–65 ms; a parked car asks once per 20 s. **iPhone 17 Pro Max with Xcode taps, a real route:** `ribbon-trim lift=0` on the route; the car AND the arrow skin drawn at the base of the ribbon with the normal gap, nothing over them. Codex ×9 (17 findings, all implemented; pass 7 approved). NOT exercised: CarPlay / AA (same component, same source), Android, a phone's query cost. HYPOTHESIS: one 3.5 s JS stall at nav start on the sim = the ribbon owner re-rendering 10×/s during the slide → throttled to 2×/s. |

| `626e37b` | **iOS template-stack receipts** — the iOS twin of `aa-stack`, at every CarPlay push / pop / pop-to-root (Rodrigo's dead-buttons report below had no receipt that could say what the stack did). | `ios-stack op=push id=search\|whereto · op=root why=selected\|dismiss\|end · op=recover pushed= presented=` (≤ 60 / session, reliable path) | instrumentation only; no CarPlay sim available tonight (the CarPlay window needs a Simulator menu click). |

**Rodrigo, WhatsApp 09-10 20:41 / 20:54 (read 21:15) — three reports, verdict per report:**
1. **"I'm missing being able to choose the route and preview it before starting. Right now it starts automatically."** VERIFIED by code + rows: on CarPlay a saved place or a search result calls `startCarNav` at once (`carActions.ts` `onItemSelect`); the phone has the Best / AI chips + Start, CarPlay has nothing. His 10:27:21 search tap → guidance at 10:27:26; 20:16:37 → 20:17:03. This is a DESIGN gap, not a bug: the CarPlay equivalent is `CPMapTemplate` trip previews (react-native-carplay `showTripPreviews`) with the route choice and a Go button. Preview UX before shipping → a mockup for Jeff first (§A 8).
2. **"Sometimes it won't load until I unlock my phone."** VERIFIED mechanism, twice today: with the phone locked and CarPlay live, JS timers freeze (`timer-starve dt=` climbing to 644 s tonight) AND network responses are HELD — every reroute fetch aborted at 15 s by the fix-driven sweep (21 tonight, 8 this morning), then all of them settled in one burst the moment the phone woke (`route-fetch-settled-late ms=284016–378988 n=2` at 10:40:17; 21 `reroute-result … superseded` at 20:35:25). A route asked for while locked cannot arrive until the phone wakes. Other testers' reroutes applied in 0 s tonight (SPL_GRC ×3). This is the standalone-CarPlay item (§B 0); nothing OTA can deliver a response iOS is holding. HYPOTHESIS on the exact holder (the RN networking queue vs the process state).
3. **"Search on CarPlay without unlocking → unlock and start the trip on the phone → press back on CarPlay while it loads → none of the buttons work, only fix was to close the app."** His 10:27 receipts: `carplay-tap:car-search` 10:27:15 (+ a deduped double), a SECOND `carplay-tap:car-search` at 10:27:21 (not a double — 6 s later, with the list up), guidance auto-started 10:27:26, `carplay-tap:car-end` 10:27:28, a trip started from the phone 10:27:37 (`nav-eta step=1/10`, a different route), `main-gap dt=8893 surface=car` at 10:27:47, and then NOT ONE `carplay-tap` row for 12 minutes while he says he was pressing buttons. CARPLAY.md rule 7: a re-pushed template "renders, takes no touches". HYPOTHESIS: the second Search tap with the list already up is the trigger (the handler's recovery branch pops to root, then his selection / End / phone start raced the stack). The `ios-stack` receipts (`626e37b`) exist so the next occurrence says what the stack did instead of guessing. Repro needs the CarPlay simulator window (a Simulator menu click Jeff denied computer-use for tonight) or Rodrigo's next drive.

**Say Phin, WhatsApp 09-10 20:50 — "Just got blank screen on connection. Force closed hairpin and it came back."** The AJ instrumentation answers it: on the 20:40:34 and 20:40:40 connects `aa-crumb root-render → aa-stack op=root → template-set → root-mount` landed and **no `surface-render`** — the app set its root and the map never began drawing; the 21:02:29 connect after his force-close has `surface-render`. Same shape as the 16-of-48 finding; the Kotlin guards wait for build 77.

**Greeting "double line" (Jeff, 09-10 morning):** the text was never logged, so what Nova said is UNRECOVERABLE. HYPOTHESIS: the
template fallback (3–4 short sentences, clipped) — the new `greet-prep` receipt settles it on the next route start.

**Reverted, NOT in the candidate — GPS stall watchdog parked window.** Jeff's 17:36 drive showed `nav-loc src=car` / `src=bg`
pairs every ~30 s ONLY while parked before CarPlay connected: distanceFilter 2 m means a stationary phone legitimately gets no
fixes, and the 25 s watchdog reads that as a stall and restarts both feeds. A "180 s window while the last fix said < 1 m/s"
change was written and REVERTED on Codex's review (both findings hold): (1) the parked flag outlives the evidence — a feed that
dies parked and a car that then drives off gets no fix, so the flag never flips and guidance waits up to 3 min; it also survives
watchdog stop/start, relite and CarPlay acquisition; (2) Android speed 0 is not evidence (expo's `LocationResults.kt` never
checks `hasSpeed()`), so a moving speed-less fix would grant the long window. Ship it only with: parked state reset on every feed
generation / CarPlay connect / AppState active, Android zero-speed kept strict until build 77 forwards `hasSpeed()` (§B 7), and a
bounded one-shot position probe in place of the restart while parked. Cost today: wasted restarts while parked, nothing visible.

## 2 · Yesterday evening, complaint by complaint (the receipts are in `HANDOFF-2026-09-06.md` §3.10 and memory `event-pushes-and-car-spot-trust-2026-09-06`)

1. **"The event got wiped out"** — nothing deleted it. Say Phin's original "BBT Sunday Evening" was scheduled for **8:00 AM** (the picker shows AM/PM chips; the Hub showed "Sun, Sep 6 · 8:00 AM" all week) and the Hub hid it 3 h after start. He remade it for 20:00. → AD keeps today's past events visible until midnight.
2. **"Double notifications"** — up to four pushes in 70 s for an event created 43 min before start. → backend `eea3db1` + `bdfa558`, AD buttons.
3. **"Compass button puts me here but I'm not there, I'm home"** (Say Phin, 19:21) — his car spot had been written from a 7 km/h fix the instant the app died at 12:11 with Android Auto attached; hydrate adopted it 7 h later. → AC (wrong) → **AE** (right).
4. **"Showing me near SunTea still" (22:31)** — **AC's regression**: the write-path speed cap stopped the correctly witnessed park at the meet from following him 9.3 km home; it also meant a phone-only driver could never record a spot at all. → AE reverted the cap and moved every rule to hydrate. Lesson (memory): **never gate the spot write path; the spot must FOLLOW the car; judge the RECORD at launch.**
5. **"Is a green line supposed to be there"** — NOT identified. His drive ended normally at 22:01:42, no navigation after it, and the app has no line that connects a person to their car; the only green lines are route ribbons. Asked him in the group what he tapped after arriving.
6. **Rodrigo "crashing"** — his own words: both crashes were **09-05** ("while trying to find a route" and "while I wasn't even using it"). Fatal reporting only became real in OTA-AA, so no row exists. Tonight's 12 launches 18:34–19:35 were push/Hub opens (every instance ends after the normal `[updates-log] … endStartup` block). Asked in the group for the `.ips` files (Settings → Privacy & Security → Analytics Data, "Hairpin…2026-09-05"). **Not received yet.**
7. **Jeff's ask: Scout should know the destination** ("100 9420 200A Street Langley") → measured: a Google text search returns only the address; a nearby search at the geocoded point returns International Motorsports Motorcycle Company at Unit 100 → AF.
8. **Crew messages sent as Claude** (Jeff's "send it" / "reply to the chat"): 20:10 (three complaints + red pill + `.ips` request) and 23:16 (AC-was-on-us correction + the new arrival feature). WhatsApp is read and sent through Chrome on `web.whatsapp.com` — the Hermes bridge has 0 conversations.
9. **Say Phin is a man (he/him)** — Jeff, 23:1x. Every "she/her" written about him on 09-06 was corrected in the docs, code comments, gate and memory (`6332cd8`, `431b3c9`); `tester-handles.md` carries the note.

---

## 3 · Field verdicts (VERIFIED from `crash_reports`; window 09-05 12:00 → 09-07 08:00 PDT)

### 3.1 Settled
- **Abort question**: iOS honours `abort()` when the runtime is live (5/5 timer-fired aborts settled `n=0` within 15 ms); during a CarPlay timer freeze the request keeps running and a route arrives 27–160 s later (`n=1 aborted=1 dropped=1`, 5/5 dropped by OTA-Y's guard). The freeze itself continues on every bundle (native, build 77).
- **Ribbon anchor**: `anchorOff` avg 362 m (Y) → 1.6 m (Z) → 6.9 m (AB); `hint=prev` dominant.
- **Reroute storms**: bounded (one in flight; `off-route held why=inflight … starved=`).
- **Event pushes**: the fix is code-verified + suite; the next event made inside 24 h of its start is the live check (expect exactly the invite).

### 3.2 Open verdicts (the receipt to read, and what each outcome means)
| receipt | for | good | bad |
|---|---|---|---|
| `draw-cmp … spotDrop=` on Say Phin's next map session | AE refused his stale SunTea pin | `spotDrop=drove-after` once, then `mode=raw` (no pin) until his next drive | `mode=pin … sep` in the hundreds again |
| `dest-place name= type=` then `arrive-speak … poi=1 ending=100|101|102` | AF names the business | a name and a matching closer at a shop/restaurant; `ending=102` = a generated quip (also a row in Mongo `arrival_quips`) | `dest-place none` at a place that obviously has a business (a strata unit Google doesn't list) |
| `event-push-action id= action= ok=1` | AD buttons | present after someone taps "I'm in" on a real push | `ok=0 err=` |
| `depart-rank constrained=1 … off=` | bearing on (Rodrigo/Alfred "routes me the wrong way") | `off` ≤ 75 after the re-ask | `constrained=1` with `off` > 75 while moving (`draw-cmp spd` > 5 km/h in the same minute) → Mapbox ignored the bearing or the facing was stale |
| `off-route held why=trend … since=` after a swap | Rodrigo's slow re-route | zero rows so far on Z/AA/AB/AF (vacuously good) | a hold after the car has joined the new line |
| `self-scale-refresh surf=car` + a PHOTO | huge car at route start | normal car in the photo | 2.8× car |
| `speed-alert … episode=` | one ding per episode | ≤ 2 rows per episode | repeated tier-1 rows in one episode |
| `js-error kind=` | production errors | none | any → §4 A1 |
| `launch_kind=unknown` + `update_id NULL` per handle per day | CarPlay-first stranding | none (clean since 09-06 20:00) | send the rescue routine (unplug → force-quit → reopen unplugged → red pill → plug in) |
| `carscan-restored` / `carscan-reconcile-fail err=Request failed with status code 503` | owner-based scan recovery on a reinstall | `restored … from=none` | the 503 message |
| HTTP 409 on social login / `forbidden` floor frames / 503 `Scan credit service…` | the security release meeting real users | none | any → read the backend log for that minute |

### 3.3 Seen in the rows, not explained (investigate, do not theorise)
- Olaf: `js-error kind=rejection Could not find view with tag 788 in updateCameraStop after 55 attempts over 10101.0ms` ×2 at 09-07 00:11 (AD, phone surface, `nav=0`), after the same with tag 910 at 09-06 15:48 and 19:09 (AB). A `@rnmapbox` camera call retrying against a gone view for 10 s. Owner: surfaces.
- Rodrigo AB 15:17–15:45, parked: 20 `cam-apply surf=car dM>1000` rows (`dM=7970/8044`, `dz=0`) — the car surface asked to centre 8 km away and did not move (the "native non-apply" item).
- Say Phin: `parked=1` while `aa=1` — the Android head-unit attach claim (90 s TTL) lapses mid-session, so the self marker pins while AA is still connected. Separate from the spot fixes; unfixed.
- Olaf AB `main-gap dt=32006 surface=car app=a` (16:45) — a 32 s main-thread gap while ACTIVE; correlate with `timer-starve` before naming it.

### 3.8 Telemetry volume
`select (created_at at time zone 'America/Vancouver')::date, count(*)::numeric / count(distinct coalesce(handle,'<null>')||'|'||date_trunc('hour',created_at)) from crash_reports … group by 1` → 09-05 390.0 · 09-06 172.1 · 09-07 45.8 (partial).

---

## 4 · What needs to be fixed — ranked, split by where it can ship

### A · OTA-reachable now
1. **`js-error … updateCameraStop` (tags 910, 788)** — Olaf, four rows across AB/AD, phone surface. First step: the 10 s of `cam-mode`/`car-surface-gone` rows before each; then which camera call (`ConvoyMapbox` vs `CarMapView`) is issued after unmount.
2. **Android attach claim lapsing while AA is connected** (Say Phin `parked=1 aa=1`): find who refreshes `_carConnectedAt` during an AA session (`carDataService`/`AndroidAutoRoot`) — a 90 s TTL with no heartbeat pins the self marker mid-drive.
3. **Rodrigo's `cam-apply dM=8 km` on the car surface** — read every `cam-apply dM>1000` since Z, reproduce on the CarPlay sim.
4. **Say Phin AA screen-off** (09-04): "avatar moves off the AA screen, zoom-out does not bring it back"; `aa-recenter why=appstate fix=1` fired five times for him — nobody has asked whether the avatar came back.
5. **The green line** (Say Phin 22:30) — wait for his answer; if it recurs, the receipt to add is the route-source state at `nav=0`.
6. **HUD text size cap** (Olaf's Dynamic Type) — Jeff's call (~1.15×).
7b. **GPS stall watchdog while parked** — restarts both feeds every ~30 s on a stationary phone (§1b, reverted change + Codex findings + the design that would hold).
7c. **`sim-ios.sh` skips the watch targets for simulator builds** (tooling, not an OTA).
8. **CarPlay route PREVIEW before guidance** (Rodrigo 09-10) — trip previews on the map template with the route choice and a Go button; mockup first.
9. **CarPlay dead buttons after Search → End → phone-started trip** (Rodrigo 09-10) — read his next `ios-stack` rows; repro on the CarPlay sim window.
7. **Failed-render Garage copy** — needs a mockup OK (rule: preview UX before shipping).
8. **CarPlay layout guide, JS half** — `HairpinSystem.carMapButtonInsets()` exists since build 70 (never consumed); wire it into `carStore.chromeInsets` and retire `CAR_RIGHT_INSET` 48 / `carRightInsetFor` / `CAR_BAR_*`; verify sim 400×240 + wide, then Alfred's Sienna photo.
9. **Tester roster**: `Ni GR` and `SMSGRC` real names unknown (`tester-handles.md`).
10. **Docs**: `HANDOFF-2026-09-02.md` §4 rows 2/6 and §5 item 2 are stale (history file; left).

### B · Build 77 (native — paid, both platforms at 77, Jeff's explicit go)
0. **STANDALONE CarPlay + Android Auto** (Jeff 09-04, "period") — the only real end to the locked-phone timer freeze, CarPlay-first stranding, "phone off → CarPlay freezes".
1. CarPlay layout guide native remainder: a change OBSERVER for `mapButtonSafeAreaLayoutGuide` (the getter is one-shot) — only if polling on scene events is not enough.
2. RevenueCat · 3. **Apple Watch companion — CODE COMPLETE 09-10** (`targets/watch`, `targets/watch-widget`, `modules/hairpin-watch`, `src/watch*.ts`; spec + plan in `docs/superpowers/`; 22 sim-qc assertions for the tap rule, trap rule 22; Codex + a whole-branch review, all findings fixed). **Still needs Jeff before the cut:** (a) Mac unlocked → paired-sim drive (`tools/sim-qc/drive.sh`, read `watch-ctx`/`watch-tap`) + the complication rendering `N live` on a face (App Group `group.com.sw0rdfisch.convoy.watch`); (b) a dev-signed build to his phone + watch and three drives = Spike 2 (extended-runtime sessions are OUT — Apple's own doc, spec §5; decide (b) workout vs (d) watch location vs fallback-only from the `watch-tap via=` rows); (c) register bundle ids `com.sw0rdfisch.convoy.watchkitapp` / `.watchkitapp.widget` + the new App Group in the Apple developer account and refresh profiles INTERACTIVELY before a paid `eas build` (no credentials.json in the repo). Local iteration: always `expo prebuild --clean` (non-clean crashes in apple-targets 4.0.7); build the watch scheme with `-destination name=`, never `id=`. Widgets (home-screen family) remain a separate item · 4. `Screen.setMarker` patch · 5. AA `isConnected` getter + self-heal A1 · 6. Victor's AA cold connect (he is on AD now — read his `aa-crumb`/`aa-appstate` rows first) · 7. Premium gates need the entitlement backend (C).
3b. **Home-screen WIDGET FAMILY + the iOS 27 full-page widget — IN SCOPE (Jeff 09-10 21:5x: "include it and also add the new ios 27 phone full screen widget as well").** The 08-16 family (small Comms launcher / crew count, medium + large crew snapshot, lock-screen circular / rectangular / inline, mic App Intent, `GET /widget/snapshot`) plus `WidgetFamily.systemExtraLargePortrait` (iOS 27.0+, VERIFIED from Apple's DocC: "an extra-large widget that uses a portrait orientation … on the Home Screen on iOS"), gated `#available(iOS 27, *)` so the target's deployment target stays 17. Constraints VERIFIED 09-10: needs the iOS 27 SDK = Xcode 27 (beta 6 `27A5252f` is on this Mac beside 26.6); EAS has NO Xcode 27 image yet (`latest` = `macos-tahoe-26.5-xcode-26.6`, our `eas.json` `image: "auto"` = Xcode 26.0 for SDK 54) — the cut waits for an Xcode 27 image or goes local. Today's binary already carries the build-66 "Next up" event widget (small/medium) and nothing else. Mockup of the portrait page before Swift. Spec: `WIDGETS.md`.
8. **Xcode "Update to recommended settings" nag (Jeff 09-10, "leave it, but put it in to build 77").** Cosmetic only. VERIFIED 09-10: `RECOMMENDED_IPHONEOS_DEPLOYMENT_TARGET = 15.0` under Xcode 26.6 / SDK 26.5, ours is 16.0 — the deployment target is NOT the trigger and stays 16.0. The nag comes from the prebuild template's `LastUpgradeCheck = 1130` + the obsolete `ALWAYS_EMBED_SWIFT_STANDARD_LIBRARIES` / `EMBEDDED_CONTENT_CONTAINS_SWIFT` on both targets. `ios/` is gitignored and regenerated, so the fix is a tiny `withXcodeProject` config plugin (`expo-build-properties` has no knob for these): set `LastUpgradeCheck` to the running Xcode's version and strip the two obsolete settings. **Do NOT accept the sheet's other items:** `ENABLE_USER_SCRIPT_SANDBOXING` breaks RN/Expo pod script phases; WMO / warnings / string-catalog symbols / `-target` parallelization change nothing we ship.
Pre-flight: typecheck · trap-check · corner gate · `ios/` + `android/` gitignored · lockfile clean · Play target-API 36 · `buildNumber` and `versionCode` move together · `eas submit` is Jeff's.

- **expo-location patch: forward course/speed TRUST** — iOS `CLLocation.courseAccuracy` / `speedAccuracy`, Android `hasBearing()` / `bearingAccuracyDegrees` / `speedAccuracyMetersPerSecond` (VERIFIED 09-10: neither platform forwards them; `ios/LocationUtils.swift:30` sends only `heading: location.course`). Then the estimator weights the course by its accuracy instead of the 3 m/s threshold, and the Android 0°-drop workaround in `src/fixCourse.ts` goes. Same patch: add `activityType` to the iOS WATCHER options (only the task sets it today).
- **Mapbox Navigation SDK enhanced location engine** (map matching + dead reckoning + tunnels, native) — evaluate against our route projection / snap-to-road / pose estimator on all four surfaces. The road-heading rule in AM is the OTA-sized version of what that engine does natively; a native animated puck turn is what a LOCKED phone needs (JS renders at ~1 Hz there).
- **Car surface projection window** — `CarMapView` projects GLOBALLY (`projectOntoRoute(lat, lng, routeLL, …)`), the phone has the arc window (`projAtRef`, movedM, travelHdg); a roundabout's legs are 10–20 m apart. OTA-able, half a day; the AM release + the > 90° refusal cover the wrong-leg case in the meantime.
- **`projectOntoRoute` into a pure module** so the pose gate runs the SHIPPED chord (today the gate re-implements it; the refuter verified the two agree analytically).
- **Departure zone** (Mapbox: within 40 m of a leg's first coordinate the course may unsnap at any speed/accuracy) — pulling out of a lot at route start, AM pins the nose to the street until ≥ 3 m/s with > 45°. Optional OTA.
- **Android NAV_TASK options after process death** — HYPOTHESIS (refuter 09-10): persisted options come back through org.json and `timeInterval as? Long` may read an Integer as null → the enum default (500 ms for BFN = harmless; 2000 ms under Lite). One logcat check after a force-stop settles it.

### C · Backend / Supabase
1. scan-worker v3 hardening — HELD for Jeff's go (review 09-02).
2. `POST /api/entitlement/redeem` + premium-gate entitlement endpoints (build 80 path).
3. Prior-abuse check for the holes closed 09-06 — Mongo is reachable now (Atlas connector works); Jeff has not asked.
4. `mapbox tokens.txt` rotation — Jeff's.
5. Two cron files share `scan-worker-tick`.

### D · Decisions only Jeff can make
1. Invite codes are admin-only since the security release — should members invite for the club launch?
2. HUD cap (A6) · failed-render copy (A7) · scan-worker item 6 policy.
3. Pricing / free-tier / donate URL (ROADMAP §5).
4. Tripo pre-buy, twin quality, plate privacy, tier enforcement (HANDOFF-2026-09-02 §7).
5. **Jeff's own list** ("i have a huge list of fixes and errors") — still not sent.

---

## 5 · Codex in the process (unchanged from 09-06; full section in `HANDOFF-2026-09-06.md` §2)

Codex is a second opinion (Jeff 09-03). Adversarial review on COMMITTED code before a nav-behaviour OTA:
`node ~/.claude/plugins/cache/openai-codex/codex/1.0.6/scripts/codex-companion.mjs adversarial-review --wait --base <commit> "<focus>"` <!-- doc-check:ignore: path under ~/.claude -->
— its parser accepts only `--base/--scope/--model/--cwd` + `--json/--background/--wait`; **any other flag (`--effort`, `--help`) becomes focus text and starts a review.** `/codex:rescue` for investigations — the subagent ADDS `--write` unless the request says "investigation-only". A Codex bug hunt hands over a HANDOFF; Claude reviews the REAL checkout diff (the staged copies differ), ports the tests, releases. Side effects seen 09-06: an uncommitted edit shipped in OTA-AB; a Debug build cut Jeff's phone off OTAs for an hour; the classifier blocks backend pushes (commit locally, Jeff pushes; poll `/api/health rev`).

**2026-09-07 morning — now Jeff's RULE, not a recommendation (`RULES.md` §4; memory `codex-review-gate-when-not-certain`):**
*"if you are not 100% that the resolution is going to fix the issues please get codex to review it with you. double check his
work and implement if correct. the same issues keep arising and i am tired of it."* Context: he was told things were resolved
that were not. Every fix now carries a written verdict before it ships — **VERIFIED-FIXES** (receipt) or **NOT CERTAIN** →
read-only Codex review with the failing case as focus → each finding re-verified here → implement what holds → the report
says VERIFIED or NOT CERTAIN per symptom. Codex readiness re-checked 09-07: `codex login status` → "Logged in using ChatGPT".

---

## 6 · Roadmap as recorded

- **Now:** Jeff's list (D5); read the §3.2 verdicts on every drive; §4 A items as they are root-caused. Bundling per OTA is Jeff's call; every fix in a bundle is still root-caused, gated, sim-smoked.
- **Build 77** (both platforms, paid): §4 B. **78:** Play closed testing + TestFlight public link · **79:** hardening + staged invites · **80:** GRC club launch (170 members). Must harden before then: Render, Supabase, Play declarations (8 open), background-location demo video, UGC Guideline 1.2.
- **Parked by Jeff:** tier-skinned icon; weather radar overlay; offline.

---

## 7 · Gates and commands (all green at `431b3c9` / `629d3f5`, run 2026-09-07 08:0x PDT)

```bash
# frontend — /Users/jeffmorton/convoy-frontend/frontend, branch mapbox-migration
yarn typecheck                                   # exit 0
python3 scripts/trap-check.py                    # "trap-check: 13 rules, 0 hits."
python3 scripts/doc-check.py --live              # "Live docs clean." (structural only)
for f in tools/sim-qc/*_test.mts; do node --experimental-strip-types "$f"; done   # 9 files, each ends "PASS"
node tools/sim-qc/nav_start_permissions_test.cjs # pass 6 / fail 0
# backend — /Users/jeffmorton/convoy-backend (no venv of its own; Codex's works)
/Users/jeffmorton/Documents/Codex/2026-09-06/h/work/test-venv/bin/python -m pytest -q -p no:cacheprovider --disable-warnings \
  tests/test_scan_slots.py tests/test_scan_store.py tests/test_security_regressions.py tests/test_events_windows.py tests/test_arrival_quip.py   # "55 passed"
# NEVER run tests/test_convoy_v3.py blindly — it is the old LIVE-server suite.
```

Node gates (9): `arrival_line` (order, closers, §9 place closers) · `car_spot_trust` (A–H incl. Say Phin's two records refused) · `corner_blend` (11) · `offroute_storm` (A–R) · `place_identity` (A–G, Jeff's Langley address) · `ribbon_anchor` · `ribbon_lead` (HYPOTHESIS gate) · `speed_episode` (A–I) · `timer_starve`. trap-check's 13 rules are listed in `HANDOFF-2026-09-06.md` §7.

**Publish ritual** (`.claude/skills/ship-ota/SKILL.md`): gates → `git status` (read any unexplained diff) → commit + push → parity (`eas build:list` both at 1.27.0, channel `mapbox-migration`) → `npx eas-cli env:exec preview "npx eas-cli update --branch mapbox-migration --clear-cache -m '<summary>' --non-interactive"` (**never bare `eas update`**) → `python3 tools/ota/verify-bundle-key.py <group> 1.27.0` → `KEY_PRESENT=1` both → testers get ONLY the red pill.

**Backend release:** commit on master → Jeff pushes (`git -C ~/convoy-backend push origin master`) → poll `GET /api/health` for `rev` → DDL via Supabase MCP `apply_migration`, data via `execute_sql`.

**Reading the crew:** Chrome → `web.whatsapp.com` (logged in) → click the "Hairpin App Testing" TEXT element (clicking the row does not open the pane) → `get_page_text` shows the latest screen; scroll up in the pane (~(1000,450)) for earlier. Sending: click the composer, `type` ONE line (Enter sends), Enter — as Claude, only on Jeff's word.

**Sim tooling:** `./scripts/sim-ios.sh`, sim `95B623F9-F5E2-436B-A63E-5AFE05DFF9C8`, `tools/sim-qc/drive.sh <UDID> park|go|shot|stop`, `measure.py` (day palette only), Xcode MCP taps (`t x y`, `drag x1 y1 x2 y2 [s]`, `w s`; sessions die ~3 min), location stream stalls near 49.1139,-122.5243 → `simctl location clear` + `go`.

---

## 8 · Traps learned 09-05 → 09-07 (memory file named where one exists)

- **Never gate the car-spot WRITE path** (AC): the spot must follow the car; judge the persisted record at hydrate (`carSpotTrust`). A two-hour regression cost Say Phin a 9.3 km wrong pin. Memory `event-pushes-and-car-spot-trust-2026-09-06`.
- `crash_reports.update_id` = per-platform update id; map with `eas update:view --json`; a row without the new field is old JS.
- Google Places: a TEXT search on a bare address returns the address; a NEARBY search (40 m, ranked by distance) returns the business at the unit. BC streets are numbers ("200A Street") — `addressTokens` handles "100 9420 200A St" vs "9420 200A St". Memory `scout-arrival-place-identity`.
- Node gates: a pure module imported by another pure module must use the `.ts` extension in the import (`allowImportingTsExtensions`); anything importing `./api`/AsyncStorage cannot be gated under Node — split the pure core out (`placeIdentityCore.ts`).
- The Mapbox MCP on this Mac has a bad token ("not in valid JWT format"); the app's own Mapbox calls are unaffected.
- MongoDB Atlas MCP works after Organization Settings → App Connections → "Allow users to connect AI clients" (read-only); connection id from `remote-atlas-connect(projectId 6a19062a955f1942bed4be94, Cluster0)`; database `convoy`.
- The auto-mode classifier blocks `git push` to the backend master and SQL-generating scripts; commit locally, Jeff pushes.
- A `grep | cut` can leak a key prefix — print names/lengths/hashes only (memory `never-print-config-files`, 09-06 addendum).
- Say Phin is he/him (memory `tester-handles`).
- **The sim can be driven with NO taps** (memory `sim-tap-free-verification-recipe`, 09-10): `simctl privacy grant location-always` writes 4 into locationd's PRIVATE enum (2 = when-in-use; hand-writing 3 made the app read undetermined); a `simctl location` scenario swap while the app runs delivers NOTHING — terminate + start + launch every time; settings/saved places are editable in `RCTAsyncLocalStorage_V1` (md5(key) files) with the app closed. Cameras still need a route; `convoy://go?to=` never fires there.
- **`./scripts/sim-ios.sh` breaks on the watch targets** since the companion landed (memory `ios-sim-build-works` TRAP 3): arm64-only xcodebuild after detaching the watch targets in the LOCAL pbxproj; `expo prebuild --clean` throws that away. The script needs a sim-only path that skips them.
- **OSM speed limits are 2-D:** an overpass road is the nearest way at the crossing. Direction is the tie-break (§1b).
- **Mapbox Standard's layers are INVISIBLE to `queryRenderedFeatures` on @rnmapbox 10.3.1** (no featureset API): the box came back with only our own POI-label copy and our own car, i.e. "no road here" on every road (the 20:12 sim run lifted the car at 12 m/s). Every "what is under the car" question goes to the app's OWN streets-v8 source (`ROAD_SRC_ID`, the road-snap's) via `querySourceFeatures` — roads, water, POIs and now buildings.
- **Streets v8 spells service types `service:parking_aisle`, `service:driveway`, `service:drive_through`, `service:parking`** (Codex); `2d-building` in Standard is a 0.05 m slab hidden when 3D is on, `3d-building` extrudes only `extrude=="true"` — a non-extruded footprint cannot hide the car.
- **A signed-in sim can be CLONED without credentials:** copy `Library/Application Support/` of the app's data container from the iOS 18.6 sim to the iOS 27 sim (`rsync -a`), install the same build, grant location — Xcode's DeviceInteraction (iOS 27 only) then TAPS: `t 186 77 w 2 t 131 128 w 5 t 115 831 w 3` = search → "Drive to qccam" chip → Start. Memory `sim-tap-free-verification-recipe`.

---

## 9 · Jeff's decisions this window (quoted; do not re-litigate)

- 09-05: "ship W build X", "go", "send it" ×2, "put this all in ota-z and send it".
- 09-06 morning: "bearing on"; "all i want is this to be fixed we do not have time to screw around… multiple nav fixes per ota… i have a huge list" → bundling is his call, no process lectures; "before we get to my list i want everything fixed"; End button "i dont remember" (closed), free-drive PBs "no", approved car "yes" (wired), Olaf's smoke request ditched.
- 09-06 midday: Codex fixes — "i am not allowing him to deploy… for you to review and deploy"; Atlas "toggle it"; "pushed"; "i deleted the app on the phone and reinstalled from test flight".
- 09-09 evening: "read the chat" → "ship it and send saphins message and address the chat about the drives" (OTA-AI + two 19:12 messages); "why are you guessing…stop it now" (three unverified claims in the AI message: the "48 on record" count and the "shape of your black screen / first solid lead" lines — **corrected in the group 23:55**); "fix the issues with drives… fix sayphins issue with car play i dont want any excuses" → OTA-AJ (drives + the JS half of the AA fix; Kotlin half held for 77); the slow-corner root cause → "do the instrmentation and estimator and ship it" → OTA-AK; "send the crew the message. but first did we also fix Sayphins AA issues" → answered NO with receipts (15-agent workflow, 12/12 refuters held), message sent 23:55 as drafted (pickup, gyro steering, drives, Say Phin not-fixed + correction).
- 09-06 evening: "look at the whats app chat… do not make up excuses and apologize fix these issues"; "send it" (OTA-AC + the 20:10 message); "do both and i ran the push" (past events till midnight + confirm buttons); "read our chat claude. more errors are appearing…" → AE; Scout arrival by address details "ok thats fine" + the personal-touch lines → AF; "back end pushed… reply to the chat and include this new feature (btw sayphin is a male)".
- Standing: CarPlay/AA standalone, period (09-04); 77 on both; red pill only; Codex = second opinion; identify as Claude to testers.

---

## 10 · Unknowns, stated (each with the one check)

| unknown | the one check |
|---|---|
| Did AE refuse Say Phin's stale SunTea pin? | his next map session: `draw-cmp … spotDrop=drove-after` then `mode=raw` |
| Does AF name a real business on arrival, and how does the quip read? | anyone's next arrival at a shop: `dest-place name=` → `arrive-speak poi=1 ending=…`; `arrival_quips` in Mongo |
| Say Phin's green line | his answer in the group |
| Did OTA-AJ's one-pass AA screen build (`defersNativeCreate`, JS) change Say Phin's black screen? **Not fixed, unverified** — the Kotlin guards (`createTemplate` catch Throwable, `setRootTemplate`/`pushTemplate` wrapped, `carContext.isInitialized`) are NATIVE and first reach a phone in build 77; the patch was byte-identical from a078b48 through both build commits | his first AA connect on AJ or later (Android update `01a0894d-…` / AK `01a089de-…d90b-796c…`): `aa-crumb root-render/template-set/root-mount` then `surface-render` vs silence. As of 09-09 23:55 PDT he was still on AI (`01a08914-…`), zero rows after 19:21 PDT, and NO Android device had picked up AJ or AK |
| Rodrigo's two 09-05 crashes | his `.ips` files |
| Olaf's `updateCameraStop` rejection | the rows in the 10 s before each; which surface's camera |
| Why JS timers freeze with CarPlay foreground + locked phone | native (77) |
| Bearing constraint effectiveness | `depart-rank constrained=1` vs `off=` joined to speed |
| Real-drive feel of the 20 s speed dwell | `speed-alert` rows per episode |
| Owner-based scan recovery for Olaf | his next reinstall: `carscan-restored` vs the 503 message |
| Prior abuse before the security release | Mongo queries (connector works) — not asked |
| Which people are `Ni GR` and `SMSGRC` | ask Jeff |
| What did Nova's route greeting say on 09-10 morning (the "double line")? | unrecoverable; the next route start's `greet-prep src= len= text=` row (§1b) |
| Does the Clearbrook overpass still ding at 95? | Jeff's next westbound TCH pass: `speed-limit lim=…` rows show `x=50@…` rejected and no `speed-alert` |
| Do the neon camera pins / clusters / place pins render on device and on CarPlay? | one look at any routed drive with `speedCameras` on (the DriveBC pin is sim-verified; the rest share the path) |
| Does `convoy://go?to=` still open a route on a phone? | it produced no `deeplink drive` row on the local sim build, warm or cold (HYPOTHESIS: scene-delivered URL never reaches expo-linking there); Jeff runs it from Safari once → a `deeplink drive to=home` row |
| Does the road-only lift hold on a PHONE at a light (cost, no lift) and lift in Jeff's driveway / a parkade? | his next drive: `self-lift q … ms=` (a phone's query cost) and `self-lift to=` rows — `why=road|speed|nav` on roads only; `why=offroad lot=1` in the driveway; CarPlay rows `surf=car` |
| Was the 3.5 s JS stall at nav start on the sim (20:33:33) the lift's ribbon-owner re-render (now 2×/s) or nav start itself? | a `main-gap surface=phone fixGap<100` row at Jeff's next nav start says nav start; none says the throttle was it |
| Do un-heighted extruded footprints (Standard draws `est_height`, our source has none → 4 m) leave the car inside a tall parkade? | a `self-lift q … bld=4` row where the map shows a tall building |
| Was AM's road heading right on the 17:36 drive? | VERIFIED partly: `src=road` through the roundabout, nose within ~10° except ~35° at the sharpest exit; `nav-loc` pairs every 30 s only while parked (§1b watchdog note) |

---

## 11 · Where things live

| what | where |
|---|---|
| 09-05 → 09-06 log (every root cause + receipt, the refutation pass) | `HANDOFF-2026-09-06.md` |
| 09-02 → 09-05 log | `HANDOFF-2026-09-02.md` |
| Ranked issues, launch table, decisions pending | `ROADMAP.md` §5–§7 |
| Arrival by name | `src/placeIdentityCore.ts`, `src/placeIdentity.ts`, `src/arrivalPlaceClosers.ts`, `src/arrivalEndings.ts`; backend `/scout/arrival-quip` |
| Car spot trust | `src/carSpotTrust.ts`, `src/locationPrivacy.ts`, gate `tools/sim-qc/car_spot_trust_test.mts` |
| Event pushes | backend `create_event` / `update_event` / `_sweep_event_reminders`, `tests/test_events_windows.py`; app `app/(app)/_layout.tsx` (category + action tap), `app/(app)/hub.tsx` (past events till midnight) |
| Codex plugin + artefacts | `~/.claude/plugins/cache/openai-codex/codex/1.0.6/`, `~/Documents/Codex/2026-09-06/h/` <!-- doc-check:ignore: outside the repo --> |
| Backend + offline tests (55) | `~/convoy-backend/server.py`, `supabase_admin.py`, `tests/` |
| Gates | `scripts/trap-check.py`, `scripts/doc-check.py`, `tools/sim-qc/`, `tools/ota/verify-bundle-key.py` |
| Rituals | `.claude/skills/ship-ota/SKILL.md`, `.claude/skills/cut-build/SKILL.md` |
| Memory index (165 entries) | `~/.claude/projects/-Users-jeffmorton-convoy-frontend/memory/MEMORY.md` |
