<!-- ═════════ RULE #1 — READ THIS BEFORE ANYTHING ELSE ═════════ -->
# 🛑 NO GUESSING. NO THEORIZING. NO HALLUCINATING.

**Every claim is VERIFIED, or the word HYPOTHESIS is said out loud. No exceptions.**
Reading code and reasoning about it is **NOT** verification. Check the instrumentation
that ALREADY EXISTS before inventing an explanation. *"I don't know — here is the ONE
check that would settle it"* is a GOOD answer.

---

# HANDOFF — 2026-09-02, 18:30 PT

**This file is CURRENT STATE.** It supersedes `HANDOFF.md` (whose header is stale at
build 70) and the "Shipped state" section of `ROADMAP.md` for anything dated after
2026-09-01. Everything below was verified today from the database, the EAS API, the
running edge functions or the files themselves — receipts inline.

**Read order for a new session:** `RULES.md` → this file → `ROADMAP.md` (ranked issues) →
`SCAN-PIPELINE.md` (the photo→car pipeline, now finished) → `CLAUDE.md` (conventions).

---

## 1 · Where the app is RIGHT NOW

| | |
|---|---|
| Marketing version | **3.10.0** (`app.json`) |
| `runtimeVersion` | **1.27.0** — fixed string, not a policy (bumped for build 75, 2026-09-02 22:44) |
| Native build | **75**, iOS `a65064e0` + Android `967a8402`, both `FINISHED` 2026-09-02 22:52/22:58, both runtime **1.27.0**, both channel `mapbox-migration`. Parity is GOOD. **Submits pending Jeff** (classifier blocks `eas submit` for Claude) — see §4. Until then testers are on **74 / 1.26.0**. |
| Store | Play internal track: 74 completed. TestFlight: 74 submitted by Jeff 08-27. |
| Branch | `mapbox-migration`, HEAD `c86a336`+docs, **0 unpushed** |
| Current OTA | group **`0a6ea103`** (update ids `01a062b7-…`), published 2026-09-02 ~08:55 PT, both platforms, runtime 1.26.0. `KEY_PRESENT=1` on both — weather key proven baked. |
| Scan pipeline | **LIVE and automatic.** `pipeline_flags.enabled = true`, `require_slot = false` |
| Backend | Render, `Bigperm1/convoy-backend` master `9b5bda2`, auto-deploys on push (VERIFIED: `/api/entitlement` flipped 404→401 45 s after the push) |

### Who is actually running today's OTA (VERIFIED, `crash_reports` last 30 h)

**On `01a062b7` (today's OTA):** Jeff · Enablewhore · SPL_GRC · Rodrigo · GRSIENNA ·
Ni GR · SMSGRC. Essentially the whole crew picked it up.

**⚠ STRANDED RIGHT NOW — `launch_kind='unknown'`, `update_id=null`:**
- **`Ni GR`** — 347 rows, 1 session, last seen 09-02 **17:45**
- **`GRSIENNA`** — 297 rows, 2 sessions, last seen 09-02 **17:43**

Both are running the **embedded build-74 bundle**, not today's JS, and the red pill
cannot appear for them. This is the CarPlay-first stranding bug (`CARPLAY-OTA-STRANDING.md`,
root-caused 08-28, fix is build-bound — see §4). Interim workaround to hand a tester:
> Unplug from the car, force-quit and reopen the app while disconnected, then plug back in.

(`Jeff` also shows `launch_kind='updates-disabled'` on 11 sessions — that is his local
sim/dev build, not a tester problem.)

---

## 2 · What shipped in the last 24 hours (this session)

All on `mapbox-migration`, all pushed. Newest last.

| commit | what |
|---|---|
| `26b21f4` | **Watchdog-kill fix.** Nothing that moves per tick is a layer property any more. |
| `dc6c2ab` | roadmap entry for the above (OTA `586e1273`) |
| `cfe0090` | Car-scan viewfinder shoots at full sensor resolution (OTA `164b4e93`) |
| `b4ff998` | **The automatic scan worker** — manifest upload → Tripo → QC → publish, no human step |
| `3272eaa` | Worker reads its own key from Vault — one human paste, not three |
| `990a7ff` | docs: pipeline LIVE, first unattended tester render |
| `ddec6f4` | review hardening + the phone receipt |
| `e372d91` | roadmap: v3 hardening list |
| `c2d43d0`, `c51a1c4` | runbook corrections + executed-status banner |
| `afcb2e6` | **Scan slots** — the app asks the backend for its render, server mints the id |
| `2a77d16` | **Worker v3 part 1** — several cars per tick, 15 s ticks, abort-before-spend |
| `8e1cd8e` | **Worker v3 part 2** — Tripo rate limits, pool cap, server-issued slots |
| `a5f2eed` | docs: v3 live |
| backend `9b5bda2` | `/api/scan/slot`, `/api/entitlement`, owner-only tier setter |

### The three root causes closed this session

**1. Watchdog kills (`0x8BADF00D` ×5 on Jeff's 09-01 09:00 drive).**
Root cause, from the `.ips` stacks + `@rnmapbox` source: **any per-tick change to a layer
`style`/`filter` prop is a full main-thread read-modify-write** (`RNMBXLayer.reactStyle
didSet → StyleManager.updateLayer`; there is no style diff). The route ribbon was pushing
`lineTrimOffset` + a re-baked congestion gradient at 12 Hz across three layers per surface,
on one main thread shared by phone and CarPlay. Fix: **per-tick state now lives in the
SOURCE** — `src/routeRibbon.ts` (new) cuts ribbon geometry at the car and carries
colour/alpha per feature; the self-car marker's heading rides its source feature.
Verified after the fix by an 8 s / 1 ms `sample` while navigating at 100 km/h through
bends: `getStyleLayerProperties` **0 hits**. The same mechanism was the "Show map froze
the phone and CarPlay" report — one main thread, two surfaces.
**Rule now in `CLAUDE.md`: nothing that moves per tick may be a layer property.**

**2. Weather died on every surface for ~20 h / 13 OTAs.**
A bare `eas update` bakes an **empty** `EXPO_PUBLIC_OPENWEATHER_KEY` (`PROD_OPENWEATHER_KEY`
is `""` in `src/api.ts`; the real key exists only in the EAS environment). Silent — no error,
just no weather. **The OTA ritual is now mandatory:**
```bash
npx eas-cli env:exec preview "npx eas-cli update --branch mapbox-migration --clear-cache -m '<msg>' --non-interactive"
python3 tools/ota/verify-bundle-key.py <group> 1.26.0     # expect KEY_PRESENT=1 on BOTH
```
`tools/ota/ow.key` is gitignored and regenerated with
`npx eas-cli env:exec preview 'printf "%s" "$EXPO_PUBLIC_OPENWEATHER_KEY" > ow.key'`.
This is enforced in `.claude/skills/ship-ota/SKILL.md`.

**3. `yarn typecheck` — the OTA gate — was RED and nobody noticed.**
The new Deno worker sources under `supabase/` were swept into the app's `tsconfig.json`
`include`, producing **111 errors**. Fixed by adding `"exclude": ["supabase"]` (Deno checks
those itself via `deno task check`). `yarn typecheck` now exits 0. **If a future session
adds server-side TS, exclude it too.**

### Also shipped/changed
- Nav UI: End + "show map" are 50×50 r14 gradient candy tiles side by side; Add stop /
  Share pills moved off iOS blue onto the app gradient; the Share pill next to the
  bookmark was **removed**; bookmark = candy red, share = system green
  (`ACTION.save = #FF3B5C`, `ACTION.share = #2DEC86` in `src/theme.ts`) **system-wide**;
  "Round trip" now fits its button.
- `src/heatProbe.ts` gained the **`main-gap`** breadcrumb (see §6 — read the caveat).
- `supabase/functions/register-scan/index.ts` is **in the repo for the first time** — it
  had only ever existed on Supabase. v2 was fetched verbatim before being rewritten as v3.
- New rule saved: **Chrome only, never Safari** (Jeff, 09-01). CLI logins open the system
  default browser; use `open -a "Google Chrome"` or a `--no-browser` flag.

---

## 3 · The 3D scan pipeline — FINISHED

**`SCAN-PIPELINE.md` is the single source of truth and is up to date.** Do not re-derive
any of it. Summary of state only:

Photos → finished car, **no human step**, twice proven end to end:

| run | path | time | credits | errors |
|---|---|---|---|---|
| `enablewhore-20260901-210315` (Olaf, real tester) | v2, legacy handle | **7 m 47 s** | 50 | 0 |
| `qa-20260902-081500` (supervised) | v3, server-issued slot | **5 m 46 s** | 50 | 0 |

Olaf's iPhone logged `carscan-ready hero=1 map=1` at 03:47:51 — the return leg to the
sending device is proven, not assumed. Published bytes were re-downloaded and re-inspected
with `tools/glb-pipeline/glbinfo.py`; sha256 matches the worker's ledger in both cases and
both twins pass every Mapbox gate.

**Live configuration (verified 18:28 PT):**
`enabled=true` · `require_slot=false` · `daily_credit_cap=6000` (100 cars/day) ·
`per_user_cap=2` · `min_balance=300` · cron `scan-worker-tick` **every 15 s** (jobid 2) ·
edge fn `scan-worker` **version 4** · `register-scan` **version 3** ·
239 ticks in the last hour, 238 × 200 · **0 worker errors in 24 h** · 150 credits spent
in 24 h · jobs: 5 done / 3 skipped · slots: 1 issued, 1 consumed, 0 released.

**The off switch (one line):**
```sql
update public.pipeline_flags set enabled=false, paused_reason='manual', updated_at=now() where id=1;
```

**The one flip still owed** — after testers are on OTA `0a6ea103` (all but the two
stranded ones are), slot-less uploads should become impossible:
```sql
update public.pipeline_flags set require_slot=true, updated_at=now() where id=1;
```
⚠ Do **not** flip it while `Ni GR` and `GRSIENNA` are stranded on the embedded bundle —
their uploads would be skipped, never rendered.

**Money (VERIFIED from Tripo's own billing console + docs, `tripo-billing-facts` memory):**
no auto-recharge exists, and no API can buy credits. Flat $0.01/credit, credits never
expire, 50 credits = $0.50 per car. Launch load 170 members × 2 scans = **17,000 credits =
$170, a one-time pre-buy**. Balance was 1,840. Low-balance email alert is ON at 500 —
raise it to ~2,500 before launch. Tripo allows **10 concurrent tasks per account** (code
2000 above that); the worker caps itself at 8 and treats 2000/1007 as a wait, not an error.

---

## 4 · BUILD 75 — CUT (see the record below); what it carried and what moved to 76

### ✅ BUILD 75 — CUT 2026-09-02 22:44 PDT, BOTH PLATFORMS FINISHED (runtime 1.26.0 → **1.27.0**)
- iOS `a65064e0` (profile `mapbox-ios`) finished 22:52 · Android `967a8402` (profile
  `mapbox-android-store`, versionCode 75) finished 22:58 · both **v3.10.0 / build 75 /
  runtime 1.27.0 / channel `mapbox-migration` / distribution store** (verified on the build
  records). Commit `c86a336`. Parity: GOOD.
- **Submits: `eas submit` is blocked for Claude by the auto-mode classifier (as on 74) —
  Jeff runs both:** `eas submit --platform ios --id a65064e0-da9a-4dc7-b75d-a08f68ddcfb3 --profile production`
  and `eas submit --platform android --id 967a8402-9724-48e5-a66d-cf2dfa919ab9 --profile production`
  (Play `internal` track). Until they land, testers stay on 74.
- **What 75 carries (over 74):** the CarPlay-first OTA-stranding fix (`CARPLAY-OTA-STRANDING.md`
  B1/B2/B4 — the cold-CarPlay host wait never mints the RN host; placeholder + App Group
  diagnosis + unbounded slow poll; ceiling 20 s → 90 s) · `HairpinSystem.getSharedDefaults/
  removeSharedDefaults` + the `carplay-host-ceiling` next-launch report · `hairpin://` scheme
  ADDED beside `convoy://` · Google Maps SDK dropped at prebuild · `eas.json` mapbox-ios image
  `auto`. JS embedded (all OTA-able): `main-gap` full AppState sequence + `sinceApp=` ·
  `cam-probe` (applied zoom/pitch vs target + speed, on change, ≤1/s) · `route-fetch-fail
  why=` on the `fetchRoutes` catch · updates-log harvest oldest-first with entry timestamps.
- **NOT in 75:** RevenueCat, widgets/watch native, `Screen.setMarker` patch, AA `isConnected`,
  the OTA-able self-heal A1. **Widgets + watchOS = build 76** (no watch target exists; the
  `WIDGETS.md` haptic spike must precede any watch code).
- ⚠ **The runtime bump is a one-way door:** every OTA from now on targets 1.27.0 — a tester
  still on 74 gets nothing further. Sequence: crew installs 75 → then OTAs. Do NOT flip
  `require_slot` until the stranded/74 testers are on 75.
- Pre-flight that earned the green first try: `yarn typecheck` · `expo export` both platforms ·
  a **clean** local `expo prebuild --clean` + Release compile (the first local compile reused a
  stale `ios/` from Aug 14 and proved nothing — check `CarSceneDelegate.swift` carries the new
  constants before trusting BUILD SUCCEEDED).


Build 74 is the shipped native binary. Everything since has gone OTA. **Nothing forces a
build 75 today** — this is the accumulated native-bound backlog, to batch when Jeff calls it.
`eas build` costs money and needs his explicit go-ahead.

**⚠ Runtime rule:** if `runtimeVersion` bumps for 75, **both platforms must be cut** or the
un-rebuilt one is orphaned from every later OTA (this happened at build 62). Cutting 75
without native changes does not require a bump.

| # | Item | Why native | Receipt | State |
|---|---|---|---|---|
| 1 | **RevenueCat / purchases SDK** | No purchase code exists at all. Blocks the $14.99 Ultra paywall, and therefore blocks enforcing "Ultra includes 2 scans". | `react-native-purchases` is **not** in `package.json` or `yarn.lock` (0 hits, verified today); `src/PaywallSheet.tsx` buy buttons are a stub Alert; `src/entitlements.ts:26` `ENTITLEMENTS_ENFORCED = false` | **NOT STARTED.** The server side is already built and waiting (§7). |
| 2 | **CarPlay-first OTA stranding** | Fix is in `plugins/withConvoyCarPlay.js:375-413` — the plugin mints the RN host itself and resolves the embedded bundle because `launchAssetUrl()` is still nil. | `CARPLAY-OTA-STRANDING.md` (root-caused with source citations 08-28); **live victims today: Ni GR, GRSIENNA** | Root-caused, fix specified, **not written**. Highest field cost of anything here. |
| ~~3~~ | ~~**Android Auto pinch-to-zoom**~~ | — | — | ✅ **SHIPPED IN BUILD 72 — not a 75 item.** `patches/react-native-carplay+2.4.1-beta.0.patch:513` carries `override fun onScale(...)`; patch dated 2026-08-12, the 72 cut. The `aa-pinch-needs-native` memory was STALE and has been corrected. |
| 4 | **Tier-skinned app icon (Gold/Silver)** | iOS alternate icons need a custom config plugin and pop a system alert; Android activity-alias loses home-screen position. | memory `tier-skinned-app-icon` | **PARKED by Jeff** until entitlements exist. |
| 4b | **`react-native-carplay` `Screen.setMarker` patch**, and a native sync `isConnected` getter on `RNCarPlay` | Native surface-API gaps; the second leaves half the Android-Auto background-GPS leak open. | sweep 2026-09-02 | NOT STARTED |
| 5 | **Google Maps native SDK drop** | Happens automatically at the next prebuild — `app.json` no longer lists the Google Maps plugin and the JS deps are already gone. | `react-native-maps`, `expo-symbols`, `expo-background-fetch`, `@vis.gl/react-google-maps` are **all 0 hits in `package.json`**; the 11 remaining source hits are **comments only**; `node_modules/react-native-maps` absent | **DONE in JS — nothing to do.** ⚠ `CLAUDE.md` still says this is "staged for the next native build" — that line is now stale. |

| 6 | **`hairpin://` deep-link scheme alias** | `app.json` `"scheme"` is native config. | `app.json:8` is still `"convoy"` only (verified today) | NOT STARTED. ⚠ **ADD it alongside `convoy://`, never rename** — a rename breaks every link already in the wild. |
| 7 | **Widgets + Apple Watch** (`WIDGETS.md`), incl. the mic App Intent and a watchOS target | New native targets. | `WIDGETS.md` | NOT STARTED |
| 8 | **Hold-to-talk on CarPlay** | Native button behaviour. | sweep 2026-09-02 | NOT STARTED, unplanned |
| ? | **CarPlay "End button did not work"** | Unknown whether the press ever reached JS. | open field report | ⚠ Needs **Jeff's answer**, not code — is it build-bound at all? |

**Proof the backlog is 0% written:** build 74 is commit `ff0de488`, and
`git diff ff0de48..HEAD -- app.json package.json yarn.lock plugins/ patches/ modules/ targets/ eas.json google-services.json`
returns **empty** (verified today). Everything since the cut is JS or docs. There is
nothing to "carry over" into 75 — only work to write.

**Pre-flight before queueing 75** (free, and it would have caught the error that killed the
first 74 attempt): run `expo export:embed` locally first.

**Store paperwork riding alongside 75** (not code): Play has 8 declarations open and
background-location is blocked pending a demo video; App Store Guideline 1.2 (UGC) wants
report + block + EULA, of which report/block/EULA are app code and could ship OTA. See
memory `store-listing-open-items`.

---

## 5 · Open issues, ranked

**Server-side, and it breaks a rule Jeff stated**
0. 🔴 **A failed render still burns one of the tester's two scans.** The worker correctly
   calls `releaseSlot` on any terminal `failed` — but it writes `released_at` into
   **Supabase** `public.scan_slots` (`supabase/functions/scan-worker/deps.ts:133`), while
   the backend's cap counts **Mongo** `db.scan_slots` (`server.py:3390`,
   `count_documents({user_id, released_at: None})`). The backend never reads Supabase back;
   the worker never writes Mongo. **The release is invisible to the gate the phone asks.**
   Jeff's rule — a failed render must not burn one of the two included scans — is therefore
   only half-implemented. Fix: the worker calls a backend endpoint on release, or the
   backend counts Supabase. Nobody has hit it yet (0 failed renders to date).

**OTA-fixable**
1. **`main-gap` cannot tell a freeze from a suspension — or from an idle loop** — see §6. This is the top item:
   it currently makes every reading ambiguous, including the Android Auto report.
2. **Failed renders are invisible to the tester.** `carScanStatus:'failed'` exists in
   `src/settings.ts` but nothing ever sets it. A failed scan just never arrives.
   (The worker now correctly gives the slot back — the tester is not charged a render.)
3. **`POST /api/entitlement/redeem` does not exist.** `src/entitlements.ts:173` already
   calls it. That is the club-code / beta-code path for build 80.
4. **Off-route churn worth a look.** Today: Rodrigo 30 off-route→30 reroutes, GRSIENNA 20,
   Enablewhore 15, Ni GR 11, SPL_GRC 9. All applied cleanly, no loops, so this may just be
   city driving — but the volume is worth one look at the divergence threshold before
   170 people arrive. HYPOTHESIS only; nobody has complained.
5. Map orientation "freak-out" and per-device-local settings — memory
   `tester-feedback-open-items`, unchanged.

**Native-bound** — everything in §4.

**Decided and closed this session:** the pipeline's daily cap ($3/day → 6000 credits/day),
identity binding (server-issued slots), Tripo rate limits, credit exhaustion no longer
disabling the pipeline.

---

## 6 · Today's drives — and a caveat you must read first

### ⚠ The `main-gap` instrument cannot yet distinguish a freeze from a suspension

Two independent reasons, both verified in code:

**(a) The loop it samples is not free-running.** `noteFrame` has exactly ONE caller —
`src/ConvoyMapbox.tsx:1446`, inside the SelfCarModel animation step. That loop *stops*
when the ease ends and is not re-armed on the GPS jitter dead-band. So a "gap" can simply
be **the animation loop not running**, with the main thread perfectly healthy. `main-gap`
does not prove a stall.

**(b) `app=` is sampled after the fact.** `src/heatProbe.ts:181` emits from inside
`noteFrame`, i.e. only when a frame finally arrives; `app=` is `_appSeen.slice(-1)`, the
most recent `AppState`. A phone that slept and woke reports `app=a` exactly like one that
froze. Threshold `GAP_ALERT_MS = 2000`, rate-limited to one row per 10 s — so a row count
is a **floor**, not a count.

Also: `surface=car` means "a head unit was connected", not "the car map stalled".

Today's rows make the problem obvious: **8 gaps of 60 s or more on `surface=car` with
`app=a`, the largest 866,221 ms — 14.4 minutes.** An app whose main thread is genuinely
blocked for 14 minutes is killed by the OS watchdog in ~20 s. Those are phone-lock /
JS-suspension, not stalls (HYPOTHESIS, but physically near-certain, and consistent with
the known iOS behaviour recorded in `carplay-dead-buttons-root-cause`).

**Do not report these numbers to Jeff as freezes without the discriminator.**

**The fix (OTA-able, small):** (1) log the whole `_appSeen` sequence instead of its last
character (`'aba'` = active→background→active proves a suspension); (2) add ms since the
last `AppState` change; (3) emit from a source that runs unconditionally, or tag rows with
whether the step loop was armed. Until then treat every figure below as an upper bound on
badness, not a measurement. **Candidate for the real Android stalls:** the road-snap path
calls `querySourceFeatures` (`src/ConvoyMapbox.tsx:2226`) on the road source — profile
that before anything else.

### What the data actually shows (2026-09-02, all handles, no fatals anywhere)

| handle | plat | rows | fatals | main-gap rows | max | off-route/reroute | arrivals |
|---|---|---|---|---|---|---|---|
| Jeff | ios | 1614 | 0 | 10 | 55.7 s | 2/2 | 2 |
| Enablewhore | ios | 1601 | 0 | 59 | 866 s | 15/15 | 2 |
| SPL_GRC | android | 599 | 0 | 23 | 37 s | 9/9 | 2 |
| Ni GR | ios | 442 | 0 | 0 | — | 11/11 | 0 |
| Rodrigo | ios | 428 | 0 | 1 | 702 s | 30/30 | 2 |
| GRSIENNA | ios | 284 | 0 | 0 | — | 20/20 | 1 |

Distribution by surface: `car/active` 71 rows (16 in 2–5 s, 30 in 5–15 s, 17 in 15–60 s,
8 over 60 s) · `phone/active` 18 · `phone/background` 4.

### Say Phin (`SPL_GRC`) — the Android Auto report Jeff asked about

**Android Auto connected and stayed connected. There was no AA error, no fatal, no crash.**
- 07:01:52 app opened and took the overnight OTA within seconds.
- 07:04:35 relaunch — that is AA connecting (the car surface restarts the app).
- **07:04:48 AA mounted cleanly**: `aa-crumb root-mount / root-render / template-set /
  surface-render`, plus `android-auto-canvas surface=213x107dp px=800x400`.
- Drove on the car surface to `arrive-speak` at **07:21:30**. Trip recorded.
- Three reroutes at 07:05 (diverged 48/58/55 m on steps 0–2), all applied, no loop.
- **Five `main-gap` rows on `surface=car`: 5.9 s, 3.3 s, 4.2 s, 8.9 s, and 12.9 s at
  07:20:47** — ~40 s before arrival. Two of the five land within a second of a route swap;
  the two largest do not.

The 3–13 s band during confirmed active navigation is the interesting one and is **not**
explained away by suspension. What he saw on the head unit is still **UNVERIFIED** — ask
him what the screen showed and roughly when. If it was around 07:20, it is the 12.9 s stall.

### Rodrigo

Drove 10:32–12:39 on **CarPlay**, connected cleanly (`carplay-onconnect conn=1`,
`carplay-idleroot-set`, `carplay-live-paint attempt=0`, `car-viewport surf=470x265`),
navigated (`nav-eta` stepping 0→1/11), used the surface (`carplay-tap:car-search`,
`carplay-tap:car-compass`, `carplay-gesture:tapZoomIn`), arrived twice. One `main-gap` row
of 702 s — almost certainly the phone asleep (see the caveat). **No errors.**

### ⚠ WhatsApp could not be read
The Hermes bridge reports **0 channels** — it is not linked (memory
`hermes-whatsapp-risk`: it is a QR-linked Baileys device, not the Mac app). I could not
read today's group chat. Anything Say Phin or Jeff said there is not reflected here.

### Tester handle roster — three gaps
`GRSIENNA` (ios, since 08-29), `Ni GR` (ios, since 08-23) and `SMSGRC` (android, since
08-24) are long-standing, active testers whose WhatsApp names are unknown to the roster in
memory `tester-handles`. Ask Jeff to map them; querying "how did X's drive go" for those
three will otherwise fail.

---

## 7 · Waiting on Jeff (nothing proceeds without these)

1. **Pre-buy ~$170 of Tripo credits** and raise the low-balance alert 500 → ~2,500.
   Credits never expire; there is no auto-recharge to configure.
2. **Build 75** — whether to cut it, and what from §4 rides in it. Costs money.
3. **Twin quality: 20k vs 40k faces.** The 40k evaluation build (28,367 verts, still u16)
   looks better but fails the current <25,000-vert gate. If 40k wins the gate moves to
   <35,000. `SCAN-PIPELINE.md` "Open quality work".
4. **Plate privacy.** A hero texture can carry a legible licence plate and the `models`
   bucket is public-read. Automation now publishes every tester's car with nobody looking.
5. **Tier enforcement.** When purchases exist: set `SCAN_REQUIRE_TIER=1` on Render and
   assign tiers via `POST /api/admin/users/{id}/tier` (owner-only, already deployed).
   Until then every signed-in account gets its two scans.

---

## 8 · Traps — do not step on these

- **Publish OTAs to `mapbox-migration`, never `preview`.** Read the installed build's
  `Channel` from `eas build:list`; assuming cost three silently-eaten updates on 07-05.
- **Never a bare `eas update`** — it empties the weather key. Use `env:exec` + the verifier.
- **`patch-package <name>` REGENERATES, it does not apply.** Bare `npx patch-package`
  applies. To check whether a patch is applied ask git:
  `git apply --check --reverse patches/<name>.patch`. Always regenerate with
  `--exclude 'android/build/'`.
- **`.env` is TRACKED in a PUBLIC repo** (`Bigperm1/convoy-frontend`). Never write a
  secret there. `.mcp.json` and `eas.json` are permanently dirty in the working tree —
  **never stage them**.
- **Line numbers in these docs drift — grep for the comment text, never trust `file:123`.**
- **Never run `eas submit`** without Jeff's explicit go-ahead, and never `eas build`
  without it either.
- **Four surfaces, always**: iOS phone, CarPlay, Android phone, Android Auto. "Android
  doesn't do this" is not an answer.
- **One nav change per real drive** (the 07-31 rule) — 11 OTAs in a day made the app worse.

**Armed right now — verified 2026-09-02, each bites silently:**
- ⚠ **`eas.json` has an UNCOMMITTED change** (`"image": "latest"` → `"auto"` on the iOS
  profile) that will **silently ride the next paid build**. Decide it deliberately before 75.
- ⚠ **`mapbox tokens.txt` is untracked AND unignored** in the repo tree
  (`~/convoy-frontend/`), whose `.env` is already tracked in a **public** GitHub repo.
  One `git add -A` publishes it.
- ⚠ **`.claude/` is not gitignored** — "never stage `.claude`" is discipline, not enforcement.
- ⚠ **`modules/convoy-call-detector/android/build/`** (Gradle output, ~860 KB) is untracked
  and unignored — the same shape as the patch-package `android/build/` trap.
- ⚠ **Two cron files share one job name.** `supabase/ops/scan_worker_cron.sql` (30 s) and
  `scan_worker_cron_15s.sql` (15 s) both `cron.schedule('scan-worker-tick', …)`. Running the
  OLD one silently reverts the v3 poll cadence. Delete or rename the 30 s file.
- ⚠ **`RULES.md:138` still orders "Always provide an APK + QR" for every Android build** —
  RETIRED (Android ships via Play internal; AA cannot be tested from a sideloaded APK, as
  its own next line says). Following it at the 75 cut wastes a paid build.

---

## 9 · Commands you will actually need

```bash
# ship an OTA (the ONLY correct way)
yarn typecheck
npx eas-cli env:exec preview "npx eas-cli update --branch mapbox-migration --clear-cache -m '<msg>' --non-interactive"
python3 tools/ota/verify-bundle-key.py <group> 1.26.0

# pickup instruction for testers — the RED PILL, and nothing else
# "Open the app, wait a few seconds on the map, tap the red 'Update ready — tap to install' pill."
```

```sql
-- pipeline health, one query
select enabled, require_slot, daily_credit_cap, per_user_cap, min_balance, paused_reason
  from public.pipeline_flags where id = 1;

-- a scan's life
select scan_id, status, reason, credits_spent, user_id, twin_published_at, hero_published_at
  from public.car_scan_jobs order by updated_at desc limit 10;

-- what the worker did (breadcrumbs land under the tester's handle)
select created_at, handle, message from public.crash_reports
 where platform = 'worker' order by created_at desc limit 40;

-- every tick's JSON verdict
select created, status_code, left(content::text,160) from net._http_response
 order by created desc limit 20;

-- WHO IS STRANDED OFF OTAs (run this before flipping require_slot)
select handle, count(distinct instance_id) sessions, max(created_at) last_seen
  from public.crash_reports
 where launch_kind = 'unknown' and created_at > now() - interval '3 days'
 group by 1 order by sessions desc;
```

---

## 10 · Where things live

| what | where |
|---|---|
| The scan pipeline, end to end | `SCAN-PIPELINE.md` ← finished, authoritative |
| Scan deploy runbook | `supabase/SCAN-WORKER-DEPLOY.md` (one human paste: `TRIPO_API_KEY`) |
| Worker source | `supabase/functions/scan-worker/` (+ `register-scan/`) |
| Backend | `~/convoy-backend/server.py` — "Entitlements + scan slots" section |
| CarPlay stranding | `CARPLAY-OTA-STRANDING.md` |
| Thermal analysis | `WHY-IT-HEATS.md` · probe in `src/heatProbe.ts` |
| Locked visual language | `DESIGN.md` (Gold = Ultra, Silver = Premium) |
| Locked car-surface spec | `CARPLAY.md` |
| Ranked issues + history | `ROADMAP.md` |
| ⛔ Stale header, history only | `HANDOFF.md` |
