diff --git a/node_modules/@lomray/react-native-apple-music/ios/CatalogService.swift b/node_modules/@lomray/react-native-apple-music/ios/CatalogService.swift
index e3f6577..883a32b 100644
--- a/node_modules/@lomray/react-native-apple-music/ios/CatalogService.swift
+++ b/node_modules/@lomray/react-native-apple-music/ios/CatalogService.swift
@@ -22,6 +22,10 @@ final class CatalogService {
   struct SearchResult {
     let songs: [[String: Any]]
     let albums: [[String: Any]]
+    // Convoy: playlists. Needed so a crew member can be sent a playlist by NAME —
+    // a library playlist's id is private to the sender's account and resolves to
+    // nothing on anyone else's device, so name is the only shareable handle.
+    let playlists: [[String: Any]]
   }
 
   func search(term: String, types: [String], options: SearchOptions) async throws -> SearchResult {
@@ -29,6 +33,7 @@ final class CatalogService {
       switch typeString {
       case "songs": return Song.self
       case "albums": return Album.self
+      case "playlists": return Playlist.self   // Convoy
       default: return nil
       }
     }
@@ -41,7 +46,8 @@ final class CatalogService {
 
     return SearchResult(
       songs: response.songs.map(MusicItemMapper.map),
-      albums: response.albums.map(MusicItemMapper.map)
+      albums: response.albums.map(MusicItemMapper.map),
+      playlists: response.playlists.map(MusicItemMapper.map)   // Convoy
     )
   }
 
diff --git a/node_modules/@lomray/react-native-apple-music/ios/MusicItemMapper.swift b/node_modules/@lomray/react-native-apple-music/ios/MusicItemMapper.swift
index fd92ce8..3cc1bb2 100644
--- a/node_modules/@lomray/react-native-apple-music/ios/MusicItemMapper.swift
+++ b/node_modules/@lomray/react-native-apple-music/ios/MusicItemMapper.swift
@@ -33,11 +33,24 @@ enum MusicItemMapper {
   // MARK: - Playlist
 
   static func map(_ playlist: Playlist) -> [String: Any] {
-    [
+    // Auto-collage playlists (no custom cover) expose NIL artwork — Apple builds the
+    // 2x2 collage client-side, so MusicKit hands us nothing. Fall back to the first
+    // track's cover so the playlist shows real art in Convoy instead of a gradient
+    // placeholder. Needs the playlist fetched `.with([.tracks])` (LibraryService does).
+    // (Convoy patch.)
+    var art = extractArtworkURL(playlist.artwork)
+    if art.isEmpty, let first = playlist.tracks?.first {
+      switch first {
+      case .song(let s): art = extractArtworkURL(s.artwork)
+      case .musicVideo(let v): art = extractArtworkURL(v.artwork)
+      @unknown default: break
+      }
+    }
+    return [
       "id": String(describing: playlist.id),
       "name": playlist.name,
       "description": playlist.standardDescription ?? "",
-      "artworkUrl": extractArtworkURL(playlist.artwork),
+      "artworkUrl": art,
       "trackCount": playlist.tracks?.count ?? 0,
     ]
   }
@@ -63,15 +76,23 @@ enum MusicItemMapper {
       "id": String(describing: item.id),
       "title": item.title,
       "subtitle": String(describing: item.subtitle ?? ""),
+      "artworkUrl": "",
     ]
 
+    // Recently-played items DO carry artwork on their underlying album /
+    // playlist / station value — bind the associated value and pull the URL
+    // through so the Recently Played row can render real art. (Convoy patch:
+    // upstream's mapper drops artwork for these items.)
     switch item {
-    case .album:
+    case .album(let album):
       result["type"] = "album"
-    case .playlist:
+      result["artworkUrl"] = extractArtworkURL(album.artwork)
+    case .playlist(let playlist):
       result["type"] = "playlist"
-    case .station:
+      result["artworkUrl"] = extractArtworkURL(playlist.artwork)
+    case .station(let station):
       result["type"] = "station"
+      result["artworkUrl"] = extractArtworkURL(station.artwork)
     default:
       result["type"] = "unknown"
     }
diff --git a/node_modules/@lomray/react-native-apple-music/ios/MusicModule.m b/node_modules/@lomray/react-native-apple-music/ios/MusicModule.m
index 3ec62e2..603b8a7 100644
--- a/node_modules/@lomray/react-native-apple-music/ios/MusicModule.m
+++ b/node_modules/@lomray/react-native-apple-music/ios/MusicModule.m
@@ -27,6 +27,7 @@ @interface RCT_EXTERN_MODULE(MusicModule, RCTEventEmitter)
 RCT_EXTERN_METHOD(skipToPreviousEntry)
 RCT_EXTERN_METHOD(restartCurrentEntry)
 RCT_EXTERN_METHOD(seekToTime:(double)time)
+RCT_EXTERN_METHOD(setShuffleMode:(BOOL)enabled)
 RCT_EXTERN_METHOD(togglePlayerState)
 RCT_EXTERN_METHOD(getCurrentState:(RCTPromiseResolveBlock)resolve rejecter:(RCTPromiseRejectBlock)reject)
 
diff --git a/node_modules/@lomray/react-native-apple-music/ios/MusicModule.swift b/node_modules/@lomray/react-native-apple-music/ios/MusicModule.swift
index 6c1b18c..c9e1f1e 100644
--- a/node_modules/@lomray/react-native-apple-music/ios/MusicModule.swift
+++ b/node_modules/@lomray/react-native-apple-music/ios/MusicModule.swift
@@ -181,6 +181,11 @@ final class MusicModule: RCTEventEmitter {
     playbackController.seek(to: time)
   }
 
+  @objc(setShuffleMode:)
+  func setShuffleMode(_ enabled: Bool) {
+    playbackController.setShuffleMode(enabled)
+  }
+
   // MARK: - Authorization
 
   @objc(authorization:)
@@ -231,7 +236,7 @@ final class MusicModule: RCTEventEmitter {
           types: types,
           options: searchOptions
         )
-        resolve(["songs": result.songs, "albums": result.albums])
+        resolve(["songs": result.songs, "albums": result.albums, "playlists": result.playlists])   // Convoy: playlists
       } catch {
         reject("ERROR", "Failed to perform catalog search: \(error)", error)
       }
diff --git a/node_modules/@lomray/react-native-apple-music/ios/PlaybackController.swift b/node_modules/@lomray/react-native-apple-music/ios/PlaybackController.swift
index bc8133e..40c55b4 100644
--- a/node_modules/@lomray/react-native-apple-music/ios/PlaybackController.swift
+++ b/node_modules/@lomray/react-native-apple-music/ios/PlaybackController.swift
@@ -95,6 +95,13 @@ final class PlaybackController {
     playbackTime = time
   }
 
+  /// Turns shuffle on (.songs) or off on the application music player (iOS 16+).
+  func setShuffleMode(_ enabled: Bool) {
+    if #available(iOS 16.0, *) {
+      player.state.shuffleMode = enabled ? .songs : .off
+    }
+  }
+
   // MARK: - Queue Management
 
   func setQueue<T: PlayableMusicItem>(_ item: T) async throws {
diff --git a/node_modules/@lomray/react-native-apple-music/modules/player.d.ts b/node_modules/@lomray/react-native-apple-music/modules/player.d.ts
index a432d94..46cae6f 100644
--- a/node_modules/@lomray/react-native-apple-music/modules/player.d.ts
+++ b/node_modules/@lomray/react-native-apple-music/modules/player.d.ts
@@ -57,6 +57,10 @@ declare class Player {
      * Toggles the playback state between play and pause.
      */
     static togglePlayerState(): void;
+    /**
+     * Turns shuffle on/off (iOS 16+).
+     */
+    static setShuffleMode(enabled: boolean): void;
     /**
      * Starts playback of the current song.
      */
diff --git a/node_modules/@lomray/react-native-apple-music/modules/player.js b/node_modules/@lomray/react-native-apple-music/modules/player.js
index 59aa6f5..16fd0b4 100644
--- a/node_modules/@lomray/react-native-apple-music/modules/player.js
+++ b/node_modules/@lomray/react-native-apple-music/modules/player.js
@@ -36,6 +36,12 @@ class Player {
     static togglePlayerState() {
         MusicModule.togglePlayerState();
     }
+    /**
+     * Turns shuffle on/off (iOS 16+).
+     */
+    static setShuffleMode(enabled) {
+        MusicModule.setShuffleMode(enabled);
+    }
     /**
      * Starts playback of the current song.
      */
