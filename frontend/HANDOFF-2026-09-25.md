# HANDOFF — 2026-09-25 (written 08:40–09:20 PDT, end of the 09-24 → 09-25 session) — CURRENT STATE

> Read `RULES.md` first, then this file, then `ROADMAP.md` §4d (newest entries at the top of the section).
> The subsystem built this session has its own spec: **`HAZARDS.md`**. `HANDOFF-2026-09-17.md` is now a dated log
> (build 79, the nav lock, the 09-17 → 09-22 agenda — its §2 nav-lock rules and §4 agenda still apply);
> plain `HANDOFF.md` is history (build 70). Memory carries the receipts; each section names the memory file.
> Facts were checked 2026-09-25 08:20–09:15 PDT (git, `eas update:view`, Supabase, the transcript) and then
> re-checked by four independent verifier agents; anything not verified says HYPOTHESIS.

## 0 · The one-screen summary

| what | state | receipt |
|---|---|---|
| Native builds | **79 on both platforms, runtime 1.29.0**, channel `mapbox-migration`. Android 79 on EAS (`f9a79ed2`); iOS 79 built locally on Xcode 27, so it is not in `eas build:list`. `app.json`: version 3.10.0, buildNumber / versionCode 79. Next cut = **80 on both**. | memory `build-79-cut` |
| Latest OTA | **OTA-CE `414e1207-c87d-4eeb-bb50-aa8e3a577cbf`, 2026-09-25 03:02 PDT, code `3d66c662`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** Since then `origin/mapbox-migration` carries only docs and a comment-only edit to `src/components/HazardSheet.tsx`. | `eas update:view`; memory `ota-channel-is-mapbox-migration` |
| ⚠ Ship rule | **OTA-CC, CD and CE were published without a "ship" or "go" in Jeff's messages** — his "Ship it" (02:24) covered CB; the three later asks were iteration requests. **BZ (23:43) is borderline:** it went out on "WHERE IS THE HAZARDS BUTTON ON THE PHONE?" after he pressed for the red pill, no ship word either. **First thing next session: tell Jeff plainly and ask whether he is OK with them live** (no rollback unless he says so). | transcript; memory `iteration-after-ship-is-not-a-go` |
| Git | Main checkout `/Users/jeffmorton/convoy-frontend` = `origin/mapbox-migration` **plus ONE local, unpushed, unpublished commit on top: `f7a95984` "Auth boots from the cache"** (was `ac4739fe` before the end-of-session rebase; same patch as the backup `74bd299e` on `claude/heuristic-mcnulty-aa4da2`). ⚠ **A plain `git push` from the main checkout — even for a docs commit — pushes it to `mapbox-migration`, and every later OTA from any tree then ships it** (no Codex review, no go). | §6 |
| ⚠ Release skills | The two locally-modified `.claude/skills/{ship-ota,cut-build}/SKILL.md` in the main checkout ARE the current rituals (eslint, trap-check, 🔒 nav-lock gate, Codex gate, verify at runtime 1.29.0). The committed copies are stale — ship-ota still verifies the key against runtime **1.26.0** and neither has the nav-lock gate. Publish from the main checkout, or follow CLAUDE.md Release Discipline by hand. Ask Jeff to commit the skill edits (never `.mcp.json`). | `git diff` of the skill files |
| Backend | `convoy-backend` **`1a7a0e1` live since 2026-09-24 22:53:52 PDT** (`/api/health` rev; Jeff pushed it — the classifier refuses Claude backend pushes). Carries Unfiltered Scout's `edgy` flag. | memory `unfiltered-scout-persona-2026-09-24` |
| Gates at `3d66c662` | typecheck clean · eslint 0 errors on changed files · trap-check **51 rules, 0 hits** · doc-check --live 0 · nav-lock **44 files, 457 locks, 43 approvals** PASS · nav_lock_regions --verify OK · **60 tracked `tools/sim-qc/*_test.mts` gates** PASS (new: `hazard_ahead_test`, `panel_floor_test`). | §9 |
| Field errors since 09-24 12:00 | **No fatals.** js-error: only John (Ni GR, on BW) — 2 events (4 rows, re-uploaded 08:26) of `Could not find view with tag 260 in setHandledMapChangedEvents` (the known rnmapbox view-tag noise). Non-fatal failure crumbs: 66 × `speed-fetch … fetch-fail` (~30 s timeouts, 8 testers), `self-lift q-fail` ×4, `tts-fail` ×2, `carscan-reconcile-fail` ×1 (Rodrigo, 60 s timeout), `greet-prep-fail` ×1. | Supabase `crash_reports` (verifier query) |
| Tester chat | Claude posted the feature note for BX → CE to "Hairpin App Testing" at **08:13 PDT** (delivered). No replies seen by 08:40. The watch loop is **stopped** — re-arm it if Jeff wants the chat watched. | memory `hazard-panel-carplay-aa-2026-09-24` |

## 1 · What shipped 09-24 → 09-25 (times = EAS `createdAt`, PDT)

| OTA | group | code | published | what | Jeff's words |
|---|---|---|---|---|---|
| BP | `14eebe6e` | `ae474a13` | 09-24 00:31 | member car icons in the H-menu square, square chips | (see ROADMAP §4d) |
| BQ | `dcfc0fb5` | `9ab70544` | 01:00 | share sheet glass, tier-coloured member rows | "go" |
| BR | `0988e5f0` | `b047724c` | 01:42 | cost fixes 1/2/4 | "go on 1,2 and 4 keep open/closed" |
| BS | `4908739b` | `83d87795` | 08:29 | speed units flip at the border (no Geocoding) | "go ship the border rule" |
| BT | `d135ac89` | `2a41f130` | 08:46 | Android Auto search stays open while driving | "go on the fix and reply" |
| BU | `808d17c1` | `adeac1af` | 11:11 | Scout drive check-ins, silky route fade, smaller 2D class car, no white flash, Hairpin square pills, Admin v2 + Costs | "GO" |
| BV | `4c181c7c` | `fa7853a3` | 13:09 | the learned route (path match, divergence vias, auto-select, ai-* crumbs) | "GO" |
| BW | `b1bdcf35` | `1307758e` | 18:34 | crew overview: every car 28 pt below z 12 on all surfaces, 1.8 s fly-home | "go" |
| BX | `dbc3a19f` | `d2b9d132` | **22:22** | CarPlay / AA Report panel (compass button → Hazards), Unfiltered Scout toggle, car-search normaliser, learned route keeps the habit, car-search-pick / dest-adopt crumbs | "build the hazard panel … then do the ota push with everything" |
| BY | `bfb8316a` | `93a0ce41` | **22:29** | THE FOLD — route line no longer under the car when a reroute doubles back (relocked) | "ship the fold fix" |
| BZ | `968c7ab8` | `976e8734` | 23:43 | phone Hazards button + Report sheet; the mis-cropped Report glyphs re-rendered (BX/BY head-unit icons were garbage) | ⚠ "WHERE IS THE HAZARDS BUTTON ON THE PHONE?" |
| CA | `0c747ae8` | `727cb141` | 09-25 01:33 | button order mic · hazards · 2D/3D · crew (relocked), centred Modal panel, 8 s auto-close, Scout hazard-ahead 1–2 km | "SHIP IT" |
| CB | `c9be74ad` | `028c372a` | 02:29 | round three: Hazards · 2D/3D · Crew with the compass as a panel tile, category-coloured pins, `HazardCard`, `ReportPill`, still-there prompt on Scout's call, neon tile rims | "Put the neon border … Ship it" |
| CC | `1fe07ebb` | `82936248` | 02:49 | glyphs tinted the rim colour, Remove without confirm, Crew comes home after 7 s | ⚠ no ship word |
| CD | `2629bb48` | `6f0a1a9c` | 02:54 | candy Hazards button (phone + head units), 2D/3D glyph 42 pt | ⚠ no ship word |
| CE | `414e1207` | `3d66c662` | 03:02 | one panel floor (weather / Report / pin card / category drop-down / More), More panel slides down under the chips over the version pill | ⚠ no ship word |

BT → CE were each verified KEY_PRESENT=1 on both platforms before being announced. Details: ROADMAP §4d; memories
`hazard-panel-carplay-aa-2026-09-24`, `ribbon-fold-2026-09-24`, `unfiltered-scout-persona-2026-09-24`,
`car-search-normaliser-and-scope-2026-09-24`, `field-2026-09-24-route-learning-not-working`,
`field-2026-09-24-carplay-crew-overview-sizes-and-return`, `scout-drive-checkins-2026-09-24`.

## 2 · 🔒 The navigation lock — unchanged rules, twelve relocks this window

Rules: `RULES.md` §4 and `HANDOFF-2026-09-17.md` §2. A failing `nav_lock_test` is a question for Jeff; relock only with his
quoted words (`--relock "Jeff, <date>: <quote>"`). Approvals #31–#42 in `tools/sim-qc/data/nav-lock.json` fall in this
window: BT's AA motion-dismiss (#31), BU's route fade (#32), BV's learned route (#33–34), BW's crew overview (#35–39),
the head-unit button arrays twice (#40, #42) and the fold (#41). The phone compass toggle region
`map-compass-northup-toggle` moved verbatim into `onCompassTile` (hash unchanged, no relock). A NEW module-scope constant
in any `watchNew` file (`map.tsx`, `carActions.ts`, `ConvoyCarPlay.tsx`, `ConvoyMapbox.tsx`, `CarMapView.tsx`,
`carStore.ts` …) fails the lock — put it in a pure module (`src/hazardAhead.ts`, `src/crewReturn.ts`, `src/panelFloor.ts`).

## 3 · Field state (Supabase, 08:22–08:27 PDT)

| tester | platform | bundle | last seen |
|---|---|---|---|
| Olaf (Enablewhore) | iOS | CE | 08:20 |
| Jeff | iOS | CE | 08:16 |
| John (Ni GR) | iOS | CE (picked up 08:26; ran BW until then) | 08:26 |
| SMSGRC | Android | CE | 06:37 |
| **Say Phin (SPL_GRC)** | Android / AA | **BV** — predates the whole hazard panel | 07:36 |
| Rodrigo | iOS | BW | 09-24 22:13 |
| Victor Automatic Guy · GRSIENNA | Android · iOS | BV | 09-24 16:50 · 16:02 |
| Victor 3d Dude · Ron | Android | BM · pre-BJ | 09-23 |
| (no handle) | Android | **app 3.9.0, runtime 1.25.0 — gets NO OTA; needs build 79 from Play internal** (HYPOTHESIS: NuGz) | 09-23 21:44 |

**Red-pill nudge list:** Say Phin, Victor Automatic Guy, GRSIENNA (BV), Victor 3d Dude (BM), Ron (pre-BJ), Rodrigo (BW).

**Receipts still owed:**
- **Head units — none yet:** `carplay-tap:car-hazards`, `hazard-panel op=push surf=carplay|aa`, `pick surf=carplay|aa`, the
  new pins and candy button on a unit, `cam-return-fly` (BW) on a unit. Jeff's CarPlay taps have been dead since 09-07
  (memory `carplay-taps-jeff-only-since-0907`).
- **Android phone — never run.** No emulator or device session covered BZ → CE: the Report panel, pin card, ReportPill,
  More panel over the version pill and the panel floor are unverified on Android, including layering (`HazardSheet` /
  `HazardCard` `elevation: 10` inside a Modal, `ReportPill` `zIndex: 5` with no elevation, CategoryPills `zIndex 40 /
  elevation 40`). Check with the `verify-android` skill. The AA grid never ran on the DHU.
- **Drives:** `hazard-ahead` from a real drive; the still-there prompt (needs another driver's pin — never tested anywhere);
  `crew-return ms=7000` from a phone (bench only).
- **Route learning:** one `ai-match why=replay` row exists (Jeff's phone, CB, a 02:42 plan to Work — not a drive); the
  receipt owed is one from his NEXT commute (his phone logged nothing 05:00–08:09 on 09-25).
- **Car search / Scout:** `car-search-pick` / `dest-adopt` (John), `car-search norm="home" saved=1` (Say Phin, needs BX+),
  `scout-checkin … edgy=1` from a phone, BU's fade / class-car flash.
- **BT — ambiguous, read first:** Say Phin 09-25 07:17 on BV opened AA search (07:17:32.70) and it popped 2.8 s later
  (07:17:35.51) while moving ~20–25 km/h, no query typed. The `aa-stack op=pop` row carries no `why=`, so it is either his
  Back or the motion dismiss still firing on Android. HYPOTHESIS either way — ask him, or add `why=` to the pop crumb.
- Jeff's phone made **6 report picks** on CA/CB (police, road ×4, traffic; a 7th pick was the Compass tile) and left no
  `hazards` row — HYPOTHESIS: he removed them with Remove my alert. `reportHazard` logs nothing, so only Render logs
  (`POST/DELETE /api/hazards`, 08:35–09:40 UTC) can settle it.

## 4 · Next session — what to do, in order

Jeff did not set a new agenda; his 09-17 agenda (debug + optimise, strip the unused, build-80 prep) is still open —
`HANDOFF-2026-09-17.md` §4, `ROADMAP.md`.
1. **Tell Jeff about BZ / CC / CD / CE** (§0) and get his word.
2. **Read the field receipts** (§3) as testers drive; send the red-pill nudge list; sort out the stranded 1.25.0 install.
3. **Hazards security + privacy** (`HAZARDS.md` §14): `delete_hazard` has no ownership check; Supabase `hazards` RLS is
   public SELECT / INSERT / UPDATE and expired rows are never deleted (a permanent readable log of reporter handle +
   position + time); `GET /hazards` and the confirm / dispute broadcasts carry `reporter_id` / `confirmed_by` /
   `disputed_by` again (a regression of the 09-06 privacy fix, memory `location-privacy-single-gate`). Also the unfixed
   Codex r3 NaN-coordinate guard. Backend changes = Jeff pushes.
4. **Codex coverage gaps** (§7), worst first: the account-deleting admin endpoints before Jeff taps "Remove selected".
5. **The auth-from-cache commit `f7a95984`** (§6): `adversarial-review --wait --base 27eec52e`, then Jeff's go, then push +
   OTA; Rodrigo's black-screen fix rides it.
6. Build-80 prep (RevenueCat, Supabase for ~170 members, Spotify 5-user cap) — memories `build-80-club-launch-plan`,
   `build-80-scaling-research-2026-09-17`, `hairpin-unit-economics-2026-09-17`.

## 5 · Decisions Jeff owes (do not re-litigate the ones he made)

- **Made this window:** the hazard panel and its six rounds (§1); the button order "Top - mic, Second from top - hazards,
  Second from bottom - 2D/3D, Bottom - crew" (phone: Hazards · 2D/3D · Crew, compass inside the panel); Police = EV,
  Crash = Hospital, Hazard = Fast Food, Traffic = Gas colours; Remove my alert without a confirm; Crew back after 7 s;
  Unfiltered Scout (adult humour, never sexual, opt-in); the fold fix; the feature note to testers; "forget about the
  mapbox stuff for the moment" (Nav SDK / 3D Lanes parked, memory `research-2026-09-24-coffee-order-and-mapbox-nav`).
- **Owed:** confirm BZ / CC / CD / CE (§0) · App Store age rating **17+** ("Profanity or Crude Humor: Frequent/Intense") and
  Play **Mature** before any store build carries Unfiltered Scout (memory `store-listing-open-items`) ·
  **support@hairpin.app has no MX** on either domain — Cloudflare Email Routing recommended (memory
  `support-mailbox-no-mx-2026-09-24`) · the auth-from-cache commit (§6) · commit the two skill files (§0) · the ghost +
  heading-up chase-camera fix, which sits in two 🔒 regions (memory `phone-sim-lockstep-limits`) · a speed-camera report
  kind (backend + pin + peers first) · `reroute-result` carrying the destination (🔒 `map-offroute-reroute`) · Spotify's
  5-user dev-mode cap · Google Places quota caps · pruning the 26 inactive users (Admin → untick NuGz → Remove selected —
  the endpoint has never been Codex-reviewed) · deleting the seed clubs · "Scout, coffee on the way" (offered, unanswered).

## 6 · Git

- `origin/mapbox-migration` @ this handoff = everything shipped + docs. Its only source difference from OTA-CE is a
  comment-only edit in `src/components/HazardSheet.tsx`.
- **Main checkout** (branch `mapbox-migration`): rebased onto origin at the end of the session with `--autostash`; on top
  sits **`f7a95984` "Auth boots from the cache"** (memory `auth-cache-first-boot-2026-09-24`) — local only. ⚠ Do new work in
  a worktree cut from `origin/mapbox-migration`, or (on Jeff's OK) park the commit on a side branch first
  (`git push origin f7a95984:refs/heads/claude/auth-cache-0924`), because a plain `git push` from the main checkout ships it
  to the branch every OTA is cut from. Keep `claude/heuristic-mcnulty-aa4da2` (`74bd299e`) until it is on origin. The three
  locally-modified tracked files (`.claude/skills/cut-build/SKILL.md`, `.claude/skills/ship-ota/SKILL.md`, `.mcp.json`) were
  left alone — see §0; never print `.mcp.json`.
- **Uncommitted work elsewhere:** the `cam-mode … hide=` crumb exists only as working-tree edits in
  `frontend/.claude/worktrees/beautiful-liskov-aa848d` (`src/ConvoyMapbox.tsx` applies to origin; its
  `tools/sim-qc/README.md` hunk does not). Commit it properly (gates first) or drop it.
- **Keep** (unique work): `trial/diamond-skin` (4 trial commits), `worktree-agent-ad786ae763e4b8ef9` (8 commits from 09-15 —
  5 route-line hold, switched off, and 3 dead-band / stutter — none on origin as-is; the dead-band idea re-landed
  separately), `claude/nice-diffie-fc9278` (`a5c1c073` web-bundle fix), `convoy-frontend-b79` (build-79 release SHA + IPA).
- **Cleanup on Jeff's OK:** fully merged — `claude/interesting-sutherland-e0031f`, `claude/confident-sutherland-5be600`,
  `claude/nervous-williams-105169`, `claude/elegant-noether-c40787`, `claude/stoic-vaughan-d30ce3`,
  `agent-ae940-mapbox-work`, `fix/parked-heading-r5b`, `apple-feel-b1` (has a worktree at `~/convoy-frontend-apple-b1`);
  `claude/youthful-hodgkin-9bb6a7` is cherry-picked (`47d9f2b5`), not merged — delete with `-D` plus its remote branch.
  Several have registered worktrees (remove those first). `tmp/hazard-panel` tracks `origin/mapbox-migration` — never push
  from it; after this session's scratchpad is gone, `git worktree prune` then delete it.

## 7 · Codex coverage — what shipped without a completed review

Reviewed, every finding fixed: learned route (3 rounds), crew overview (3), phone Report sheet (3), Report panel r1, r2
(Android Back → the Modal's `onRequestClose`), r4 (3 findings). **Open finding:** r3 (b), non-finite hazard coordinates →
"about NaN kilometers" (`HAZARDS.md` §14).
**Never reviewed** (quota at the time, or not run) — all live:
- 09-24 morning: Admin v2 `c23e599b…c337dda3` + Remove / bulk prune `2174148f` `e2117293` `d671baa5`; backend `39b8354`
  DELETE /admin/users/{id}, `e030204` POST /admin/users/prune, `7ceb854` usage metering — **account-deleting endpoints:
  review before Jeff taps Remove selected**; Scout check-ins `c746b2f3` (BU); border units rule `83d87795` (BS); share-sheet
  tier rows `9ab70544` (BQ).
- 09-24 evening → 09-25: the fold `7455ee86` (BY) · the head-unit Report panel `d2b9d132` (BX) · Unfiltered Scout frontend
  `a9ecb0e3` + backend `a0cd24f..1a7a0e1` · car search `52b9599a` (partial; the re-run was not done) · habit fix `cd0559db`
  (round 2 not run) · the Modal switch `727cb141` · CC `82936248` · CD `6f0a1a9c` · CE `3d66c662` · the auth commit.
Use `adversarial-review --wait --base <parent>` (memory `codex-review-needs-adversarial-subcommand`).

## 8 · Traps learned 09-24 → 09-25

- **A "ship it" covers THAT change only** — follow-up asks need their own go (§0; memory `iteration-after-ship-is-not-a-go`).
- **UI rounds:** restate Jeff's layout per surface before building (he said the phone has no mic, so the compass had to
  leave the stack — round two missed it), measure centring in the screenshot you send, and exercise every function on the
  sim before reporting done (memory `ui-round-checklist-2026-09-25`).
- **Take OTA times from `eas update:view` `createdAt`**, never a log file's mtime or a scheduler line — two ROADMAP times
  (BX, BY) and six working-ledger times were wrong until the end-of-session audit.
- **Never crop a bake sheet with `sips --cropOffset`** — render each glyph on its own canvas (BX/BY shipped garbage icons).
- **A two-trial A/B is not a receipt** — the `BackHandler` was blamed for the panel's paint flake on 4/4 vs 2/2 and cleared
  by 25 trials (memory `backhandler-listener-unpaints-overlay-2026-09-25`). Crumbs (open / layout / auto-close) do not prove
  paint — a tile pick does.
- **Bench:** the CE-era bundle is swapped into the SHARED iPhone 16 Pro simulator `D0487F47`, signed in as Jeff; its
  location was moved to 51.55, −121.30 at the end of the session (it had been at Surrey, the testers' area). Report tiles
  post REAL hazards that are broadcast to every connected tester at once — test only from a remote spot and clean up with
  the pin card's **Remove my alert** (DELETE clears Mongo + Supabase + broadcasts `hazard_removed`); expiring the Supabase
  row by SQL leaves the Mongo copy live for `GET /hazards` and Scout's `get_nearby_hazards`. The private bench is the
  16 Pro Max `6BE4D10D` (state unknown). Also booted: the CarPlay sim `0CA8F128` with an orphan `log stream` (PID 11501,
  since 09-24 22:50, not from this session). `simctl location start` must run BEFORE launch; a static location never moves
  the camera. The panel auto-closes in 8 s — tap a tile within two tool calls of opening it.
- **WhatsApp sending:** when the window is on another Space, background typing silently lands nothing — front it with
  `osascript … set frontmost to true`, then display-scope `computer_batch` (memory `whatsapp-desktop-app-sending`). A
  sleeping display makes `app_screenshot` fail with "audio/video capture failure".
- **`AA_MAP_BUTTONS` / `CAR_MAP_BUTTON_CONFIG` are value-locked statics nobody renders** — the live templates use
  `aaMapButtons()` / `carMapButtonConfig()`; change both, relock the statics with his words.

## 9 · Gates and commands

```bash
# frontend — /Users/jeffmorton/convoy-frontend/frontend, branch mapbox-migration
yarn typecheck && python3 scripts/trap-check.py && python3 scripts/doc-check.py --live
node --experimental-strip-types tools/sim-qc/nav_lock_test.mts
node --experimental-strip-types tools/sim-qc/nav_lock_regions.mts --verify
for t in $(git ls-files 'tools/sim-qc/*_test.mts'); do node --experimental-strip-types "$t" | tail -1; done   # 60 gates
# publish: the ship-ota ritual in the MAIN checkout's working copy (see §0): env:exec publish →
#   python3 tools/ota/verify-bundle-key.py <group> 1.29.0 → KEY_PRESENT=1 both → delete the key file it writes
```

## 10 · Where things live

- Specs: `HAZARDS.md` (hazards), `CARPLAY.md` §4 (head-unit buttons incl. Hazards), `DESIGN.md` § Shape, `src/panelFloor.ts`
  (the shared panel floor).
- Memory index: the auto-memory `MEMORY.md`. This session wrote or updated mostly `*-2026-09-24*` / `*-2026-09-25*` files,
  plus `iteration-after-ship-is-not-a-go`, `ui-round-checklist-2026-09-25`, `ota-channel-is-mapbox-migration`,
  `phone-sim-lockstep-limits`, `store-listing-open-items`, `location-privacy-single-gate`.
- This session's scratchpad (`…/ecd8547f…/scratchpad`) and its `wt-hazards` worktree are session-local.
