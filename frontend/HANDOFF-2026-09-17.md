# HANDOFF — 2026-09-17 (written 01:xx PDT after the build-79 cut; finished 09-17 morning) — CURRENT STATE

> Read `RULES.md` first. Then this file. Then `ROADMAP.md` §4c. The dated fix logs are `HANDOFF-2026-09-07.md` (09-05 → 09-11),
> `HANDOFF-2026-09-06.md` and `HANDOFF-2026-09-02.md`. `HANDOFF.md` is stale history (build 70).
> Memory (the auto-memory directory) carries the receipts; every section below names the memory file that has them.

## 0 · The one-screen summary

| what | state (VERIFIED 2026-09-17 01:0x PDT) | receipt |
|---|---|---|
| Native builds | **BUILD 79 on BOTH, runtime 1.29.0**, channel `mapbox-migration`, release SHA `f9a79ed2`. iOS built **locally on Xcode 27 RC 27A266a** (not in `eas build:list`), verified by `tools/build/verify-ipa.py` (XL widget present, carplay-maps, channel, KEY_PRESENT=1), submitted to ASC 23:58 (submission `0457e036`), Apple processed it (Olaf saw it at 00:25). Android EAS `fb6a73de` → Play INTERNAL (submission `efd3672e`), no APK. | memory `build-79-cut`, ROADMAP §4c |
| Latest OTA | ⮕ **superseded by `HANDOFF-2026-09-25.md`** · **OTA-CE PUBLISHED 2026-09-25 03:02 PDT — group `414e1207-c87d-4eeb-bb50-aa8e3a577cbf`, code `3d66c662`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android. Contents over CD: one panel floor (`src/panelFloor.ts`) for the weather card, Report panel, pin card, category drop-down and More panel; the More panel slides down under the chips, layered over the version pill. Before it: OTA-CD PUBLISHED 2026-09-25 02:55 PDT — group `2629bb48-1187-4ce0-8a9e-b7dc8a112197`, code `6f0a1a9c`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android. Contents over CC: the Hazards button in the candy finish on the phone and the head units, 2D/3D glyph 42 pt. Before it: OTA-CC PUBLISHED 2026-09-25 02:49 PDT — group `1fe07ebb-071a-425d-a319-1568a9bdbd84`, code `82936248`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android. Contents over CB: Report tile glyphs in their neon rim colour, Remove my alert without the confirm popup, the Crew press comes home after 7 s (`src/crewReturn.ts`). Before it: OTA-CB PUBLISHED 2026-09-25 02:29 PDT (Jeff: "Ship it") — group `c9be74ad-7384-4857-9b81-51e4a413cb36`, code `028c372a`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android. Contents over CA: phone stack Hazards · 2D/3D (always) · Crew with the compass as a panel tile; category-coloured hazard pins with the Apple glyphs on every surface (Police = EV, Crash = Hospital, Hazard = Fast Food, Traffic = Gas); neon-rimmed Report tiles; the tapped-pin card / pass-by prompt as the panel's twin with a big Remove; the report pill under the crew pill; the still-there prompt on Scout's call; Codex r4 ×3. Before it: OTA-CA PUBLISHED 2026-09-25 01:33 PDT (Jeff: "SHIP IT") — group `0c747ae8-d98e-47c5-8c67-7004dae4b4df`, code `727cb141`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android. Contents over BZ: buttons mic · hazards · 2D/3D · crew on CarPlay (AA map strip hazards above crew; phone compass · Hazards · 2D/3D · Crew), the phone Report panel as a centred transparent-Modal card that closes itself after 8 s (both head-unit grids too), Scout's hazard-ahead call at 45 s of travel (1–2 km) in a forward cone naming the kind and distance, the FAB triangle 30 pt, corrected head-unit Report icons. ⚠ bench paint flake NOT root-caused (11/25 sim launches painted the panel; `hazard-panel layout` receipt shipped) — Jeff's phone decides. Before it: OTA-BZ PUBLISHED 2026-09-24 23:43 PDT — group `968c7ab8-dd4f-4368-ac06-d8e679c44705`, code `976e8734`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android. Contents over BY: the PHONE Hazards button (below Crew, above the compass) + Report sheet (Police/Crash/Hazard/Traffic in the tier metal; Codex ×3), and the 24 Report glyph PNGs re-rendered per glyph with real mask cut-outs — the sips crops were wrong, so BX/BY's head-unit Report icons were garbage (never seen on a head unit) and `carButtonIcons.ts` is re-baked. Before it: OTA-BY PUBLISHED 2026-09-24 22:29 PDT (Jeff: "ship the fold fix") — group `bfb8316a-e240-456c-b783-38cb4cfe5b65`, code `93a0ce41`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android. Contents over BX: `7455ee86` THE FOLD — on a reroute that doubles back past its own corner the cut anchor and the phone projection latched on the outbound corner (John's 18:57 'car under the route line'); `ribbonAnchor.ts` ANCHOR_RECHECK_M 20 + `src/routeFold.ts` escape inside 🔒 mbx-route-project-a, relocked `93a0ce41` (42 approvals); gate `ribbon_fold_test`; sim A/B receipt (memory `ribbon-fold-2026-09-24`). Codex owed on both the panel and the fold (quota). Before: OTA-BX PUBLISHED 2026-09-24 22:22 PDT (Claude ran it on Jeff's "build the hazard panel with the apple glyphs i mentioned. then do the ota push with everything") — group `dbc3a19f-b1c8-4a42-be2f-077348fef82e`, code `d2b9d132`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android. Contents: `d2b9d132` the Report panel (CarPlay + AA compass button → Hazards grid Police/Crash/Hazard/Traffic/Compass in the tier metal, Apple-symbol glyphs; 2 button constants relocked on his words) · `a9ecb0e3` Unfiltered Scout toggle (backend half `1a7a0e1` is LOCAL in convoy-backend — the push was refused by the auto-mode classifier as a production deploy, Jeff runs `git push origin master`) · `52b9599a` car search normaliser + saved place first · `cd0559db` learned route keeps the habit · `47d9f2b5` John's car-search-pick/dest-adopt crumbs · `91ed311f` glyph PNGs. NOT in it: the fold fix `2cd2521a` (claude/ribbon-fold-0924, 🔒×2, needs Jeff's words). Codex owed on the panel (quota). Head-unit receipt owed: `carplay-tap:car-hazards` + `hazard-panel op=push` + a `<Kind> reported ✓` pill. Before: OTA-BW PUBLISHED 2026-09-24 (Claude ran it on Jeff's "go") — group `b1bdcf35-9435-44a0-9240-f61bee733d8d`, code `1307758e`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android (control 4c181c7c also 1). Contents: `1755e1c7` + `694e45de` + `9d707052` + `c423003e` the crew overview (every car 28 pt below z 12 on all surfaces, Olaf's scanned twin on the live zoom, a 1.8 s fly-home instead of the cut; Codex ×3; relocked, 40 approvals; ROADMAP §4d). FIELD RECEIPT OWED: his next Crew tap on the head unit → `cam-return-fly surf=car` and the 28 pt cars.** Before it: **OTA-BV PUBLISHED 2026-09-24 (Claude ran it on Jeff's "GO") — group `4c181c7c-841f-44ed-b8ba-f247a9144034`, code `fa7853a3`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android (control 808d17c1 also 1). Contents: `e488e93b` + `26a70088` + `8b1d0309` + `296e7ca0` the learned route (Jeff: "EVERYDAY I MERGE OFF THE HIGHWAY … IT ALWAYS IS TAKING ME THE FASTEST WAY NOT MY WAY. LETS FIX IT") — match along the remembered road, via points on the divergence, replay vetted, auto-selected, `ai-learn`/`ai-match`/`ai-select` crumbs; Codex ×3; sim receipt `ai-match why=replay idx=13/328 via=8` → `ai-select`; relocked (35 approvals); ROADMAP §4d. FIELD RECEIPT OWED: tonight's arrival `ai-learn why=learn` + `ai-learned`; tomorrow's plot `ai-match why=replay` + `ai-select`, the line leaving the highway at his exit.** Before it: **OTA-BU PUBLISHED 2026-09-24 (Claude ran it on Jeff's "GO") — group `808d17c1-056d-4b1f-b84e-d9fafa6cc5cc`, code `adeac1af`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android (control d135ac89 also 1). Contents: `c23e599b` + `d79b89d6` + `bce5f5d2` + `c337dda3` Admin panel v2 (Users/Clubs/Costs; chip rail wraps; Costs lists every service app-vs-tooling incl. Spotify's 5-user dev-mode cap; sim receipts on all three tabs; backend `7ceb854` metering + endpoints and `8146d4f`/`a0cd24f` full plan table PUSHED, Render deploy verified, first `usage_daily` row seen) + `8a81e68c` Hairpin-square sweep (37 radii, DESIGN.md § Shape) + `2174148f` + bulk "Remove selected" admin panel (backend `39b8354` DELETE + `e030204` prune await Jeff's push — Jeff: "keep the 9 and nugz delete the rest", NuGz to be unticked) + `606381ce` three drive fixes from Jeff's 09-24 CarPlay drive (16-piece smoothstep route fade, 2D class car × 0.9, no white flash re-selecting the class car — snapshot lives across markers; Codex withdrew the early-show half) and `c746b2f3` Scout drive check-ins (90-min moving-time clock → one spoken line: fatigue / stretch / water / joke / crew / destination; toggle "Drive check-ins" default ON; gate `scout_checkin_test.mts`; sim receipt: two check-ins on the minute at the 1-min QA interval, TTS played). ROADMAP §4d has the four entries. Sim receipts on every tab; the drive fixes' field receipt (a smooth fade and no white frame on a tier switch) is owed by Jeff's next CarPlay drive.** Before it: **OTA-BT PUBLISHED 2026-09-24 (Claude ran it on Jeff's "go on the fix and reply") — group `d135ac89-929c-4f87-8cc9-84ba9a576fa7`, code `2a41f130`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** Android Auto search stays open while driving: the 🔒 `act-search-motion-dismiss` rule (speed > 2.5 m/s for 2 ticks → pop, written for the 8/19 CarPlay stuck keyboard) returns early on Android. Say Phin's 09-24 report, VERIFIED in crash_reports: three `carplay-tap:car-search` → `aa-stack op=push id=search` → `aa-stack op=pop had=1` at 14:25 UTC with gaps 1.28 / 0.25 / 2.09 s at 41–60 km/h — that pop crumb is aaPop() ← dismissCarSearch() ← this rule and nothing else. The AA host enforces its own driving restrictions (keyboard off, voice prompt + mic), so the screen was usable and we yanked it. Codex read-only review: approve, no findings. Relocked under Jeff's quote (32 approvals). Device receipt owed by his next drive (a Search that stays up > 2 s in motion, pops only from Back/select). Same session also proved the build-79 AA black-screen fix in the field (car-started session drew). Memory `field-2026-09-24-sayphin-aa-search-motion-pop`. Before it: **OTA-BS PUBLISHED 2026-09-24 (Claude ran it on Jeff's "go ship the border rule") — group `4908739b-a26c-4a04-9b78-e548c05f39b2`, code `83d87795`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** Speed units flip at the border from the car's OWN position with no network: `src/borderUnits.ts` (49th parallel from Point Roberts −123.1° to Lake of the Woods −95.2° with a ~220 m dead band for 0 Avenue; Juan de Fuca split at 48.30° west of that; only unambiguous latitudes east of Manitoba; Mexican zone keeps), gated by `tools/sim-qc/border_units_test.mts` (38 real-coordinate cases). It replaced the Google Geocoding call in `map-speed-unit-border-detect`, which had NEVER been enabled on convoy-497805 (REQUEST_DENIED, verified live) — nobody had ever been auto-switched. Relocked under Jeff's quote (31 approvals). Sim receipts: launch at Abbotsford "0 km/h · 10°C", relaunch at Sumas WA "0 mph · 50°F", back "0 km/h". ⚠ Sim lesson: a mid-run `simctl location set`/`start` never reached the app — pin BEFORE `simctl launch`. Google Geocoding stays disabled; nothing needs it. Before it: **OTA-BR PUBLISHED 2026-09-24 (Claude ran it on Jeff's "go on 1,2 and 4 keep open/closed") — group `0988e5f0-0cfa-443e-b830-7e824f2c8e4a`, code `b047724c`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** Three of the four 09-17 unit-economics cost fixes: (1) `DEFAULT_SETTINGS.novaMidDrive` true → false — the 60 s faster-route re-fetch is off for new installs (existing ones were already OFF via `novaQuietMigrated`); (2) the units-country Geocoding check re-runs at ≥ 50 km from the last check OR ≥ 20 min + ≥ 2 km — worst case 3 calls an hour instead of 30 (the 20-min rule is Codex's catch: 50 km alone is a straight line, a border crossing followed by a loop near the check point never re-checked); (4) Places (New) search: one `sessionToken` per search + an Essentials-only Details mask (`location,formattedAddress`, $5/1000, 10k free) with the label from the prediction's `structuredFormat.mainText` — `displayName` was Pro ($17/1000) and Google bills a token session that ends in a Pro Details as Enterprise + Atmosphere ($25/1000) regardless of fields, so the research's "tokens save $17" was wrong and tokens alone would have cost MORE (Codex). Fix 3 (arrival field mask) DECLINED — open/closed stays. Two relocks under Jeff's quote (30 approvals). Receipts: curl 200 on both endpoints with the token + mainText present; sim "Tim Hortons" → 5 suggestions → pick → 410 m route. NOT yet measured: the saving itself — GCP Billing → Reports by Places SKU a week out is the one check. **⚠ CORRECTION 09-24 (receipts pass): the Geocoding API is NOT ENABLED on convoy-497805** — live `geocode/json` with the app key returns `REQUEST_DENIED: This API is not activated on your API project`, and the library page shows "Enable". The units-country auto-detect has therefore never returned a country (`unitForCountry(undefined)` → km/h for everyone, wherever they drive), the research's "~$80/mo Geocoding" was a call that never succeeded, and fix 2's gate is moot until the API is enabled — Jeff's call: enable it (~$25 USD/mo at 170 with the new gate) or derive the unit from the OSM `maxspeed` unit the speed-limit hook already fetches (free, but `speedLimit.ts` is hash-locked). GCP budget "Hairpin Maps monthly" ($100/mo, 50/90/100 %) CREATED 09-24; per-API quota caps refused by the auto-mode classifier — Jeff types them (values in memory `hairpin-unit-economics-2026-09-17`). Before it: **OTA-BQ PUBLISHED 2026-09-24 (Claude ran it on Jeff's "go") — group `dcfc0fb5-9583-4016-bad9-b4d2d7f47646`, code `9ab70544`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** The share sheet wears the category-results panel's glass; each member row's car-square border and name take the member's tier colour (`appSkin.memberSkin`) — backend `convoy-backend b9e06b9` serialises `tier` on every roster/peer/attendee/search/admin/pending row, deployed 00:55 PDT (Jeff ran the push; `/api/health` rev). Sim-verified against the live server. Before it: **OTA-BP PUBLISHED 2026-09-24 (Claude ran it on Jeff's "go") — group `14eebe6e-4238-43fd-a7c8-968348745752`, code `ae474a13`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** Every member icon is the member's actual car (2D/3D arrow in their paint, class car in class + paint, 3D class car, or scan) in the H menu button's rounded square, app-wide (`src/components/MemberCarIcon.tsx`; presence gained `arrPick`; write path `garageCars.syncAppearanceToProfile` with a store-backed retry); category chips are the same square, results list styled, a second tap on the active chip opens the list, Garage 2D class car a touch above the arrow — ROADMAP §4d; memory `member-car-icons-2026-09-24`. Backend half `convoy-backend 60e30d9` (profile `appearance`, 104/104 tests) DEPLOYED 2026-09-24 00:35 PDT — Jeff ran the push (the classifier blocked Claude's), `/api/health` rev `60e30d9`. Live-peer icon paths and the CarPlay cold payload not yet seen on a device. Before it: **OTA-BO PUBLISHED 2026-09-23 (Claude ran it on Jeff's "go") — group `05e5f277-85cc-4891-8a0f-d8666b2e876d`, code `a4038493`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** The category chips under the search bar sit 10 pt apart, edge to edge — the map's FAB-stack gap between the Crew button and the compass (Jeff: "make the icons closer together… same space as the crew/compass"). Before it: **OTA-BN PUBLISHED 2026-09-23 (Claude ran it on Jeff's "…chips good....go") — group `10a1d52a-90b8-4914-9697-52f2af88e3f8`, code `85c877a5`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** Category search pins redesigned off Jeff's Mapbox screenshot (solid deep-colour teardrop + live number, no glyph on the pin; round category-coloured chips, results badges, gas price + cheapest in green; Food coral-pink; bake in `tools/poi-pins/`), the tier 2D lock + class car in nav + the 3D tease (`0b176b39`…`ff335849`), and the scan guide/consent padded past the absolute tab bar so Start Capture is reachable on Android (Victor) — ROADMAP §4d; memory `poi-category-pins-2026-09-23`. Head-unit render of the new pins NOT yet seen. Before it: **OTA-BM PUBLISHED 2026-09-23 (Claude ran it on Jeff's "GO") — group `dfff4cd8-32fa-484f-bcd1-a720ee53866c`, code `1af723ff`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** Route Color: spectrum removed, 20 measured colours, traffic-coloured presets removed — ROADMAP §4d. Before it: **OTA-BL PUBLISHED 2026-09-23 (Claude ran it on Jeff's "GO FOR BOTH") — group `e3a0b8a1-f1a5-4607-8408-b8b97a74a034`, code `51194a6b`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android; backend convoy-backend `2e11f70` deployed first (live 16:08 PDT).** Message the crew + change/cancel pushes, Edit meet fixed, club admins restored, local times in event pushes — ROADMAP §4d. Before it: **OTA-BK PUBLISHED 2026-09-23 (Claude ran it on Jeff's "GO") — group `936eb956-f8a1-484f-ab97-ad09d587db6f`, code `f952f855`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** The Diamond weather bubble at the destination (was the green flag) + the Crew pill green while a member is online — ROADMAP §4d. Before it: **OTA-BJ PUBLISHED 2026-09-23 (Claude ran it on Jeff's "go") — group `0df234e8-02fc-40fe-ba9c-38cb5da4dc5b`, code `c621575e`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** Diamond unlocked by the 1st 3D scan + the unlock wave — ROADMAP §4d. Before it: **OTA-BI PUBLISHED 2026-09-23 (Claude ran it on Jeff's "PLEASE DO IT" from work) — group `6be8e905-76cb-4840-89f9-fc81f57be5d5`, code `fe665daa`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** Contents: Apple feel batch 1 + Garage round 3 + the keyed Ultra still + the class car kept off the profile — ROADMAP §4d. Android not runtime-verified (first Reanimated import). Before it: **OTA-BH PUBLISHED 2026-09-23 (Jeff ran it) — group `4a0de4dd-89af-442b-80ce-c5011ff0289f`, code `de777c25`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android.** Contents: the menu reorganization (`6c3755a0`: H menu On the road + Places, Settings in 6 sections, Hub → Club), the Garage in rung order (`ef8dca4d`), nav held at 60 fps on all four surfaces (`39cdbc34` + `de777c25`, Codex-reviewed; Android proof = heat-probe `cam=` ≤ ~3600/min). Before it: **OTA-BG PUBLISHED 2026-09-22 (Jeff ran it) — group `c8cbbd88-78a5-403f-9661-8ffe04295b45`, code `93e985f8`, runtime 1.29.0, KEY_PRESENT=1 iOS + Android (control b61b18be also 1).** Contents: route-gated ahead alerts, speed-limit cover (Olaf), course gate, weather fix, footer/Route here, pricing sheet, the Showroom Garage + art fix, Diamond. ✅ `src/speedLimitCover.ts` locked on Jeff's words (he ran the relock; 44 files / 457 locks). Was: **COMMITTED, UNPUBLISHED: HEAD = route-gated alerts + fixes 2/3 + footer/Route-here UI + WEATHER (sun until overcast, trace-rain demoted, current conditions for near arrivals — `f679aa4a`) (2026-09-22, Jeff: "do 1-3")** — speed-limit cache cover + continue-the-number rule (Olaf's phantom tier-2), course gate at 1.5 m/s (stale walking course), relocked on his words, 40/40 sim-qc, sim-driven; publish blocked by the auto-mode classifier → Jeff runs the env:exec command. Base: `3d725053` (2026-09-22 ~12:15 PDT — ahead alerts gated to the ROUTE, 21 → 6 on the day's rows; `splash-done` receipt; `routeKm=` = planned route; corners proven untouched, nav lock PASS with no relock; sim-smoked in the release binary). Waits on Jeff's go — see ROADMAP §4d. · **OTA-BF** `b61b18be` (2026-09-21 ~19:00 PDT, Jeff: "Yes build them … Ship it"; KEY_PRESENT=1 both; runtime 1.29.0, code `82dc16b3`): voice heads-up for railway crossings / playground / school zones, longer speed-camera lead, splash safety timeout. · **OTA-BE** `bbdb70fd` (2026-09-21 ~01:20 PDT, Jeff: "you can fix them ...go"; KEY_PRESENT=1 both; runtime 1.29.0, code `a6d355d6`): a U-turn now costs a route its ranking — Rodrigo's "borderline illegal u-turns"; `uturns=` on depart-rank/route-swap is the first maneuver telemetry the fleet has ever had. · **OTA-BD** `1767048c` (2026-09-20 ~22:50 PDT, Jeff: "do it all"; KEY_PRESENT=1 both; runtime 1.29.0, code `2d58196f`): the CarPlay "Where to?" ownership fix + the phone turn-by-turn action row + Add stop mid-drive — row below. · **OTA-BC** `ad19e9d4` (2026-09-19 ~11:15, Jeff: "ship it"; KEY_PRESENT=1 both; code `0622b2f3`: the underpass reroute `why=crossed` 3 s sooner + route-follow fixes — row below) · **OTA-BB** `7fad7fad` (2026-09-18 ~23:05: route follow — no corner overshoot; zoom 17; slight turns silent; U-turns spoken; curb rule — Codex NOT run) · **OTA-BA** `82ad8bfb` (row below) · before it **OTA-AZ** `01918fc9-ba18-484d-a8aa-6e8949c8a7c4`, code `cc511678`, runtime 1.29.0 (2026-09-17 19:1x PDT) — the parked-heading fix (Codex r5b + ship-review guards; gates G5–G7), shipped on Jeff's "deploy the heading fix"; KEY_PRESENT=1 both. Before it: **OTA-AY** `63cfe9ed` (alert defaults), the first 1.29.0 OTA. | memory `ota-channel-is-mapbox-migration` |
| Previous OTAs (1.28.0, now frozen) | AX `3d7a9751` roundabout stutter dead-band · AW `800aeb4b` cold car-strip heal + receipts · AV `ce5e7162` off-ramp zoom ceiling. Every 1.28.0 install (Rodrigo, Ni GR, Olaf, Alfred, Jeff / Say Phin, SMSGRC, Ron, Victor, Explorer1, TestUser123) gets NOTHING until it installs 79. | same |
| Git | frontend `mapbox-migration` = origin = the nav-lock commits (`git log`: region markers ← `17edd14d` docs ← `d4edb607` lock v2 ← `46d8ecfa` ← **`f3508f17`** lock v1) ← `ca8ea181` (alert defaults, = OTA-AY) ← `7c403069` docs ← `f9a79ed2` (79 bump) ← the night's 13 nav commits ← `ac1cb9ab` (= OTA-AX). Worktree `/Users/jeffmorton/convoy-frontend-b79` at `f9a79ed2` holds the IPA copy. | `git log` |
| Gates | **ALL GREEN — re-run 2026-09-20 22:2x PDT on the OTA-BD working tree**: typecheck clean · trap-check **50 rules** 0 hits (49 + the whereto rule) · doc-check --live 0 broken · **36 of 36 `tools/sim-qc` gates** PASS incl. **`nav_lock_test`** (43 files, **457 locks** = code hashes + values + regions, **20** approvals on record) and `nav_lock_regions.mts --verify`. Counts at the 09-17 cut were 49 rules / 33 gates / 460 locks (`6a168f64` removed the 3 auto-boat WATER_* values). | §7 |
| OTA-BD (09-20, `1767048c`) | **Olaf's five complaints by iMessage — two fixed.** (1) The CarPlay "Where to?" list owns `_whereToPushed` instead of borrowing the keyboard's `_searchPushed`, so `armSearchAutoDismiss` no longer pops it while the car moves; `ios-stack` rows gain `whereto=0|1`; new trap-check rule `whereto-list-claims-the-keyboards-ownership-flag`. His receipts (re-queried 09-20 22:1x): 10 × `op=push id=whereto`, 10 × `op=root why=dismiss` (nine immediately after a push, shortest 0.14 s by `event_at`), 2 × `why=selected`, 0 × `op=push id=search`. **NOT FIELD-VERIFIED — it needs him to tap Search while moving.** (2) The phone turn-by-turn footer is four equal buttons End · Arrived · Show map · Add stop (Jeff's words) replacing the full-width Show map, the floating squares deleted; Add stop also lands as a blue tile in `StepDrawer` (the arrival clock hides at four tiles) and as `addStopNow` in `map.tsx`, with the first-ever `phone-tap:add-stop` receipt. **OPEN, all need Olaf:** the 09-16 triple-launch blank screen (he was on 1.28.0 with 305 `timer-starve` rows that window — co-occurrence only; ⚠ `timer-starve` is far rarer but NOT gone on 1.29.0: 109 rows on 09-19), an undescribed "sleep glitch", and CarPlay pinch (0 `carplay-gesture:zoomBegin` rows from any handle in the ~52 days the probe has been live; his own `tapZoomIn` double-tap works). | `ROADMAP.md` §4d |
| OTA-BC (09-19, SHIPPED `ad19e9d4`) | **`3f844877` → HEAD** (Codex r1–r4 findings reproduced + fixed: 2.5 s no-fix hold; reacquire after a gap / on this lap / after a U-turn / after a detour; the crossing hardened) — memory `field-2026-09-19-first-drives-on-ota-bb`, `ROADMAP.md` §4d |
| OTA-BA (09-18) | **SHIPPED `82ad8bfb` ~10:30 PDT on Jeff's "do it now please"** (KEY_PRESENT=1 both): in-app update check (Codex APPROVE) · auto-boat removed · the route line's notchy start fixed (near/far split, sim 206 → 0 jumping frames; Codex r1–r5 fixed, **r6 blocked by the Codex usage limit until 2026-09-19 10:38**). Glow start look awaits Jeff's OK. | ROADMAP §4d, memory `route-line-near-far-split-2026-09-18` |
| 🔒 Navigation lock | **ON since `f3508f17`, completed 09-17 morning (v2 `d4edb607` + the region markers)** — Jeff: "i dont want anything to change without my say so". See §2 before touching any nav file. | `RULES.md` §4, memory `nav-lock-jeff-2026-09-17` |
| Backend | **Pushed + LIVE 2026-09-17 ~20:55 PDT: `1405d3d`** (Render auto-deploy 1m14s; `/api/health` → `rev 1405d3d`, Supabase probe ok). That shipped the five Nova-greeting commits from 09-11 (`cd4be64` → `eb49c18`, never deployed before) + the Hairpin-branded reset email. Render: `EMAIL_FROM` set by Claude; `RESEND_API_KEY` = Jeff (Resend key `hairpin-backend`, sending-only, mail.gethairpin.com). Render bill = $50/mo (Pro workspace + Standard instance). | Render Deploys page |
| Supabase | **PRO since ~17:00 PDT 09-17** (Jeff). It ended a ~10 h REST outage (07:10 → 17:00 PDT: 504s + statement timeouts; telemetry in that window is lost). After: 0 errors, 23/60 connections, 225 MB. Compute probably **Micro** (`shared_buffers` 224 MB ⇒ ~1 GB — HYPOTHESIS, the dashboard is the check); research says **Small** before the club. | memory `supabase-disk-io-budget-free-nano-swap-2026-09-15` |
| Telemetry volume | last 24 h: ~15,000 rows from ~40 instances — draw-cmp 1775 · pose-fix 1714 · ribbon-trim 1582 · cam-probe 1579 · corner-trace 955 · nav-loc 798 · car-strip 748 · **timer-starve 656 (14 instances, all on 78 — the 79 pump is the fix)** ⚠ **superseded 09-20**: it still fires on runtime 1.29.0 — 109 rows on 09-19 from 5 instances / 4 handles, 23 on 09-20 (query 09-20 22:1x); far rarer than 1.28.0's 496–620/day, not gone. See the OTA-BD row. · cam-apply 645 · heat-probe 640 · nav-eta 633. At 170 members that is ~250k rows/day on a Nano — §4a. | query 01:0x |
| Codex | Release review 78→79 at `f9a79ed2`: **APPROVE**. Parked heading: r5b finding → fix → ship review HELD it (two regressions, both reproduced) → guards + G6/G7 → r2 **APPROVE** → OTA-AZ. | memory `build-79-cut` |

## 1 · What shipped 09-14 → 09-17 (receipts in the named memories)
- **Build 79 native** (12 commits, cut 09-17): CarPlay-first host gate + AA cold-start resume `0e4785a3` · AA CarJsKeepAlive `8e45f5bf` · car-screen status messages + AA permission request `defb1ecc`/`667fc42b` · location on While-Using `7452b128` · CarPlay root/tap receipts + dormant placeholder `10cee4eb` · x27 profile + verify-ipa `d9bc56ce` · HairpinTimerPump `57538092`+`92f2d909` · pod floor `517b76ac` · isProducedByAccessory `c71cf8d5` · iOS 27 XL widget `9d3461f2`/`77bb5866` · boot screen `c48aab35`. **None field-verified** — the proof rows are in memory `build-79-deploy-runbook-2026-09-16` §4.
- **JS riding 79 un-OTA'd (2026-09-16 night):** the **corner nose clamp** (`81d94274` → `b960c242`, 7 commits, Codex-approved after 7 rounds; field replay 18:27 corner 46° → 38°, 17:59 39° → 30°), the **route ribbon at the nose** (`cf743242`, phone-sim render measured), the **parked heading + curb-pass bearing + CarPlay constrained re-ask** (`9d998eb3` → `a2ec4824`, 5 commits, Codex rounds 1–4 fixed). Memory `field-2026-09-16-two-drives-on-ota-ax`.
- **OTA-AY** (alert defaults) — above.
- Field-verified on 09-16 (Jeff's two drives on OTA-AX): AV's ramp ceiling (zt=16.22 @86 km/h), AW's cold-strip heal (fired twice), AX's roundabouts ("looked good"). Same memory.

## 2 · 🔒 THE NAVIGATION LOCK — how to work from now on
Jeff, 2026-09-17: *"lets lock in/gate the navigation settings i think we have finally got 95% there is dont want anything
to change without my say so."*
- `tools/sim-qc/nav_lock_test.mts` + `tools/sim-qc/data/nav-lock.json` — THREE locks, 43 files:
  - **CODE lock (content hash, comments/whitespace ignored) — 33 drive-engine modules:** poseEstimator, chaseZoom, camGlide, routeTrim, routeRibbon, ribbonAnchor, coldStepHeal, offRouteGate, cornerBlend, selfLiftRule, navAnchor, departureBearing, carSpotTrust, driveFeed, speedProfile, speedLimitSnap, speedEpisode, arrivalEndings, tripOdometer, yawRate, yawFeed, fixCourseHere, speedDing, nav, navNotification, speedLimit, pitstop, fixCourse, rerouteSlot, maneuverSpeech, chasePitch, roadSnap, mapboxDirections.
    Any code edit in these fails — a refactor, a reordered rule, a deleted "dead" branch.
  - **VALUE lock — 206 named tunables in the 10 mixed files** (ConvoyMapbox, CarMapView, ConvoyCarPlay, map, carActions, locationPrivacy, settings, carStatus, timerLiveness, carStore): every
    module-scope SHOUTY constant is pinned by its initializer TEXT (numbers incl. `90_000`, arithmetic, ternaries, small
    objects) plus the navigation `DEFAULT_SETTINGS.*` fields; a NEW module-scope constant in those files fails too.
    Deliberately OVER-inclusive: only constants a verify agent proved non-nav are under `ignored` (map.tsx voice-line
    tables, carStatus permission rows, timerLiveness log cadences). Never widen `ignored` by hand — `--init`/`--relock`.
  - **REGION lock — inline drive logic inside the mixed files**, between `// 🔒 NAV-LOCK begin <id>` … `// 🔒 NAV-LOCK end <id>`
    comments (between JSX children: `{/* 🔒 NAV-LOCK begin <id> … */}`), pinned by the same code hash — **221 regions**
    (§2b). Removing/renaming a marker fails. NEVER type a marker by hand: `tools/sim-qc/nav_lock_regions.mts
    --check|--apply plan.json` places them and proves with BOTH parsers (TypeScript printer + @babel/parser, comments
    removed) that the program is identical with and without them — inside JSX a `//` line is on-screen TEXT — and
    `--verify` re-proves it (it is in the ship-ota / cut-build rituals); gate `nav_lock_regions_test.mts`.
  Mutation-tested 2026-09-17: a value change, an arithmetic change (`42 + 4` → `42 + 8`), a settings default flip, a new
  constant, an edit inside a region and a deleted marker all FAIL; a comment-only edit and an `ignored` telemetry cadence PASS.
- A failing gate is a QUESTION FOR JEFF, not a manifest edit. Investigate and design; ship after his words, then
  `node --experimental-strip-types tools/sim-qc/nav_lock_test.mts --relock "Jeff, <date>: <quote>"` (appends to `approvals`;
  it re-pins EVERYTHING that drifted and prints the list — show Jeff that list, not a summary).
- Stripping/optimising INSIDE a locked module or between two 🔒 markers (§4b) is a nav change: ask first.

### 2b · The region inventory (2026-09-17 morning) — what is locked inside the mixed files
| file | regions | lines locked | what they cover |
|---|---|---|---|
| `app/(app)/map.tsx` | 72 | 1,799 of 6,690 (26 %) | route fetch + alternative choice, departure bearing, reroute offer/accept/cooldown, off-route → reroute, stops visited, nav auto-start, arrival, speed-alert episodes, voice timing, the nav banner / car session lifecycle |
| `src/ConvoyMapbox.tsx` | 63 | 1,755 of 4,849 (36 %) | `projectOntoRoute`, the self-car draw path (pose feed, snap, lift, hard-snap, background-timer ease), `pushCam` + follow zoom/pitch/padding, ribbon trim/lead, **JSX: follow Camera props, route + congestion line layers (the 09-16 widths), SelfCarModel wiring** |
| `src/carplay/CarMapView.tsx` | 41 | 1,022 of 2,714 (37 %) | the car-screen mirror of all of the above, incl. JSX camera / ribbon layers / road + building query layers |
| `src/carplay/ConvoyCarPlay.tsx` | 17 | 330 of 2,211 (14 %) | maneuver/ETA push, upcoming-instruction pick, strip feed choice |
| `src/carplay/carActions.ts` · `carStore.ts` · `src/locationPrivacy.ts` · `src/timerLiveness.ts` | 10 · 5 · 10 · 3 | 8 % · 7 % · 43 % · 5 % | car-side route start (bearing-constrained re-ask), feed rank/staleness, the car spot + parked heading + shareable position, the timer heartbeat |
- How it was made: workflow `nav-region-inventory` — 17 file shards, a finder + an adversarial verifier per shard (34 agents):
  166 regions kept, 45 more ADDED by the verifiers, 0 dropped, 3 shard-boundary duplicates removed; + 10 JSX regions added
  from the constants inventory's "uncovered knobs" list. All 218 placed with 0 refusals, every region bracket-balanced, the
  hash/value locks byte-identical before and after (markers are comments), one in-region mutation per file caught by the gate.
- The verifiers also found two BLIND SPOTS of the value lock, now covered by regions where they matter: constants declared
  INDENTED inside a component (`STOP_VISITED_M`, `SPEED_LIMIT_SETTLE_MS`, … — the extractor reads column-0 declarations only)
  and initializers the extractor skips (`new Set([...])`, e.g. `REROUTE_HAZARD_KINDS`).
- ≥ 14 regions contain telemetry calls (map.tsx 6, CarMapView 4, ConvoyMapbox 2, ConvoyCarPlay 2 — ids in memory
  `nav-lock-inventory-2026-09-17`): §4a's telemetry trimming WILL trip them. Ask Jeff once for the batch ("trim the receipts
  inside the locked regions"), then one `--relock` with his words.
- To lock more later: write a plan JSON → `nav_lock_regions.mts --check` → `--apply` → `nav_lock_test.mts --relock "Jeff, …"`.

## 3 · Field state and open defects
- 79 proof rows to read after the first drives (Jeff car-first on iOS 27; Say Phin car-started AA): `ios-toolchain xcode=27A266a` ·
  `carplay-phone-hostwait` / `carplay-rootwatch` · `launch_kind=unknown` → ~0 · no `timer-starve surf=car` with the display off ·
  `loc-src accessory=1` on a CarPlay drive · `aa-native op=hold on=1` + `carplay-live-paint` in a car-started AA instance ·
  `car-status` rows · `depart-rank … fsrc=` · pose-fix rows at 90° corners (nose lag should be ~8–10° less in the first second).
- ✅ **SHIPPED — parked heading (Codex r5b → OTA-AZ, 09-17 evening):** a stop with CarPlay/AA still attached no longer freezes the
  approach heading, so a slow turn after it retires it. Codex's ship review found two ways the first version erased a CORRECT
  heading (GPS jitter while parked attached; Android's attachment TTL lapsing without a disconnect, then walking away) —
  both reproduced, both guarded (net distance from the stop; a lapsed attached hold freezes), gates G6/G7, r2 approve.
  Residual (= build 79): a three-point turn made after an attached stop with little net displacement keeps the old heading.
  Proof on the next drives: `depart-rank … fsrc=spot` departures that start the way the car faces after a lot manoeuvre.
- 🛑 **NEW, MEASURED 09-17 (unit-economics skeptic, Supabase realtime_logs, 24 h): 1,325 × `ClientPresenceRateLimitReached`.**
  Supabase allows 5 presence calls per client per 30 s; `src/presenceHub.ts` tracks every 1.5 s. Peer car positions are being
  throttled at ~10 users. Fix (not nav-locked): positions on Broadcast, Presence for join/leave only — build-80 prep, needs a design.
- Still open, pre-existing: phone force-closed while AA stays connected freezes the car session · Jeff's CarPlay End/Search
  taps dead since 09-07 (receipt only) · auto-arrival detection (Arrived button is the workaround) · the 20 m GPS fix jumps
  at corners (poseFix `jump` needs both flags — one nav change per drive, NOW BEHIND THE LOCK) · the 17:32 lot-exit
  departure (Mapbox honours `bearings=`; the lot geometry chose the other exit — HYPOTHESIS) · departure reroute storm
  leaving the home parkade (3 reroutes in 28 s).
- Camera changes still switched OFF (`ROUNDABOUT_HOLD_ENABLED`, `CAM_GLIDE_PUMP_ENABLED`) — a render is impossible on this Mac
  (Xcode 27 has no Simulator.app/CarPlay); the route-line hold branch `worktree-agent-ad786ae763e4b8ef9` is NOT merged.

## 4 · NEXT SESSION — Jeff's agenda, in his words
*"next session i want to do a debug and optimization to make this run smooth. and also strip out things we do not need or
have not used. also we will be preparing for build 80 which will include the pricing/email for login issues (need
recomendation on what to use.) and update things like supabase to handle the 170members, as well as apple and google to
handle it too."*

### 4·00 · 🔒 PRICING (set in stone 2026-09-22) — Free · Silver $4.99/$39.99 · Gold $9.99/$79.99 · Gold + Ultra $84.99/yr
3 scans a year (carry over), $2.99 extras, Ultra garage (the Showroom, built `4bb9d9f5`), diamond skin (MERGED `ab782791`). `src/pricing.ts`; memory
`pricing-final-set-in-stone-2026-09-22`. Website committed locally in ~/hairpin-site, unpushed until Jeff says "push the site".

### 4·0 · TONIGHT (2026-09-22, Jeff from home): two commands only Jeff can run, then Claude finishes
**Step 1 DONE — OTA-BG `c8cbbd88` published by Jeff from `93e985f8`, KEY_PRESENT=1 both, ow.key deleted. Step 2 DONE — Jeff ran the relock: `src/speedLimitCover.ts` code-locked (44 files, 457 locks, PASS). §4·0 is closed.**
Everything is committed and pushed (`0188dc45`); the auto-mode classifier refused Claude the publish ("Production Deploy") and
the lock-manifest edit ("Self-Modification"). Jeff: *"ok ill have to do this from home tonight."* Run from `frontend/`:

1. **Publish — ONE OTA** (route-gated ahead alerts + speed-limit cover + course gate; ROADMAP §4d has the receipts):
```bash
npx eas-cli env:exec preview "npx eas-cli update --branch mapbox-migration --clear-cache -m 'Ahead alerts follow the ROUTE; no phantom speed dings from a road beside a cache hole; a walking phone no longer sets the car facing' --non-interactive"
```
2. **Lock the new pure module** (adds `src/speedLimitCover.ts` to `tools/sim-qc/data/nav-lock.json`, relocks on Jeff's words):
```bash
python3 -c "import json,pathlib; p=pathlib.Path('tools/sim-qc/data/nav-lock.json'); m=json.loads(p.read_text()); m['files'].setdefault('src/speedLimitCover.ts', {'hash':'sha256:pending','why':'pure drive-engine module: code-locked (posted-limit cache cover + continue rule, 2026-09-22, Jeff: do 1-3)'}); p.write_text(json.dumps(m,indent=2)+'\n')" && node --experimental-strip-types tools/sim-qc/nav_lock_test.mts --relock "Jeff, 2026-09-22: do 1-3 — lock the new src/speedLimitCover.ts" && node --experimental-strip-types tools/sim-qc/nav_lock_test.mts | tail -1
```
⚠ Since `0188dc45`, HEAD also carries the turn-by-turn footer/Route here, the weather fix, lint, pricing, the Showroom Garage and
`ab782791` (Garage art: 2D arrow top-down + white rim, 3D arrow from the chase cam, Silver class car in 3D; **Diamond merged** —
Ultra/scan surfaces wear it, "Automatic" stays gold for testers). One publish from HEAD ships all of it; the `-m` text above
describes only the nav part — add "new Garage + Diamond skin" if you want the update list to say so.
Then Claude: read the group id off `eas update:list --branch mapbox-migration`, `python3 tools/ota/verify-bundle-key.py <group> 1.29.0`
→ `KEY_PRESENT=1` BOTH platforms, delete the `ow.key` the verify step writes under `tools/ota/` <!-- doc-check:ignore: ow.key is deliberately deleted after every verify -->, commit `nav-lock.json` ("Nav lock: speedLimitCover.ts under lock"),
record the group in ROADMAP/HANDOFF/memory, and send the tester-chat message (as Claude, red pill) — Jeff's OK on the text first.

### 4a · Debug + optimization ("make this run smooth") — where the data already points
0. **FIRST: is Supabase serving again?** `query_logs` for `| 504 |` in edge_logs and `statement timeout` in postgres_logs over the
   last hour (memory `supabase-disk-io-budget-free-nano-swap-2026-09-15` has the 09-17 outage timeline). Nothing below can be
   measured while inserts are failing.
1. **Telemetry is the biggest self-inflicted load.** ~15k rows/day at ~10 testers, five receipts over 900/day each
   (draw-cmp, pose-fix, ribbon-trim, cam-probe, corner-trace). Each is a Supabase INSERT from the phone on a Nano that
   swaps (memory `carplay-gl-retry-storm`: logEvent is an INSERT; bound every breadcrumb). Decide per receipt: keep /
   sample / retire — the debugging receipts (pose-fix, corner-trace, ribbon-trim, draw-cmp, cam-apply) were for the
   nav work that is now LOCKED. Target ≤ 2k rows/day/tester before the club. ⚠ Receipt code inside a hash-locked module
   needs Jeff's say-so (§2); receipts in map.tsx / drawTelemetry.ts do not.
2. **Heat / rAF.** memory `raf-runaway-spin-heat` (100 % iOS, trigger unknown), `heat-probe-first-measurements` (two rAF
   pumps ~115/s), `heat-subpixel-frame-skip`. The car surface runs a 12 Hz trim ticker + 120 Hz rAF; the phone map its own
   ease loop. Read `heat-probe` rows on 79 first (does HairpinTimerPump change the picture?).
3. **Main-thread stalls.** `main-gap` rows (332/24 h) are now all `easeIdle==dt` (idle); the `0x8BADF00D` watchdog class is
   closed (memory `watchdog-kill-rnmapbox-layer-rmw`). Any per-tick layer-style write is the trap to keep grepping for
   (trap-check rule exists).
4. **Startup and bundle.** Bundle 8.2 MB (main.jsbundle); `expo-doctor` lists patch-version mismatches (non-fatal on 79).
   Measure cold start to first map frame on 79 (`js-mark` → `car-chrome`/`carplay-live-paint` rows) before touching anything.
5. **Presence / realtime churn** at 170 members is the unknown: Realtime channel joins, `updates-log` (439 rows/24 h from
   42 instances — expo-updates chatter worth silencing), WebSocket reconnects. Measure on a club cruise, not in the sim.
Method (RULE #1): rows first, one hypothesis, one change, a gate, a receipt. `HANDOFF-2026-09-06.md` §2 has the
Codex-in-the-loop process; memory `stop-over-investigating-just-fix-it` the pace.

### 4b · Strip what we do not need or have not used
- Tooling: run `npx knip` (unused files / exports / dependencies) and `npx ts-prune` in `frontend/`; the naive
  "never imported" grep tonight was wrong (it flagged files map.tsx imports) — do not trust it, run the real tools.
- Known candidates (each needs a receipt that it is unused — telemetry never seen, feature flag off for weeks, no import):
  the switches shipped OFF (`CAM_GLIDE_PUMP_ENABLED`, `ROUNDABOUT_HOLD_ENABLED`, `AA_SYNTHETIC_RESUME`, the dormant CarPlay
  placeholder, `PHOTO_AVATAR_ENABLED`, `ENTITLEMENTS_ENFORCED` stays), wrist taps (memory `wrist-taps-cannot-work-as-designed`:
  never worked; 60 `watch-tap` rows/24 h from 2 instances), the retired legacy map engine comments, `hairpin-*.png/svg`,
  `eas.json.bak-*`, `.mcp.json.bak.*`, `scripts/install-xcode27.sh` / `setup-xcode-mcp.sh` (untracked) <!-- doc-check:ignore: untracked local helpers, named on purpose -->, the Hermes MCP wiring
  (memory `hermes-whatsapp-risk`), `modules/convoy-call-detector/android/build/` (118 untracked gradle files), the parked
  tier-skinned icon and weather radar, the dead `HANDOFF.md` header. Backend: `tests/test_convoy_v3.py` is the old LIVE suite.
- Anything inside a hash-locked nav module is NOT strippable without Jeff's say-so (§2) — list it, do not touch it.

### 4c · Build 80 prep — pricing, login email, Supabase, Apple/Google for 170 members
See §6 (citation-checked research from tonight) and memory `build-80-club-launch-plan` (Jeff's 08-20 plan: codes, free-tier
split, tier ladder — Gold = Ultra Premium, Silver = Premium, $9.99 scan add-on). Native items already queued for 80:
RevenueCat, the rest of the widget family, Xcode nag plugin (`thermal=` is OUT — Jeff 09-17: "thermal out") (memory `build-79-readiness-audit-2026-09-13`).

## 5 · Decisions Jeff owes / open items (do not re-litigate the ones he made)
- Made 09-16/17: build 79 go + deploy (TestFlight + Play internal, no APK) ✅ · OTA-AY ✅ · the crew note (sent 00:28 by Claude
  from his WhatsApp Web) ✅ · the navigation lock ✅ · the alert defaults ✅ · "fixes first then 79" ✅ · corner clamp accepted
  as "a bit tighter, not tight" (the gyro stays OFF — reopen only on his word).
- Made 09-17 evening: Supabase **Pro** ✅ · "deploy the heading fix" ✅ (OTA-AZ) · **"thermal out"** ✅ · testers on 79 ✅ (verified: Jeff,
  Olaf, Rodrigo, Say Phin, Victor, testuser123 report 1.29.0; Ni GR, SMSGRC, Alfred, Ron, Explorer1 not yet seen on it).
- Owed: ~~push the backend~~ ✅ (backend `1405d3d` + `9c3f536` live) · ~~email~~ ✅ (Resend on `mail.gethairpin.com`, reset mail delivered to inbox, §6b) · ~~ASC API key rotation~~ ✅ DONE 09-17 ~20:45: new EAS Submit key `3QCZ5YTA72` (App Manager), old `P9NUHM3CL7` revoked ·
  Supabase compute Small (optional, before the club) · Play Console declarations + background-location video · UGC report/block ·
  ~~the pricing decision~~ ✅ **MADE 09-17 late: the four-rung ladder (Free / Silver / Gold / Ultra one-time + Garage per car) — `ROADMAP.md` §5**,
  with his calls on a Gold lapse (keep the scanned car) and on what viewers see (the chooser's car). The **payments prerequisites** are on
  the list, not being built yet (`ROADMAP.md` §5 💳). Ultra's look = **Diamond** ✅ · auto-boat **REMOVED** ✅ (commit `6a168f64`,
  relocked with his words, **held for the next OTA — not published**). Still his: the website update · external TestFlight testers
  only after HIS car-first drive on 79.

## 6 · Build-80 research — the recommendations (memory `build-80-scaling-research-2026-09-17` has the cited facts)
Four researchers, 332 claims each carrying a URL + a verbatim quote from the vendor's own page (read 2026-09-17). Four citation
checkers then re-fetched every URL and string-matched every quote: **308 verbatim · 23 partly · 1 unsupported · 0 unreachable —
all four recommendations HOLD.** What they corrected (memory §0 has the detail):
- **IAP:** npm `latest` became `react-native-purchases@10.10.0` the same day → choose and pin EXACTLY at install time. Open issue **#1436**
  (crash setting the Purchases log handler on Expo 54 / RN 0.81 in DEV) → never call `setLogHandler`; step 1 is a throwaway local
  Xcode 27 build + launch with the package in (nothing proves it compiles with our patched pods). The 1 % is on ALL revenue once past $2,500.
- **Supabase:** $30/mo is a FLOOR (cap off = per-unit overage; Presence→message accounting undocumented). ⚠ During the check **9 of 11 SQL
  calls to the project timed out** with 30/60 connections open — the Nano is struggling today, before any club member joins.
- 🛑 **Android closed track:** Play requires in-app ACCOUNT DELETION (+ a web link) and in-app REPORT + BLOCK for UGC, with no testing-track
  carve-out; the app has neither (only "email support"). That is BUILD-80 SCOPE, not an App-Store-later item.
- **Apple:** "new version" means the MARKETING version (3.10.0 since build 74); the first EXTERNAL-group build always gets a full Beta App
  Review. Sign in with Apple is already wired — verify it, don't build it.
- **Email:** only password-reset codes are emailed today, so the 100/day cap matters only if verification / magic-link mail is added.

| question | recommendation | cost at 170 | what Jeff must do / decide |
|---|---|---|---|
| **Pricing / in-app purchases** | **RevenueCat** — `npx expo install react-native-purchases@<pin exactly: 10.9.1 researched; 10.10.0 became latest 09-17>`, NO `react-native-purchases-ui` (build the Gold/Silver paywall in-app per DESIGN.md). Native → runtime **1.30.0**, build 80 on both. Backend webhook `POST /webhooks/revenuecat`; club comps via REST `grant_entitlement`. | $0 while tracked revenue ≤ $2,500/mo, then 1% | Silver/Gold prices (monthly ± annual); is the $9.99 scan consumable or one-per-account; create products + the In-App Purchase Key in ASC, products + license testers in Play |
| **Email for login issues** | **Keep Resend, keep it on the Python backend** (users live in MongoDB, not Supabase Auth). Verified sending subdomain + DKIM/SPF/DMARC, `reply_to` support mailbox, rate-limit `forgot-password`, delivery webhooks. THE ONE CHECK: is `EMAIL_FROM` on Render a verified Hairpin address? (code default is `onboarding@resend.dev`). | $0 (3,000/mo, **100/day**) — Resend Pro $20 for the launch month if >100 mails in a day | the sending domain + a support mailbox |
| **Supabase for 170** | **Pro + SMALL compute (2 GB) + Spend Cap OFF** before the first cruise (lifts Realtime 500→10,000 connections). App change (OTA, outside the nav lock): positions ride Presence every 1.5 s but Supabase documents 5 presence calls/client/30 s → move to **Broadcast**. `crash_reports` retention job + cut receipt volume (§4a). | ~$30/mo ($25 + $15 − $10 credits) | add a card (billing showed none on 09-15) |
| **Apple / Google for 170** | iOS: stay on **TestFlight**, enable the external group's **Public Link** (limit 500); the first EXTERNAL-group build gets a full Beta App Review (a later build of the same MARKETING version "might not"); builds expire at **90 days**. Android: the INTERNAL track caps at **100 testers** → a **Closed testing** track "GRC club" (email list ≤ 2,000 or a Google Group) after finishing App content (Data safety incl. **in-app account deletion + web link**, UGC **report + block**, background-location declaration + video). | $0 | the Play declarations + video; check the Play account type for the 12-tester/14-day production gate (public launch only) |

### 6b · Login email — what is actually wrong (VERIFIED 2026-09-17 evening unless marked)
- `server.py` `_send_email`: sender = `EMAIL_FROM` or the fallback `onboarding@resend.dev`, Resend's shared TEST sender. Resend's own docs:
  *"You can only send testing emails to your own email address"* — every other recipient gets a 403.
- `gethairpin.com` (and hairpinapp.com, drivehairpin.com, hairpindrive.com) — all on Cloudflare DNS, **none has a Resend DKIM record**;
  `gethairpin.com` has no records at all. So no Hairpin domain can be a verified sender today.
- `POST /auth/forgot-password` returns `{ok: true}` whatever happens, and the send failure is only a warning in the Render log — so a tester
  sees "code sent" and never gets it. HYPOTHESIS that `EMAIL_FROM` is unset on Render (the one check: Render → the service → Environment,
  or search its logs for `Resend error 403`).
- Workaround TODAY: the in-app Admin screen's reset-code button (`app/(app)/admin.tsx`, `POST /admin/reset-code`) — hand the tester the code.
- **VERIFIED 09-17 ~20:00 PDT in Jeff's Resend account (team "jwellsmorton", the only team): NO API keys, NO domains, NO send logs in
  15 days.** So the backend has never sent through this account — Render's `RESEND_API_KEY` is unset (then `_send_email` logs "RESEND_API_KEY
  not set" and sends nothing) or belongs to some other account. Either way testers never received reset codes.
- DONE 09-17 (Claude, Jeff's OK): Resend domain **`mail.gethairpin.com`** added (us-east-1, manual setup — no Cloudflare OAuth granted) and
  its 4 records created in Cloudflare, all DNS-only: CNAME `rsend.mail` → `rsend.forge.rmta.net`, CNAME `send.mail` → `send.forge.rmta.net`,
  TXT `resend._domainkey.mail` (DKIM, RSA-1024 checked), TXT `_dmarc` `v=DMARC1; p=none;`. Live on Cloudflare's nameservers + Google DNS;
  Resend status was "pending" with one sending record verified at ~20:05.
- ✅ **FIXED + VERIFIED 2026-09-17 20:36 PDT:** Jeff created Resend key `hairpin-backend` (sending-only, mail.gethairpin.com) and saved it
  as Render `RESEND_API_KEY`; Claude set `EMAIL_FROM`; redeploy succeeded. Test `POST /api/auth/forgot-password` for Jeff's address →
  Resend "Delivered — Your Hairpin reset code" → Gmail INBOX (not spam) from noreply@mail.gethairpin.com at 03:36:49Z. (The first test
  at 03:36:00Z hit the old instance: Render log "RESEND_API_KEY not set".) Leftover: an unused Resend key "Onboarding" (all-domains
  sending) — DELETED 09-17 by Claude (Jeff's OK; "No activity" verified first).
- ✅ **Backend `9c3f536` LIVE 2026-09-17 ~21:05 PDT — reset abuse limits + honest errors** (reviewed twice by adversarial workflows;
  v1 had a real race — a 46th parallel guess reset a password — fixed by spending a guess atomically BEFORE comparing and making the
  password write conditional on the code). forgot-password: 1 code/min/address (a repeat answers ok and sends nothing, so build 79
  still advances to code entry), 5/hour/address, 10 per network per 15 min (network = Cloudflare's CF-Connecting-IP, IPv6 by /64;
  memory bounded by evicting, never fail-closed), 503 "couldn't send" on a Resend failure and then for EVERY address for 120 s.
  reset-password: ≤5 guesses per code (atomic), ≤20 wrong per account per 24 h across codes (the owner's Admin reset code clears it),
  weak password rejected before a guess is spent. Build 79 already shows the server's message (formatErr → detail) — no app update.
  VERIFIED LIVE: real reset delivered (one email for two requests), forged CF-Connecting-IP is refused by Cloudflare (403 / 1000),
  forged X-Forwarded-For cannot rotate the key (10th request → 429 on schedule). Tests: scratchpad test_reset_v3.py 20/20.
  Accepted, documented: the 5th-wrong message differs for registered accounts (register already reveals that).

## 7 · Gates and commands (all green at HEAD, 2026-09-17 morning)
```bash
# frontend — /Users/jeffmorton/convoy-frontend/frontend, branch mapbox-migration
yarn typecheck && python3 scripts/trap-check.py && python3 scripts/doc-check.py --live
for t in tools/sim-qc/*_test.mts; do node --experimental-strip-types "$t" | tail -1; done   # 33 gates incl. nav_lock_test + nav_lock_regions_test
node --experimental-strip-types tools/sim-qc/nav_lock_test.mts --list src/chaseZoom.ts     # what the lock sees (constants + regions)
node --experimental-strip-types tools/sim-qc/nav_lock_regions.mts --verify                 # every 🔒 marker is still a real comment (both parsers)
# release rituals: .claude/skills/ship-ota (OTA, verify 1.29.0) and .claude/skills/cut-build (80 = both platforms, one number)
# iOS local build recipe: tools/build/verify-ipa.py docstring; Android: EAS mapbox-android-store; submit Android from the MAIN checkout
```

## 8 · Traps learned 09-14 → 09-17 (memory file named where one exists)
- **expo-updates launches its extracted OTA copy, not a swapped `main.jsbundle`** — three "after" renders were the old code;
  `EXUpdatesEnabled=false` in **Expo.plist** (Info.plist does nothing) + move `.expo-internal` aside (`sim-verify-ota-bundle-swap`).
- **Xcode 27 has no Simulator.app / CarPlay** — Device Hub, Apple-gated (`xcode27-simulator-app-gone-device-hub`).
- **`eas submit --platform android` must run from the MAIN checkout** — `play-store-service-account.json` is gitignored and absent in a worktree (`build-79-cut`).
- **Codex has a usage quota** (hit at 00:2x; resets 03:21) — plan reviews before midnight-long sessions.
- **The desktop classifier blocks external writes (WhatsApp) non-deterministically** — first attempt denied, second allowed; the clipboard fallback works.
- **Home-area coordinates never enter the public repo** — sim-qc data files shift longitude +0.37° (`field-2026-09-16-two-drives-on-ota-ax`).
- **A bounded ratchet is hard**: five Codex rounds found five ways a "hold the nose" rule keeps a wrong heading; the fix that survived is time-bounded on the prediction clock with a rest hysteresis and a course-motion veto (`src/poseEstimator.ts` comments; gate Z5–Z8).
- **Memory index size cap ~24.4 KB** — trim stale lines when adding.

## 9 · Where things live
- Release worktree `/Users/jeffmorton/convoy-frontend-b79` (`f9a79ed2`), IPA `~/Downloads/Hairpin-79-f9a79ed2.ipa`.
- Session scratchpad (this session, `ecd8547f…`): `render0916/` (ribbon before/after), `corner90-rootcause.md`, `b79/` (build logs),
  `codex-*.txt` (all review rounds), `drive0916_corners.txt`.
- Nav-lock inventory (constants classification, region list, telemetry-bearing regions, `ignored` candidates): memory `nav-lock-inventory-2026-09-17`; raw per-agent results: `…/subagents/workflows/wf_7b2777a2-a23/journal.jsonl` (constants) and `wf_eb59fbff-323/journal.jsonl` (regions) under this session's project directory.
- The iPhone 16 Pro simulator `D0487F47` has build 78 with `EXUpdatesEnabled=false` and a swapped bundle — reset before any OTA test there.
