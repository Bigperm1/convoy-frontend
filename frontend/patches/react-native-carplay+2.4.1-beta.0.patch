diff --git a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlayModule.kt b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlayModule.kt
index ef6aa04..757ad2e 100644
--- a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlayModule.kt
+++ b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlayModule.kt
@@ -120,13 +120,33 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
   fun updateTemplate(templateId: String, config: ReadableMap) {
     handler.post {
       carTemplates[templateId] = config;
-      val screen = carScreens[name]
+      // CONVOY FIX (build 70): this looked up `carScreens[name]`, where `name` is
+      // getName() — the NATIVE MODULE's name, "RNCarPlay". carScreens is only ever
+      // keyed by "root" (setCarContext) and by templateId (createScreen), so the
+      // lookup ALWAYS missed and this whole method was a silent no-op on Android.
+      // Consequence: androidx never received the navigationInfo / travelEstimate the
+      // JS pushes, so Android Auto drew none of its own maneuver or ETA chrome and
+      // every pixel of guidance up there came from our own rendered surface.
+      // Look the screen up by templateId, falling back to the current root screen —
+      // which is what a single-template nav app (Convoy) actually has.
+      val screen = carScreens[templateId] ?: currentCarScreen
       if (screen != null) {
         val carScreenContext = carScreenContexts[screen];
         if (carScreenContext != null) {
-          val template = parseTemplate(config, carScreenContext);
-          screen.setTemplate(template, templateId, config);
-          screen.invalidate()
+          // WRAPPED DELIBERATELY. parseTemplate runs inside this bare handler.post
+          // with no try/catch upstream, so ANY parser throw is an uncaught exception
+          // on the car app's MAIN THREAD — a crash, not a warning. The parsers use
+          // non-null assertions liberally (parseTravelEstimate does
+          // getMap("destinationTime")!!, parseRoutingInfo getMap("step")!!), so one
+          // malformed payload would take Android Auto down mid-drive. With this,
+          // the worst case is the previous behaviour: the template is not updated.
+          try {
+            val template = parseTemplate(config, carScreenContext);
+            screen.setTemplate(template, templateId, config);
+            screen.invalidate()
+          } catch (e: Throwable) {
+            Log.w(TAG, "updateTemplate: parse/apply failed for $templateId: ${e.message}")
+          }
         }
       }
     }
diff --git a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlayService.kt b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlayService.kt
index 73aad53..4f187ab 100644
--- a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlayService.kt
+++ b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlayService.kt
@@ -1,23 +1,76 @@
 package org.birkir.carplay
 
-import android.content.BroadcastReceiver
 import android.content.Context
-import android.content.Intent
-import android.content.IntentFilter
 import android.util.Log
 import androidx.car.app.CarAppService
 import androidx.car.app.Session
 import androidx.car.app.SessionInfo
 import androidx.car.app.validation.HostValidator
 import com.facebook.react.ReactApplication
-import com.facebook.react.ReactInstanceManager
+import com.facebook.react.ReactHost
+
+/**
+ * ANDROID AUTO BLACK BOX (Convoy, 2026-07-24).
+ *
+ * When Android Auto fails it shows androidx's "encountered an unexpected error" card and the
+ * real Kotlin stack trace goes ONLY to logcat — which means a tester would have to sit in the
+ * car with a laptop and adb to capture it. Not acceptable.
+ *
+ * So we record it ourselves: any throw from the car entry points is appended to
+ * `filesDir/aa_crash.txt` and RETHROWN unchanged (behaviour is identical; this is purely a
+ * recorder). `filesDir` is exactly what expo-file-system exposes as `documentDirectory`, so
+ * the JS side reads the file on the next phone-app launch, uploads it to crash_reports and
+ * deletes it — see src/androidAutoCrashLog.ts. The tester just drives, sees the error, and
+ * opens the app later.
+ *
+ * Must never throw itself: a recorder that crashes would mask the very thing it records.
+ */
+object AACrashLog {
+  private const val FILE = "aa_crash.txt"
+  private const val MAX_BYTES = 64 * 1024
+
+  fun record(context: Context?, where: String, t: Throwable) {
+    try {
+      val ctx = context ?: return
+      val f = java.io.File(ctx.filesDir, FILE)
+      if (f.exists() && f.length() > MAX_BYTES) f.delete()
+      val sw = java.io.StringWriter()
+      t.printStackTrace(java.io.PrintWriter(sw))
+      f.appendText(
+        "=== ANDROID AUTO FAILURE ===\n" +
+          "at: " + where + "\n" +
+          "when: " + java.text.SimpleDateFormat("yyyy-MM-dd HH:mm:ss", java.util.Locale.US)
+            .format(java.util.Date()) + "\n" +
+          "type: " + t.javaClass.name + "\n" +
+          "message: " + (t.message ?: "(none)") + "\n" +
+          sw.toString() + "\n\n",
+      )
+      Log.e("AACrashLog", "recorded Android Auto failure at " + where, t)
+    } catch (ignored: Throwable) {
+      // never let the recorder become the failure
+    }
+  }
+}
 
 class CarPlayService : CarAppService() {
-  private lateinit var reactInstanceManager: ReactInstanceManager
+  // Bridgeless (RN 0.81 / New Architecture): there is no ReactInstanceManager.
+  // Resolve the app-wide bridgeless ReactHost singleton instead. Under Expo SDK 54
+  // (newArchEnabled) the generated MainApplication overrides ReactApplication.reactHost
+  // via ExpoReactHostFactory, so this is non-null; elvis-throw to stay null-safe.
+  private lateinit var reactHost: ReactHost
+
   override fun onCreate() {
     super.onCreate()
-    reactInstanceManager =
-      (application as ReactApplication).reactNativeHost.reactInstanceManager
+    try {
+      reactHost = (application as ReactApplication).reactHost
+        ?: throw IllegalStateException(
+          "CarPlayService: application does not expose a bridgeless reactHost. " +
+            "Android Auto requires the New Architecture (newArchEnabled)."
+        )
+    } catch (t: Throwable) {
+      AACrashLog.record(applicationContext, "CarPlayService.onCreate", t)
+      throw t
+    }
   }
 
   override fun createHostValidator(): HostValidator {
@@ -26,10 +79,16 @@ class CarPlayService : CarAppService() {
 
   override fun onCreateSession(sessionInfo: SessionInfo): Session {
     Log.d(TAG, "onCreateSession: sessionId = ${sessionInfo.sessionId}, display = ${sessionInfo.displayType}")
-    return CarPlaySession(reactInstanceManager)
+    try {
+      return CarPlaySession(reactHost)
+    } catch (t: Throwable) {
+      AACrashLog.record(applicationContext, "CarPlayService.onCreateSession", t)
+      throw t
+    }
   }
 
   companion object {
     var TAG = "CarPlayService"
   }
 }
+
diff --git a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlaySession.kt b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlaySession.kt
index 2235853..14154c5 100644
--- a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlaySession.kt
+++ b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlaySession.kt
@@ -6,43 +6,87 @@ import android.content.Context
 import android.content.Intent
 import android.content.IntentFilter
 import android.content.res.Configuration
+import android.os.Build
 import android.os.Bundle
+import android.os.Handler
 import android.util.Log
-import androidx.car.app.CarContext.SCREEN_SERVICE
 import androidx.car.app.Screen
-import androidx.car.app.ScreenManager
 import androidx.car.app.Session
 import androidx.lifecycle.DefaultLifecycleObserver
 import androidx.lifecycle.LifecycleOwner
-import com.facebook.react.ReactInstanceManager
+import com.facebook.react.ReactHost
+import com.facebook.react.ReactInstanceEventListener
 import com.facebook.react.bridge.Arguments
 import com.facebook.react.bridge.ReactContext
 import com.facebook.react.bridge.WritableNativeMap
 import com.facebook.react.modules.appregistry.AppRegistry
-import com.facebook.react.modules.core.TimingModule
-import com.facebook.react.modules.debug.DevSettingsModule
 import org.birkir.carplay.screens.CarScreen
+import org.birkir.carplay.utils.EventEmitter
 
 
-class CarPlaySession(private val reactInstanceManager: ReactInstanceManager) : Session(), DefaultLifecycleObserver {
+// Bridgeless (RN 0.81 / New Architecture): the session is fed the app-wide bridgeless
+// ReactHost instead of the (now non-existent) ReactInstanceManager.
+class CarPlaySession(private val reactHost: ReactHost) : Session(), DefaultLifecycleObserver {
   private lateinit var screen: CarScreen
 
   @SuppressLint("UnspecifiedRegisterReceiverFlag")
   override fun onCreateScreen(intent: Intent): Screen {
+    try {
+      return onCreateScreenInner(intent)
+    } catch (t: Throwable) {
+      // Android Auto black box — see AACrashLog in CarPlayService.kt. Records and rethrows.
+      AACrashLog.record(
+        try { carContext.applicationContext } catch (e: Throwable) { null },
+        "CarPlaySession.onCreateScreen", t,
+      )
+      throw t
+    }
+  }
+
+  @SuppressLint("UnspecifiedRegisterReceiverFlag")
+  private fun onCreateScreenInner(intent: Intent): Screen {
     Log.d(TAG, "On create screen " + intent.action + " - " + intent.dataString)
     val lifecycle = lifecycle
     lifecycle.addObserver(this)
     screen = CarScreen(carContext)
     screen.marker = "root"
 
-    // Handle reload events
-    carContext.registerReceiver(object : BroadcastReceiver() {
+    // Handle reload events.
+    //
+    // ⚠ THIS LINE IS WHY ANDROID AUTO NEVER WORKED (root-caused 2026-07-25 from a real
+    // head-unit stack trace, via the AACrashLog black box):
+    //
+    //   java.lang.SecurityException: com.sw0rdfisch.convoy: One of RECEIVER_EXPORTED or
+    //   RECEIVER_NOT_EXPORTED should be specified when a receiver isn't being registered
+    //   exclusively for system broadcasts
+    //     at android.app.ContextImpl.registerReceiver(ContextImpl.java:1990)
+    //     at org.birkir.carplay.CarPlaySession.onCreateScreenInner(CarPlaySession.kt:53)
+    //
+    // Android 13 (API 33) REQUIRES the export flag for any receiver that isn't listening
+    // exclusively to system broadcasts, and we target SDK 35. "org.birkir.carplay.APP_RELOAD"
+    // is our OWN private action, so the unflagged call throws, onCreateScreen dies, and
+    // androidx shows its "encountered an unexpected error / Exit" card — the exact symptom
+    // testers reported on every build since the AA port.
+    //
+    // NOTE the @SuppressLint("UnspecifiedRegisterReceiverFlag") on this method: it silences
+    // the LINT, not the RUNTIME check. That suppression is very likely why this shipped.
+    //
+    // RECEIVER_NOT_EXPORTED is correct here — nothing outside the app should ever be able to
+    // trigger a JS reload. Branch on the API level rather than pulling in ContextCompat so the
+    // patch adds no new dependency.
+    val reloadReceiver = object : BroadcastReceiver() {
       override fun onReceive(context: Context, intent: Intent) {
         if ("org.birkir.carplay.APP_RELOAD" == intent.action) {
-          invokeStartTask(reactInstanceManager.currentReactContext!!);
+          reactHost.currentReactContext?.let { invokeStartTask(it) }
         }
       }
-    }, IntentFilter("org.birkir.carplay.APP_RELOAD"))
+    }
+    val reloadFilter = IntentFilter("org.birkir.carplay.APP_RELOAD")
+    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
+      carContext.registerReceiver(reloadReceiver, reloadFilter, Context.RECEIVER_NOT_EXPORTED)
+    } else {
+      carContext.registerReceiver(reloadReceiver, reloadFilter)
+    }
 
     // Run JS
     runJsApplication()
@@ -50,16 +94,20 @@ class CarPlaySession(private val reactInstanceManager: ReactInstanceManager) : S
   }
 
   private fun runJsApplication() {
-    val reactContext = reactInstanceManager.currentReactContext
+    val reactContext = reactHost.currentReactContext
     if (reactContext == null) {
-      reactInstanceManager.addReactInstanceEventListener(
-        object : ReactInstanceManager.ReactInstanceEventListener {
-          override fun onReactContextInitialized(reactContext: ReactContext) {
-            invokeStartTask(reactContext)
-            reactInstanceManager.removeReactInstanceEventListener(this)
+      // Instance not yet initialized — register a one-shot listener and kick off start().
+      // Bridgeless: ReactInstanceEventListener is a top-level interface (not nested on the
+      // old ReactInstanceManager), and createReactContextInBackground() becomes start().
+      reactHost.addReactInstanceEventListener(
+        object : ReactInstanceEventListener {
+          override fun onReactContextInitialized(context: ReactContext) {
+            invokeStartTask(context)
+            reactHost.removeReactInstanceEventListener(this)
           }
         })
-      reactInstanceManager.createReactContextInBackground()
+      // start() must run on the main looper.
+      Handler(carContext.mainLooper).post { reactHost.start() }
     } else {
       invokeStartTask(reactContext)
     }
@@ -67,25 +115,21 @@ class CarPlaySession(private val reactInstanceManager: ReactInstanceManager) : S
 
   private fun invokeStartTask(reactContext: ReactContext) {
     try {
-      val catalystInstance = reactContext.catalystInstance
+      // Bridgeless: resolve AppRegistry directly off the ReactContext (getJSModule is
+      // proxied to reactHost.callFunctionOnModule). Do NOT go through catalystInstance —
+      // it no longer exists in the New Architecture.
       val jsAppModuleName = "AndroidAuto"
       val appParams = WritableNativeMap()
       appParams.putDouble("rootTag", 1.0)
-      val appProperties = Bundle.EMPTY
-      if (appProperties != null) {
-        appParams.putMap("initialProps", Arguments.fromBundle(appProperties))
-      }
+      // Pass an empty (non-null) props bundle so JS that reads initialProps as an object
+      // does not NPE. Bundle.EMPTY is a shared immutable empty bundle.
+      appParams.putMap("initialProps", Arguments.fromBundle(Bundle.EMPTY))
 
-      catalystInstance.getJSModule(AppRegistry::class.java)
-        .runApplication(jsAppModuleName, appParams)
+      reactContext.getJSModule(AppRegistry::class.java)
+        ?.runApplication(jsAppModuleName, appParams)
 
-      val timingModule = reactContext.getNativeModule(
-        TimingModule::class.java
-      )
-      val carModule = reactInstanceManager
-        .currentReactContext?.getNativeModule(CarPlayModule::class.java)
+      val carModule = reactContext.getNativeModule(CarPlayModule::class.java)
       carModule!!.setCarContext(carContext, screen)
-      timingModule!!.onHostResume()
 
     } catch (e: Exception) {
       e.printStackTrace()
@@ -94,8 +138,30 @@ class CarPlaySession(private val reactInstanceManager: ReactInstanceManager) : S
 
   override fun onDestroy(owner: LifecycleOwner) {
     Log.i(TAG, "onDestroy")
-    val context = carContext
-    // stop services here, if any
+    // ── HAIRPIN PATCH: tell JS the car session ended (2026-07-31) ──────────────
+    // Upstream this body is a no-op with the comment "stop services here, if any",
+    // and CarPlayModule never calls eventEmitter.didDisconnect() either — so on
+    // Android the JS side receives NO disconnect signal at all, ever.
+    //
+    // That is a battery bug, not a cosmetic one. AndroidAutoRoot acquires the shared
+    // background-location consumer on mount and releases it only from its effect
+    // cleanup, and nothing unmounts that root: this onDestroy never tore it down, and
+    // there is no unmountApplicationComponentAtRootTag anywhere in the library. The AA
+    // root runs on the app-wide ReactHost, i.e. the SAME JS context and the same
+    // consumer Set as the phone app, so once 'androidauto' is in it,
+    // releaseBgLocation's size-0 gate makes stopping the feeds unreachable for the life
+    // of the process. A tester measured 2 h 08 m of GPS against 21 minutes of screen
+    // time, with a foreground service keeping the process alive so the leak sustained
+    // itself and the stall watchdog rebuilt the feeds every 25 s.
+    //
+    // EventEmitter.didDisconnect() already exists and is already wired through to the
+    // JS `didDisconnect` event — it was simply never called on this platform. Emitting
+    // it here lets AndroidAutoRoot release the consumer for real.
+    try {
+      reactHost.currentReactContext?.let { EventEmitter(it).didDisconnect() }
+    } catch (e: Exception) {
+      e.printStackTrace()
+    }
   }
 
   override fun onNewIntent(intent: Intent) {
diff --git a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTListTemplate.kt b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTListTemplate.kt
index f6f441a..1071baf 100644
--- a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTListTemplate.kt
+++ b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTListTemplate.kt
@@ -35,7 +35,7 @@ class RCTListTemplate(
       // Sections
       props.getArray("sections")?.let {
         for (i in 0 until it.size()) {
-          val section = it.getMap(i)
+          val section = it.getMap(i)!!
           val header = section.getString("header")
           addSectionedList(
             SectionedItemList.create(
diff --git a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTTabTemplate.kt b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTTabTemplate.kt
index 11d8261..ff87911 100644
--- a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTTabTemplate.kt
+++ b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTTabTemplate.kt
@@ -21,10 +21,10 @@ class RCTTabTemplate(
       setLoading(props.isLoading())
       props.getArray("templates")?.let {
         for (i in 0 until it.size()) {
-          addTab(parseTab(it.getMap(i)))
+          addTab(parseTab(it.getMap(i)!!))
         }
         // Apply and select first tab
-        it.getMap(0).getString("id")?.let { it1 ->
+        it.getMap(0)!!.getString("id")?.let { it1 ->
           setTabContents(parseTabContents(it1))
           setActiveTabContentId(it1)
         }
diff --git a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTTemplate.kt b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTTemplate.kt
index 01dbabd..bacb6f0 100644
--- a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTTemplate.kt
+++ b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTTemplate.kt
@@ -153,9 +153,9 @@ abstract class RCTTemplate(
     return ItemList.Builder().apply {
       for (i in 0 until items!!.size()) {
         if (type == "row") {
-          addItem(parseRowItem(items.getMap(i), i))
+          addItem(parseRowItem(items.getMap(i)!!, i))
         } else if (type == "grid") {
-          addItem(parseGridItem(items.getMap(i), i))
+          addItem(parseGridItem(items.getMap(i)!!, i))
         }
       }
     }.build()
@@ -187,12 +187,12 @@ abstract class RCTTemplate(
       if (titleVariants != null) {
         if (titleVariants.size() > 0) {
           setTitle(parseCarText(
-            titleVariants.getString(0),
+            titleVariants.getString(0)!!,
             metadata
           ))
         }
         if (titleVariants.size() > 1) {
-          setText(titleVariants.getString(1))
+          setText(titleVariants.getString(1)!!)
         }
       }
       item.getMap("image")?.let { setImage(parseCarIcon(it)) }
@@ -259,7 +259,7 @@ abstract class RCTTemplate(
     )
     props.getArray("texts")?.let {
       for (i in 0 until it.size()) {
-        builder.addText(it.getString(i))
+        builder.addText(it.getString(i)!!)
       }
     }
     props.getMap("image")?.let {
@@ -306,7 +306,7 @@ abstract class RCTTemplate(
       }
       item.getArray("items")?.let {
         for (i in 0 until it.size()) {
-          addRow(parseRowItem(it.getMap(i), i))
+          addRow(parseRowItem(it.getMap(i)!!, i))
         }
       }
     }.build()
diff --git a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/utils/EventEmitter.kt b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/utils/EventEmitter.kt
index 586b354..076bcbe 100644
--- a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/utils/EventEmitter.kt
+++ b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/utils/EventEmitter.kt
@@ -68,6 +68,36 @@ class EventEmitter(
     const val AlertActionPressed = "alertActionPressed"
     const val SelectedPreviewForTrip = "selectedPreviewForTrip"
     const val StartedTrip = "startedTrip"
+
+    // ── ANDROID AUTO PINCH-TO-ZOOM (Convoy patch) ────────────────────────────
+    // Android had no zoom event at all. androidx delivers pinch through
+    // SurfaceCallback.onScale, but the library's SurfaceCallback (VirtualRenderer)
+    // implemented ONLY onSurfaceAvailable, so the gesture died in native and no OTA
+    // could reach it. Deliberately the SAME event name iOS uses, because the JS
+    // eventMap in src/templates/MapTemplate.ts already routes
+    // didUpdateZoomGestureWithCenter -> onDidUpdateZoomGesture; reusing it means the
+    // two car surfaces share one handler instead of drifting apart.
+    const val DidUpdateZoomGestureWithCenter = "didUpdateZoomGestureWithCenter"
+  }
+
+  /**
+   * Pinch on the Android Auto map surface.
+   *
+   * ⚠ `scale` here is androidx's scaleFactor, which is INCREMENTAL (the change since the
+   * last callback). iOS CPMapTemplate reports a CUMULATIVE scale from the start of the
+   * gesture. The payload carries `incremental` so the JS side can tell them apart from the
+   * event itself rather than sniffing the platform.
+   */
+  fun didUpdateZoomGesture(scale: Float, focusX: Float, focusY: Float) {
+    emit(DidUpdateZoomGestureWithCenter, Arguments.createMap().apply {
+      putDouble("scale", scale.toDouble())
+      putBoolean("incremental", true)
+      putDouble("velocity", 0.0)
+      putMap("center", Arguments.createMap().apply {
+        putDouble("x", focusX.toDouble())
+        putDouble("y", focusY.toDouble())
+      })
+    })
   }
 
   fun didConnect() {
diff --git a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/utils/VirtualRenderer.kt b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/utils/VirtualRenderer.kt
index bafa640..30baed9 100644
--- a/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/utils/VirtualRenderer.kt
+++ b/node_modules/react-native-carplay/android/src/main/java/org/birkir/carplay/utils/VirtualRenderer.kt
@@ -5,6 +5,7 @@ import android.content.Context
 import android.hardware.display.DisplayManager
 import android.os.Bundle
 import android.util.Log
+import android.view.ContextThemeWrapper
 import android.view.Display
 import android.view.ViewGroup
 import androidx.car.app.AppManager
@@ -12,14 +13,18 @@ import androidx.car.app.CarContext
 import androidx.car.app.SurfaceCallback
 import androidx.car.app.SurfaceContainer
 import com.facebook.react.ReactApplication
-import com.facebook.react.ReactRootView
+import org.birkir.carplay.AACrashLog
+import com.facebook.react.interfaces.fabric.ReactSurface
 
 /**
  * Renders the view tree into a surface using VirtualDisplay. It runs the ReactNative component registered
+ * under [moduleName]. Bridgeless (RN 0.81 / New Architecture): the map is rendered through a
+ * ReactHost-created ReactSurface (Fabric) instead of the legacy ReactRootView.startReactApplication
+ * Paper path.
  */
 class VirtualRenderer(private val context: CarContext, private val moduleName: String) {
 
-  private var rootView: ReactRootView? = null
+  private var surface: ReactSurface? = null
 
   init {
     context.getCarService(AppManager::class.java).setSurfaceCallback(object : SurfaceCallback {
@@ -31,6 +36,26 @@ class VirtualRenderer(private val context: CarContext, private val moduleName: S
           renderPresentation(surfaceContainer)
         }
       }
+
+      // ── ANDROID AUTO PINCH-TO-ZOOM (Convoy patch) ──────────────────────────
+      // This override is the entire reason AA pinch was impossible over the air: the
+      // callback implemented onSurfaceAvailable and nothing else, so androidx called
+      // onScale into the default no-op and the gesture never left native. The JS half
+      // has worked for months (it is the same bus CarPlay's pinch uses) — only this
+      // emit was missing.
+      // Never throws into androidx: a car service callback that raises takes the whole
+      // AA screen down, and a dropped pinch frame is worth far less than that.
+      override fun onScale(focusX: Float, focusY: Float, scaleFactor: Float) {
+        try {
+          val appContext = context.applicationContext
+          val reactHost = (appContext as? ReactApplication)?.reactHost ?: return
+          reactHost.currentReactContext?.let {
+            EventEmitter(it).didUpdateZoomGesture(scaleFactor, focusX, focusY)
+          }
+        } catch (t: Throwable) {
+          Log.w(TAG, "onScale emit failed", t)
+        }
+      }
     })
   }
 
@@ -52,19 +77,42 @@ class VirtualRenderer(private val context: CarContext, private val moduleName: S
     Presentation(context, display) {
     override fun onCreate(savedInstanceState: Bundle?) {
       super.onCreate(savedInstanceState)
-      val instanceManager =
-        (context.applicationContext as ReactApplication).reactNativeHost.reactInstanceManager
-      if (rootView == null) {
-        Log.d(TAG, "onCreate: rootView is null, initializing rootView")
-        rootView = ReactRootView(context).apply {
-          startReactApplication(instanceManager, moduleName)
-          runApplication()
-        }
-      } else {
-        (rootView?.parent as? ViewGroup)?.removeView(rootView)
+      try {
+        onCreateInner()
+      } catch (t: Throwable) {
+        // Android Auto black box — see AACrashLog in CarPlayService.kt. Records and rethrows.
+        AACrashLog.record(context.applicationContext, "VirtualRenderer.MapPresentation.onCreate", t)
+        throw t
       }
-      rootView?.let {
-        setContentView(it)
+    }
+
+    private fun onCreateInner() {
+      // Bridgeless: resolve the app-wide ReactHost. The Presentation's constructor context is a
+      // display/outer context, so go through applicationContext (the Application is the
+      // ReactApplication). Elvis-throw to stay null-safe like the rest of the port.
+      val appContext = context.applicationContext
+      val reactHost = (appContext as ReactApplication).reactHost
+        ?: throw IllegalStateException(
+          "VirtualRenderer: application does not expose a bridgeless reactHost. " +
+            "Android Auto requires the New Architecture (newArchEnabled)."
+        )
+      if (surface == null) {
+        Log.d(TAG, "onCreate: surface is null, creating ReactSurface for $moduleName")
+        // A bare VirtualDisplay/Presentation context has no AppCompat/app theme, so RN views
+        // fail to resolve theme attrs and the map renders black. Wrap the application context
+        // (which carries the manifest AppTheme) so the Fabric surface has a real theme. Use the
+        // framework ContextThemeWrapper to avoid a compile-time androidx.appcompat.R reference;
+        // applicationInfo.theme is the manifest <application android:theme> resource id.
+        val themed: Context = ContextThemeWrapper(appContext, appContext.applicationInfo.theme)
+        // createSurface() attaches a ReactSurfaceView immediately, so .view is non-null here.
+        val newSurface = reactHost.createSurface(themed, moduleName, null)
+        surface = newSurface
+        newSurface.view?.let { setContentView(it) }
+        newSurface.start()
+      } else {
+        // Re-parent the existing surface view onto this Presentation.
+        (surface?.view?.parent as? ViewGroup)?.removeView(surface?.view)
+        surface?.view?.let { setContentView(it) }
       }
     }
   }
@@ -73,3 +121,4 @@ class VirtualRenderer(private val context: CarContext, private val moduleName: S
     const val TAG = "VirtualRenderer"
   }
 }
+
diff --git a/node_modules/react-native-carplay/ios/RCTConvert+RNCarPlay.m b/node_modules/react-native-carplay/ios/RCTConvert+RNCarPlay.m
index 5ef378d..26b7312 100644
--- a/node_modules/react-native-carplay/ios/RCTConvert+RNCarPlay.m
+++ b/node_modules/react-native-carplay/ios/RCTConvert+RNCarPlay.m
@@ -43,6 +43,17 @@ + (CPMapButton*)CPMapButton:(id)json withHandler:(void (^)(CPMapButton * _Nonnul
         [mapButton setImage:[RCTConvert UIImage:json[@"image"]]];
     }
 
+    // Convoy: SF-symbol map buttons. [RCTConvert UIImage:] resolves RN asset
+    // dicts through the LEGACY bridge image loader, which returns nil under
+    // bridgeless New Arch — and CarPlay renders NOTHING for a CPMapButton whose
+    // image is nil (why the police/Scout-mic map buttons "never rendered" on
+    // builds 61-64). UIImage systemImageNamed: needs no bridge at all, so it
+    // works everywhere. Takes precedence over a (probably-nil) custom image.
+    if ([json objectForKey:@"systemImage"]) {
+        UIImage *sysImg = [UIImage systemImageNamed:[RCTConvert NSString:json[@"systemImage"]]];
+        if (sysImg) { [mapButton setImage:sysImg]; }
+    }
+
     if ([json objectForKey:@"focusedImage"]) {
         [mapButton setFocusedImage:[RCTConvert UIImage:json[@"focusedImage"]]];
     }
diff --git a/node_modules/react-native-carplay/ios/RNCarPlay.m b/node_modules/react-native-carplay/ios/RNCarPlay.m
index 153dbad..bf1eaf8 100644
--- a/node_modules/react-native-carplay/ios/RNCarPlay.m
+++ b/node_modules/react-native-carplay/ios/RNCarPlay.m
@@ -103,6 +103,17 @@ + (id)allocWithZone:(NSZone *)zone {
         @"panEndedWithDirection",
         @"panWithDirection",
         @"didBeginPanGesture",
+        // Convoy: iOS-26 multitouch map gestures (pinch/zoom, rotate, pitch).
+        // Verified against CarPlay/CPMapTemplate.h (iOS 26.0 SDK, Xcode 26.6).
+        @"didBeginZoomGesture",
+        @"didUpdateZoomGestureWithCenter",
+        @"didEndZoomGestureWithVelocity",
+        @"didBeginRotationGesture",
+        @"didRotateWithCenter",
+        @"rotationDidEndWithVelocity",
+        @"didBeginPitchGesture",
+        @"pitchWithCenter",
+        @"pitchEndedWithCenter",
         @"didDismissPanningInterface",
         @"willDismissPanningInterface",
         @"didShowPanningInterface",
@@ -978,9 +989,13 @@ - (void) applyConfigForMapTemplate:(CPMapTemplate*)mapTemplate templateId:(NSStr
     }
 
     if ([config objectForKey:@"render"]) {
-        RCTRootView *rootView = [[RCTRootView alloc] initWithBridge:self.bridge moduleName:templateId initialProperties:@{}];
-        RNCarPlayViewController *viewController = [[RNCarPlayViewController alloc] initWithRootView:rootView];
-        store.window.rootViewController = viewController;
+        // Convoy patch: intentionally disabled. react-native-carplay mounts the
+        // CarPlay window with RCTRootView(initWithBridge:), which renders NOTHING
+        // under the New Architecture (bridgeless, RN 0.81 / Expo SDK 54) -> blank
+        // car screen. Convoy instead mounts the car window from CarSceneDelegate
+        // (plugins/withConvoyCarPlay.js) using Expo's bridgeless root-view factory
+        // (rootViewFactory viewWithModuleName:). Leaving this block empty stops the
+        // dead bridge path from clobbering that window's rootViewController.
     }
 }
 
@@ -1048,6 +1063,23 @@ - (void) applyConfigForMapTemplate:(CPMapTemplate*)mapTemplate templateId:(NSStr
         BOOL _disabled = [barButton objectForKey:@"disabled"];
         [_barButton setEnabled:!_disabled];
 
+        // Convoy addition: CPBarButton.buttonStyle. The library never sets it, so every
+        // nav-bar button rendered as bare text (CPBarButtonStyleNone is the default,
+        // CPBarButton.h:63). CPBarButtonStyleRounded draws the system's rounded
+        // background behind the label — the only shape control Apple exposes for a bar
+        // button; its SIZE stays system-owned. iOS 14+, so the availability guard keeps
+        // older head units on the default rather than crashing on an unknown selector.
+        if (@available(iOS 14.0, *)) {
+            NSString *_buttonStyle = [RCTConvert NSString:[barButton objectForKey:@"buttonStyle"]];
+            if (_buttonStyle) {
+                if ([_buttonStyle isEqualToString:@"rounded"]) {
+                    [_barButton setButtonStyle:CPBarButtonStyleRounded];
+                } else if ([_buttonStyle isEqualToString:@"none"]) {
+                    [_barButton setButtonStyle:CPBarButtonStyleNone];
+                }
+            }
+        }
+
         if (_type == CPBarButtonTypeText) {
             NSString *_title = [barButton objectForKey:@"title"];
             [_barButton setTitle:_title];
@@ -1460,6 +1492,43 @@ - (void)mapTemplate:(CPMapTemplate *)mapTemplate didUpdatePanGestureWithTranslat
     [self sendTemplateEventWithName:mapTemplate name:@"didUpdatePanGestureWithTranslation" json:@{ @"translation": @{ @"x": @(translation.x), @"y": @(translation.y) }, @"velocity": @{ @"x": @(velocity.x), @"y": @(velocity.y) }}];
 }
 
+// Convoy: iOS-26 multitouch map gestures (pinch/zoom, rotate, pitch).
+// Signatures verified against CarPlay/CPMapTemplate.h in the iOS 26.0 SDK
+// (Xcode 26.6). All CPMapTemplateDelegate gesture callbacks below are
+// API_AVAILABLE(ios(26.0)); the SDK-max compile guard keeps this file building
+// on older Xcode toolchains (the selectors simply won't be compiled in).
+// NOTE (from Apple's header): like the pan gesture, these "may not be called
+// when connected to some CarPlay systems" — real head units can gate raw touch.
+#if defined(__IPHONE_OS_VERSION_MAX_ALLOWED) && __IPHONE_OS_VERSION_MAX_ALLOWED >= 260000
+- (void)mapTemplateDidBeginZoomGesture:(CPMapTemplate *)mapTemplate API_AVAILABLE(ios(26.0)) {
+    [self sendTemplateEventWithName:mapTemplate name:@"didBeginZoomGesture"];
+}
+- (void)mapTemplate:(CPMapTemplate *)mapTemplate didUpdateZoomGestureWithCenter:(CGPoint)center scale:(CGFloat)scale velocity:(CGFloat)velocity API_AVAILABLE(ios(26.0)) {
+    [self sendTemplateEventWithName:mapTemplate name:@"didUpdateZoomGestureWithCenter" json:@{ @"center": @{ @"x": @(center.x), @"y": @(center.y) }, @"scale": @(scale), @"velocity": @(velocity) }];
+}
+- (void)mapTemplate:(CPMapTemplate *)mapTemplate didEndZoomGestureWithVelocity:(CGFloat)velocity API_AVAILABLE(ios(26.0)) {
+    [self sendTemplateEventWithName:mapTemplate name:@"didEndZoomGestureWithVelocity" json:@{ @"velocity": @(velocity) }];
+}
+- (void)mapTemplateDidBeginRotationGesture:(CPMapTemplate *)mapTemplate API_AVAILABLE(ios(26.0)) {
+    [self sendTemplateEventWithName:mapTemplate name:@"didBeginRotationGesture"];
+}
+- (void)mapTemplate:(CPMapTemplate *)mapTemplate didRotateWithCenter:(CGPoint)center rotation:(CGFloat)rotation velocity:(CGFloat)velocity API_AVAILABLE(ios(26.0)) {
+    [self sendTemplateEventWithName:mapTemplate name:@"didRotateWithCenter" json:@{ @"center": @{ @"x": @(center.x), @"y": @(center.y) }, @"rotation": @(rotation), @"velocity": @(velocity) }];
+}
+- (void)mapTemplate:(CPMapTemplate *)mapTemplate rotationDidEndWithVelocity:(CGFloat)velocity API_AVAILABLE(ios(26.0)) {
+    [self sendTemplateEventWithName:mapTemplate name:@"rotationDidEndWithVelocity" json:@{ @"velocity": @(velocity) }];
+}
+- (void)mapTemplateDidBeginPitchGesture:(CPMapTemplate *)mapTemplate API_AVAILABLE(ios(26.0)) {
+    [self sendTemplateEventWithName:mapTemplate name:@"didBeginPitchGesture"];
+}
+- (void)mapTemplate:(CPMapTemplate *)mapTemplate pitchWithCenter:(CGPoint)center API_AVAILABLE(ios(26.0)) {
+    [self sendTemplateEventWithName:mapTemplate name:@"pitchWithCenter" json:@{ @"center": @{ @"x": @(center.x), @"y": @(center.y) } }];
+}
+- (void)mapTemplate:(CPMapTemplate *)mapTemplate pitchEndedWithCenter:(CGPoint)center API_AVAILABLE(ios(26.0)) {
+    [self sendTemplateEventWithName:mapTemplate name:@"pitchEndedWithCenter" json:@{ @"center": @{ @"x": @(center.x), @"y": @(center.y) } }];
+}
+#endif
+
 
 
 # pragma SearchTemplate
diff --git a/node_modules/react-native-carplay/lib/interfaces/MapButton.d.ts b/node_modules/react-native-carplay/lib/interfaces/MapButton.d.ts
index 9e3b0a7..0e0a74d 100644
--- a/node_modules/react-native-carplay/lib/interfaces/MapButton.d.ts
+++ b/node_modules/react-native-carplay/lib/interfaces/MapButton.d.ts
@@ -11,6 +11,12 @@ export interface MapButton {
      * The image to display on the button.
      */
     image?: ImageSourcePropType;
+    /**
+     * Convoy: SF Symbol name to display on the button (e.g. "plus.magnifyingglass").
+     * Resolved natively via UIImage(systemName:) — works under bridgeless New Arch,
+     * where the RN asset `image` path resolves nil. Takes precedence over `image`.
+     */
+    systemImage?: string;
     /**
      * The image to display when focus is on the button.
      */
diff --git a/node_modules/react-native-carplay/lib/templates/MapTemplate.d.ts b/node_modules/react-native-carplay/lib/templates/MapTemplate.d.ts
index 442b0a5..7f21320 100644
--- a/node_modules/react-native-carplay/lib/templates/MapTemplate.d.ts
+++ b/node_modules/react-native-carplay/lib/templates/MapTemplate.d.ts
@@ -95,6 +95,46 @@ export interface MapTemplateConfig extends TemplateConfig {
     onPanEndedWithDirection?(e: {
         direction: string;
     }): void;
+    /**
+     * Convoy: raw multitouch map gestures.
+     * Pan-translation is available since iOS 12; zoom/rotate/pitch require iOS 26.
+     * `center` is in screen points on the CarPlay display. `scale` is the pinch
+     * scale factor (relative), `rotation` is in radians, velocities are per-second.
+     * Apple gates raw touch on some head units, so these may not fire everywhere.
+     */
+    onDidBeginPanGesture?(): void;
+    onDidUpdatePanGesture?(e: {
+        translation: { x: number; y: number };
+        velocity: { x: number; y: number };
+    }): void;
+    onDidEndPanGesture?(e: {
+        velocity: { x: number; y: number };
+    }): void;
+    onDidBeginZoomGesture?(): void;
+    onDidUpdateZoomGesture?(e: {
+        center: { x: number; y: number };
+        scale: number;
+        velocity: number;
+    }): void;
+    onDidEndZoomGesture?(e: {
+        velocity: number;
+    }): void;
+    onDidBeginRotationGesture?(): void;
+    onDidRotate?(e: {
+        center: { x: number; y: number };
+        rotation: number;
+        velocity: number;
+    }): void;
+    onDidEndRotation?(e: {
+        velocity: number;
+    }): void;
+    onDidBeginPitchGesture?(): void;
+    onDidPitch?(e: {
+        center: { x: number; y: number };
+    }): void;
+    onDidEndPitch?(e: {
+        center: { x: number; y: number };
+    }): void;
     onSelectedPreviewForTrip?(e: {
         tripId: string;
         routeIndex: number;
@@ -125,6 +165,18 @@ export declare class MapTemplate extends Template<MapTemplateConfig> {
         panWithDirection: string;
         panBeganWithDirection: string;
         panEndedWithDirection: string;
+        didBeginPanGesture: string;
+        didUpdatePanGestureWithTranslation: string;
+        didEndPanGestureWithVelocity: string;
+        didBeginZoomGesture: string;
+        didUpdateZoomGestureWithCenter: string;
+        didEndZoomGestureWithVelocity: string;
+        didBeginRotationGesture: string;
+        didRotateWithCenter: string;
+        rotationDidEndWithVelocity: string;
+        didBeginPitchGesture: string;
+        pitchWithCenter: string;
+        pitchEndedWithCenter: string;
         selectedPreviewForTrip: string;
         didCancelNavigation: string;
         startedTrip: string;
diff --git a/node_modules/react-native-carplay/lib/templates/MapTemplate.js b/node_modules/react-native-carplay/lib/templates/MapTemplate.js
index 839b590..8825596 100644
--- a/node_modules/react-native-carplay/lib/templates/MapTemplate.js
+++ b/node_modules/react-native-carplay/lib/templates/MapTemplate.js
@@ -28,6 +28,21 @@ class MapTemplate extends Template_1.Template {
             panWithDirection: 'onPanWithDirection',
             panBeganWithDirection: 'onPanBeganWithDirection',
             panEndedWithDirection: 'onPanEndedWithDirection',
+            // Convoy: raw multitouch map gestures. The pan-translation gesture is
+            // emitted natively but was never mapped to a callback; the zoom/rotate/
+            // pitch events are iOS-26 (see RNCarPlay.m + CPMapTemplate.h).
+            didBeginPanGesture: 'onDidBeginPanGesture',
+            didUpdatePanGestureWithTranslation: 'onDidUpdatePanGesture',
+            didEndPanGestureWithVelocity: 'onDidEndPanGesture',
+            didBeginZoomGesture: 'onDidBeginZoomGesture',
+            didUpdateZoomGestureWithCenter: 'onDidUpdateZoomGesture',
+            didEndZoomGestureWithVelocity: 'onDidEndZoomGesture',
+            didBeginRotationGesture: 'onDidBeginRotationGesture',
+            didRotateWithCenter: 'onDidRotate',
+            rotationDidEndWithVelocity: 'onDidEndRotation',
+            didBeginPitchGesture: 'onDidBeginPitchGesture',
+            pitchWithCenter: 'onDidPitch',
+            pitchEndedWithCenter: 'onDidEndPitch',
             selectedPreviewForTrip: 'onSelectedPreviewForTrip',
             didCancelNavigation: 'onDidCancelNavigation',
             startedTrip: 'onStartedTrip',
@@ -43,6 +58,13 @@ class MapTemplate extends Template_1.Template {
             android: ({ error } = {}) => {
                 error && console.error(error);
             },
+            // Convoy fix (2026-07-19): iOS had NO key here, so Platform.select
+            // returned undefined and createTemplate was invoked with an undefined
+            // callback. The legacy bridge tolerated that; the bridgeless
+            // TurboModule interop throws "TypeError: Object is not a function",
+            // which killed the MapTemplate constructor before setRootTemplate ran
+            // -> no template -> no CarPlay chrome -> no touch.
+            ios: () => { },
         });
         CarPlay_1.CarPlay.bridge.createTemplate(this.id, this.parseConfig({ type: this.type, ...config, render: true }), callbackFn);
     }
diff --git a/node_modules/react-native-carplay/lib/templates/Template.js b/node_modules/react-native-carplay/lib/templates/Template.js
index 5f6a243..a27d9ec 100644
--- a/node_modules/react-native-carplay/lib/templates/Template.js
+++ b/node_modules/react-native-carplay/lib/templates/Template.js
@@ -4,7 +4,17 @@ exports.Template = void 0;
 const react_native_1 = require("react-native");
 const CarPlay_1 = require("../CarPlay");
 // eslint-disable-next-line @typescript-eslint/no-var-requires
-const resolveAssetSource = require('react-native/Libraries/Image/resolveAssetSource');
+// Convoy fix (2026-07-19) — ROOT CAUSE of "no CarPlay chrome / touch-inert".
+// RN 0.81's resolveAssetSource.js ends in `export default resolveAssetSource`, so
+// require() returns the ESM namespace object { default: fn }, NOT the function.
+// traverse() below calls resolveAssetSource(...) on EVERY config key matching
+// /[Ii]mage$/ (our map buttons use `systemImage`), which threw
+// "TypeError: Object is not a function" inside parseConfig -> the MapTemplate
+// constructor died -> createTemplate/setRootTemplate never ran -> NO template was
+// ever created -> no nav bar, no map buttons, and nothing for CarPlay to route a
+// touch to. Verified against a local CarPlay-simulator reproduction.
+const _resolveAssetSourceMod = require('react-native/Libraries/Image/resolveAssetSource');
+const resolveAssetSource = _resolveAssetSourceMod.default || _resolveAssetSourceMod;
 class Template {
     config;
     get type() {
@@ -44,6 +54,10 @@ class Template {
                 android: ({ error } = {}) => {
                     error && console.error(error);
                 },
+                // Convoy fix (2026-07-19): see MapTemplate.js — iOS had no key, so
+                // createTemplate got an undefined callback, which the bridgeless
+                // TurboModule interop rejects with "Object is not a function".
+                ios: () => { },
             });
             CarPlay_1.CarPlay.bridge.createTemplate(this.id, this.parseConfig({ type: this.type, ...config }), callbackFn);
         }
diff --git a/node_modules/react-native-carplay/src/CarPlay.ts b/node_modules/react-native-carplay/src/CarPlay.ts
index be4f962..aa1033e 100644
--- a/node_modules/react-native-carplay/src/CarPlay.ts
+++ b/node_modules/react-native-carplay/src/CarPlay.ts
@@ -86,7 +86,9 @@ export interface InternalCarPlay extends NativeModule {
   reactToSelectedResult(status: boolean): void;
   updateListTemplateSections(id: string, config: unknown): void;
   updateListTemplateItem(id: string, config: unknown): void;
-  reactToUpdatedSearchText(id: string, items: unknown): void;
+  // Convoy fix (2026-07-20): matches the ACTUAL native selector
+  // `reactToUpdatedSearchText:(NSArray*)items templateId:(NSString*)templateId`.
+  reactToUpdatedSearchText(items: unknown, id: string): void;
   updateTabBarTemplates(id: string, config: unknown): void;
   activateVoiceControlState(id: string, identifier: string): void;
   // Android
@@ -154,7 +156,16 @@ export class CarPlayInterface {
   /**
    * CarPlay Event Emitter
    */
-  public emitter = new NativeEventEmitter(RNCarPlay);
+  // Convoy guard (2026-07-31): NativeEventEmitter throws on a null argument, and
+  // `export const CarPlay = new CarPlayInterface()` at the bottom of this file runs
+  // this field initialiser AT MODULE SCOPE — so `require('react-native-carplay')` in
+  // any JS context without RNCarPlay registered (headless background task, a
+  // partially-initialised bridge) is an uncaught throw at IMPORT. That reaches
+  // expo-updates' ErrorRecovery and triggers a ROLLBACK, so the whole app reverts to
+  // old JS because one optional car integration is unavailable. Degrade instead.
+  public emitter: any = RNCarPlay
+    ? new NativeEventEmitter(RNCarPlay)
+    : { addListener: () => ({ remove: () => {} }), removeAllListeners: () => {} };
 
   private onConnectCallbacks = new Set<OnConnectCallback>();
   private onDisconnectCallbacks = new Set<OnDisconnectCallback>();
diff --git a/node_modules/react-native-carplay/src/interfaces/BarButton.ts b/node_modules/react-native-carplay/src/interfaces/BarButton.ts
index b9891d2..319f5fe 100644
--- a/node_modules/react-native-carplay/src/interfaces/BarButton.ts
+++ b/node_modules/react-native-carplay/src/interfaces/BarButton.ts
@@ -9,6 +9,15 @@ interface BarButtonBase {
    * A Boolean value that enables and disables the bar button.
    */
   disabled?: boolean;
+  /**
+   * Convoy addition — CPBarButton.buttonStyle (iOS 14+, CPBarButton.h:63).
+   * 'rounded' draws the system's rounded background behind the label; 'none' is
+   * Apple's default (bare text). Size stays system-owned either way. Requires the
+   * matching native patch in RNCarPlay.m's parseBarButtons, so it only takes effect
+   * from the build that ships it.
+   * @namespace iOS
+   */
+  buttonStyle?: 'none' | 'rounded';
 }
 
 export interface BarButtonText extends BarButtonBase {
diff --git a/node_modules/react-native-carplay/src/templates/MapTemplate.ts b/node_modules/react-native-carplay/src/templates/MapTemplate.ts
index 13a66ff..1164d41 100644
--- a/node_modules/react-native-carplay/src/templates/MapTemplate.ts
+++ b/node_modules/react-native-carplay/src/templates/MapTemplate.ts
@@ -87,6 +87,11 @@ export interface MapTemplateConfig extends TemplateConfig {
   onPanWithDirection?(e: { direction: string }): void;
   onPanBeganWithDirection?(e: { direction: string }): void;
   onPanEndedWithDirection?(e: { direction: string }): void;
+  // Convoy: Android Auto action-strip / map-action-strip press (see eventMap).
+  onButtonPressed?(e: { buttonId: string }): void;
+  onDidBeginZoomGesture?(e: { templateId: string }): void;
+  onDidUpdateZoomGesture?(e: { center: { x: number; y: number }; scale: number; velocity: number }): void;
+  onDidEndZoomGesture?(e: { velocity: number }): void;
   onSelectedPreviewForTrip?(e: { tripId: string; routeIndex: number }): void;
   onDidCancelNavigation?(): void;
   onStartedTrip?(e: { tripId: string; routeIndex: number }): void;
@@ -115,6 +120,22 @@ export class MapTemplate extends Template<MapTemplateConfig> {
       panWithDirection: 'onPanWithDirection',
       panBeganWithDirection: 'onPanBeganWithDirection',
       panEndedWithDirection: 'onPanEndedWithDirection',
+      // Convoy fix (2026-07-23): PINCH-TO-ZOOM did nothing on CarPlay. RNCarPlay.m
+      // emits these iOS-26 CPMapTemplate zoom-gesture events, but they were never in
+      // this eventMap, so the base Template registered NO listener and the
+      // onDidBegin/Update/EndZoomGesture config callbacks were dead wiring. Pure JS
+      // (bundled from src/), so OTA-able.
+      // Convoy fix (2026-07-30): ANDROID AUTO action-strip / map-action-strip
+      // presses. The Kotlin side emits EventEmitter.ButtonPressed ("buttonPressed",
+      // RCTTemplate.parseAction -> setOnClickListener), but MapTemplate never mapped
+      // it, so every AA button press was dropped in JS before reaching a handler —
+      // the same omission that made pinch-to-zoom dead below. iOS is unaffected: it
+      // routes bar buttons through barButtonPressed and map buttons through
+      // mapButtonPressed, neither of which changes. Pure JS (bundled from src/), OTA-able.
+      buttonPressed: 'onButtonPressed',
+      didBeginZoomGesture: 'onDidBeginZoomGesture',
+      didUpdateZoomGestureWithCenter: 'onDidUpdateZoomGesture',
+      didEndZoomGestureWithVelocity: 'onDidEndZoomGesture',
       selectedPreviewForTrip: 'onSelectedPreviewForTrip',
       didCancelNavigation: 'onDidCancelNavigation',
       startedTrip: 'onStartedTrip',
@@ -132,6 +153,10 @@ export class MapTemplate extends Template<MapTemplateConfig> {
       android: ({ error }: { error?: string } = {}) => {
         error && console.error(error);
       },
+      // Convoy fix (2026-07-19): iOS had no key, so Platform.select returned
+      // undefined and createTemplate was called with an undefined callback, which
+      // the bridgeless TurboModule interop rejects.
+      ios: () => {},
     });
 
     CarPlay.bridge.createTemplate(
diff --git a/node_modules/react-native-carplay/src/templates/SearchTemplate.ts b/node_modules/react-native-carplay/src/templates/SearchTemplate.ts
index b52d6c8..530f8d3 100644
--- a/node_modules/react-native-carplay/src/templates/SearchTemplate.ts
+++ b/node_modules/react-native-carplay/src/templates/SearchTemplate.ts
@@ -49,7 +49,19 @@ export class SearchTemplate extends Template<SearchTemplateConfig> {
                 ...item,
                 image: item.image ? Image.resolveAssetSource(item.image) : undefined,
               }));
-              CarPlay.bridge.reactToUpdatedSearchText(e.templateId, parsedResults);
+              // Convoy fix (2026-07-20): ARGUMENT ORDER WAS REVERSED. The native
+              // selector is `reactToUpdatedSearchText:(NSArray*)items templateId:(NSString*)templateId`
+              // (RNCarPlay.m:910) — items FIRST. It is the only exported method that
+              // does not take templateId first, and this call site (plus the
+              // `(id, items)` type decl in CarPlay.ts) was written to the convention
+              // instead of the real selector. Native therefore got the templateId
+              // STRING where it expected an array and ran `for (NSDictionary *item in
+              // items)` over it -> unrecognized selector -> ObjC exception ->
+              // objc_exception_rethrow in performVoidMethodInvocation -> SIGABRT.
+              // Fires on the FIRST KEYSTROKE, so the CarPlay search keyboard killed
+              // the app every time. The legacy bridge tolerated the mismatch; the
+              // bridgeless TurboModule interop does not.
+              CarPlay.bridge.reactToUpdatedSearchText(parsedResults, e.templateId);
             }
           });
         }
diff --git a/node_modules/react-native-carplay/src/templates/Template.ts b/node_modules/react-native-carplay/src/templates/Template.ts
index d39678f..2d29edc 100644
--- a/node_modules/react-native-carplay/src/templates/Template.ts
+++ b/node_modules/react-native-carplay/src/templates/Template.ts
@@ -3,7 +3,18 @@ import { CarPlay } from '../CarPlay';
 import { BarButton } from '../interfaces/BarButton';
 
 // eslint-disable-next-line @typescript-eslint/no-var-requires
-const resolveAssetSource = require('react-native/Libraries/Image/resolveAssetSource');
+// Convoy fix (2026-07-19) — ROOT CAUSE of "no CarPlay chrome / CarPlay touch-inert".
+// RN 0.81's resolveAssetSource.js ends in `export default resolveAssetSource`, so
+// require() returns the ESM namespace object { default: fn }, NOT the function.
+// traverse() below calls resolveAssetSource(...) on EVERY config key matching
+// /[Ii]mage$/ — our map buttons use `systemImage` — which threw
+// "TypeError: Object is not a function" inside parseConfig, killing the
+// MapTemplate constructor before createTemplate/setRootTemplate ever ran. Result:
+// NO template was ever created, so CarPlay had no nav bar, no map buttons, and
+// nothing to route a touch to. NOTE: this package sets "react-native":
+// "src/index.ts", so Metro bundles THIS file — patching lib/ has no effect.
+const _resolveAssetSourceMod = require('react-native/Libraries/Image/resolveAssetSource');
+const resolveAssetSource = _resolveAssetSourceMod.default || _resolveAssetSourceMod;
 
 export interface BaseEvent {
   /**
@@ -129,6 +140,14 @@ export class Template<P> {
         android: ({ error }: { error?: string } = {}) => {
           error && console.error(error);
         },
+        // Convoy fix (2026-07-19): iOS had NO key here, so Platform.select returned
+        // undefined and createTemplate was invoked with an undefined callback. The
+        // legacy bridge tolerated it; the bridgeless TurboModule interop throws an
+        // ObjC exception -> SIGABRT. This path runs for every NON-MAP template
+        // (AlertTemplate, SearchTemplate, ListTemplate...), so tapping the CarPlay
+        // Police button crashed the app the moment it tried to show its confirmation
+        // alert. Same fix already applied in MapTemplate.ts.
+        ios: () => {},
       });
       CarPlay.bridge.createTemplate(
         this.id,
@@ -153,7 +172,15 @@ export class Template<P> {
         }
         if (String(i).match(/[Ii]mage$/)) {
           // eslint-disable-next-line @typescript-eslint/no-unsafe-call
-          obj[i] = resolveAssetSource(obj[i]);
+          // Convoy fix (2026-07-19): SF-symbol names (systemImage: 'mic.fill',
+          // 'plus.magnifyingglass') are plain STRINGS and must pass through
+          // untouched. resolveAssetSource() returns null for a string (it expects a
+          // require() id or {uri}), so native received a NIL image and threw an
+          // ObjC exception inside the TurboModule call -> SIGABRT. Only resolve
+          // real asset references.
+          if (typeof obj[i] !== 'string') {
+            obj[i] = resolveAssetSource(obj[i]);
+          }
         }
       }
     }
diff --git a/node_modules/react-native-carplay/src/templates/android/AndroidNavigationBaseTemplate.ts b/node_modules/react-native-carplay/src/templates/android/AndroidNavigationBaseTemplate.ts
index 5f6c067..7bef181 100644
--- a/node_modules/react-native-carplay/src/templates/android/AndroidNavigationBaseTemplate.ts
+++ b/node_modules/react-native-carplay/src/templates/android/AndroidNavigationBaseTemplate.ts
@@ -19,6 +19,13 @@ export class AndroidNavigationBaseTemplate<
 > extends Template<T> {
   get eventMap() {
     return {
+      // Convoy fix (2026-07-30): ANDROID AUTO action-strip / map-action-strip
+      // presses. RCTTemplate.parseAction wires
+      // setOnClickListener { eventEmitter.buttonPressed(id) }, but this base
+      // template never mapped "buttonPressed" — so every press on a
+      // NavigationTemplate action was dropped in JS before reaching a handler.
+      // Identical omission to the one fixed in MapTemplate's eventMap.
+      buttonPressed: 'onButtonPressed',
       didShowPanningInterface: 'onDidShowPanningInterface',
       didDismissPanningInterface: 'onDidDismissPanningInterface',
     };
