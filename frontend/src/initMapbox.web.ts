// initMapbox.web.ts — no-op on web. @rnmapbox/maps is native-only; this stub
// keeps it out of the web bundle (mirrors the ConvoyMap.web / applePlayer
// platform-file pattern). The web map continues to use its own renderer.
export {};
